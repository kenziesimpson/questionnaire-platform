/**
 * Drizzle schema — intentionally empty scaffolding.
 *
 * The domain schema (questionnaires, questions, versions, rules, sessions,
 * responses) depends on decisions not yet made: design-doc §5 (questionnaire
 * format), §6 (versioning/immutability), §7 (branching rules), §12 (database).
 * See docs/4-implementation-plan.md Phase 0.
 *
 * `drizzle-kit generate` against this file produces no migrations until
 * tables are added here — that's expected at this stage.
 */
