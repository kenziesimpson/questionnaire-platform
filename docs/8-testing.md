# Testing — Detailed Design

> Detail doc for [[2-design-doc#15. Testing]]. The design doc carries the condensed version; this is the layer-by-layer reasoning, the database strategy, the pipeline shape and the decisions behind them.
> Related: [[5-questionnaire-format]] (the rule engine and publish-time validation under test), [[7-application-boundary]] (the barrier the integration layer proves), [[6-observability#3. Respondent answers must never enter telemetry]] (the canary sentinel test), [[2-design-doc#13. Deployment]] (the compose stack end-to-end runs against).

## 1. What we are optimizing for

The brief asks for "automated tests on important domain behavior — **versioning and conditional navigation above all**." Tests are a graded deliverable here, not hygiene, which changes what the suite is for: it exists to make the design's claims checkable by someone who has not read the code.

So the organizing question for each test is *which claim does this make falsifiable?*

| Question a reviewer will ask | Layer that answers it |
| --- | --- |
| Does branching pick the right next question, for rules over several earlier answers? | Rule-engine unit tests (§2.1) |
| Is a published version *really* immutable? | Integration test that bypasses the API and writes to the database directly (§2.2) |
| Do responses collected under v1 still mean what they meant once v2 publishes? | Integration test across a publish boundary (§2.2) |
| Is the definition/execution boundary real or just a URL prefix? | Plugin-isolation test + a grant test that asserts `qp_definition` cannot read `response` (§2.2) |
| Does the whole workflow actually work? | One Playwright spec per graded flow (§2.4) |
| Can a respondent's answer leak into telemetry? | The canary sentinel test (§2.5) |

Three invariants are load-bearing, and every layer below exists to defend one of them:

1. **A published version is immutable.** Enforced in three places ([[2-design-doc#6. Versioning & Immutability]]), so it needs a test per place, not per code path.
2. **A response's meaning is pinned to the version it was collected under.** Stable option ids and `{ value, unit }` are only guarantees if something checks them across a republish.
3. **The server is the authority on the reachable path.** The client evaluates branching locally for interactivity; if the two evaluators can disagree, the server's answer is the one that must hold.

**Explicit non-goal: a coverage percentage.** A number would be satisfied by testing the easy surface — serializers, config, getters — and the graded behavior is concentrated in a small amount of intricate code. Coverage is collected and read, not gated. See §8.6.

## 2. Layers

| Layer | Runner | Needs | Speed | Count |
| --- | --- | --- | --- | --- |
| Domain unit (shared package) | Vitest | nothing | ms | most of the suite |
| Backend integration | Vitest + `app.inject()` | Postgres (Testcontainers) | ~seconds | one file per route group |
| Frontend component | Vitest + RTL + jsdom | nothing | ms | the renderer and the form |
| End-to-end | Playwright | composed stack | ~tens of seconds | three specs |

### 2.1 Domain unit — the shared package

The rule engine, publish-time validation, the snapshot `formatVersion` upgrade, and the answer canonicalization behind the submit digest ([[2-design-doc#17. Decisions Log]] #19) are all pure functions in `packages/shared`. No Fastify, no database, no fixtures beyond literals.

**The shared package means this is tested once and exercised twice.** Client and server run the *same* evaluator, so a single suite covers both — a property of the architecture rather than a testing convention. Two evaluators would need two suites and could still drift in the gap between them.

Table-driven with `it.each`, because the input space is a product of small sets rather than a list of scenarios:

- Every operator against every response type it is defined for, plus the negative cases the discriminated union is supposed to make unrepresentable (a date operator against a number answer should not compile; a test asserts the type error via `@ts-expect-error` so the guarantee is visible rather than implied).
- **Absent-answer semantics:** a condition on a question that was not shown is `false` for every operator. This is the rule most likely to be broken by a well-meaning refactor and the cheapest to pin down.
- `all` / `any` grouping, including the single-condition and empty-group edges.
- Publish-time validation: forward references, domain-intersection satisfiability, referential integrity ([[5-questionnaire-format#5. Publish-time validation]]).
- The **medical-condition demo** as explicit cases — see §3.

### 2.2 Backend integration — Fastify `inject()` against a real Postgres

**`app.inject()` is the whole technique.** Fastify's built-in `light-my-request` integration runs a synthetic request through the complete lifecycle — hooks, schema validation, handler, serializer, error handler — in-process, with no socket and no port. Route tests end up as fast as unit tests and need no lifecycle management beyond `await app.close()`.

The one prerequisite is that `apps/backend/src/index.ts` splits into a `buildApp(opts)` factory and a thin `server.ts` that calls `listen()`. Worth doing regardless: it is also what lets a test build an app with one plugin registered and nothing else.

**Plugin encapsulation is the test boundary.** Registering only the execution plugin on a bare Fastify instance and driving a session to submission proves the execution half needs nothing from the authoring half — which is the boundary claim in [[7-application-boundary#3.1 Module encapsulation]], mechanically checked instead of asserted. The reverse direction is a lint rule; this is the runtime half of the same argument.

**The database is real, and not mocked.** The invariants under test *are* database objects:

| Under test | Database object |
| --- | --- |
| Republishing a frozen version fails | The immutability trigger |
| At most one draft per questionnaire | The partial unique index |
| The audit trail cannot be rewritten | `qp_definition`'s zero grant on `audit.event`, behind the `SECURITY DEFINER` `audit.record()` function |
| Authoring cannot read medical answers | `qp_definition`'s missing grant on `response` |

A mocked repository passes every one of those with the constraint absent, which makes it worse than no test. `pg-mem` is out for the same reason at one remove: triggers, roles and the JSONB operators the snapshot index relies on are exactly what it does not faithfully implement. Strategy in §4.

**Repository tests and route tests own different things.** `_tests/db/**` calls the repository functions directly and owns what the database decides: the outcome each command returns, the row locks it takes and the order it takes them in, the audit rows it writes, and the rows left standing when it refuses. `_tests/modules/**` drives the same work through `inject()` and owns what the wire decides: the status code, the problem type and the pointers or items it carries, and the response headers — `ETag`, `Cache-Control` and `Content-Type`. A scenario belongs to one layer, not both: where a route maps a refusal the repository already proves, it asserts that mapping once, on the simplest setup that produces the refusal, instead of repeating the repository's scenario through HTTP.

Execution has no repository layer of its own. Nothing outside `src/modules/execution` calls `src/db/execution`, so its persistence is exercised through its routes: those tests live in `_tests/modules/execution/`, beside `useExecutionApp`, which is the one place the execution app is built.

The three-layer immutability story ([[5-questionnaire-format#6.4 Three layers of immutability enforcement]]) is exercised here, and the third layer *is* this test. **API 409** via `inject`, the **database trigger** via a direct client bypassing the API entirely — and that bypass is layer three, the one place a test deliberately goes around the application, and the reason the guarantee cannot silently regress when the service layer is refactored. Draft uniqueness is a separate invariant with its own row above, exercised by two concurrent draft creations.

### 2.3 Frontend component — Vitest + React Testing Library

Vitest rather than Jest, despite Jest being the more familiar tool here: Vitest reads the existing `vite.config.ts`, so there is one transform and module-resolution config in the repo instead of two, and ESM and TypeScript need no additional plumbing. The authoring API is Jest's — `describe`/`it`/`expect`, `vi` where Jest has `jest` — and React Testing Library sits on top unchanged. `jsdom` as the environment. See §8.1.

Worth testing at this layer: each response type's input renders and reports its value; required-ness produces the validation message; the next-question decision the UI makes matches the shared engine — imported, not reimplemented, so the test cannot encode a second opinion about branching.

Not worth testing here: layout, styling, and anything §2.4 covers better by exercising the real stack.

### 2.4 End-to-end — Playwright against the composed stack

Playwright runs against `docker compose up` through nginx, so what is under test is the artifact a reviewer will run: production images, the proxy, and the `migrate` service gating `backend`. CI must use `docker compose -f docker-compose.yml` explicitly, or the committed dev override swaps in the Vite dev server and the test stops resembling the deliverable.

Headless, Chromium only. Cross-browser coverage is not what this suite is for.

**Three specs, deliberately.** End-to-end tests are where suites rot, and each one here is chosen because it is the only layer that can prove its claim:

1. **The mandatory branching demo.** Author a questionnaire, publish it, answer *yes* to the medical-condition question and get the condition and diagnosis-date questions; a second session answering *no* skips both and lands on the next common question.
2. **Resume.** Abandon a part-finished session, return, and continue from where it stopped with prior answers intact.
3. **The v2 publish boundary.** Publish a second version, then confirm the v1 response still renders with v1's prompts and still aggregates on `questionId`.

Spec 1 drives the admin UI for authoring, because authoring *is* what it is testing. Specs 2 and 3 seed through the definition API in a fixture — clicking through authoring to arrange a precondition is the main way these suites become slow and brittle.

Queries use `getByRole` and `getByLabelText` rather than test ids wherever the accessible name exists, which makes the form's labelling a tested property as a side effect.

### 2.5 The telemetry canary — cross-cutting

Already decided in [[6-observability#3.1 Enforcement ladder]] (Layer 2) and restated here because it lives in this suite and gates CI: the harness installs an in-memory span exporter and pino destination, runs the branching flow with a sentinel answer value, and asserts the sentinel appears in zero spans, zero log records and zero metric attributes.

Its value is that it rides on whatever the integration suite already exercises, including paths added after it was written — the property grep-based checks cannot have.

## 3. Required coverage — the graded list

Each capability the brief grades, and the layer that proves it. This table is the argument that the suite is complete; §7 is where it becomes individual cases.

| Behavior | Layer |
| --- | --- |
| Questionnaire versioning: publish, immutability, one draft | Integration (§2.2), three layers |
| Question versioning: append-only, items pin a version at add time | Integration |
| Conditional navigation over one *and* several earlier answers | Unit (§2.1), exhaustive |
| Absent-answer semantics | Unit |
| Publish-time rule validation: forward refs, unsatisfiable conditions | Unit |
| Response validation: required, per-type value rules | Unit + integration (edge rejection) |
| Sessions: start, resume, version pinning | Integration + E2E spec 2 |
| Submit idempotency: same digest replays, different digest conflicts | Integration |
| Server as authority: an answer to an unreachable item is rejected | Integration |
| Path re-evaluation uses the *pinned* definition, not the latest published | Integration (§6, predicate-change fixture) |
| Retirement: start and submit rejected past `closes_at` | Integration, with frozen time |
| Response meaning preserved across a republish | Integration + E2E spec 3 |
| No respondent answer in telemetry | Canary (§2.5) |
| The definition/execution barrier | Plugin isolation + grant test (§2.2) |
| Duplicate `option_ids` inside one answer — rejected by the submit validator, **not** by a constraint | Unit + integration. The one `response` invariant not enforced below the application ([[2-design-doc#17. Decisions Log]] #34), so a test is the only thing holding it |
| Relative date constraints across a timezone boundary | Unit. The evaluator takes `today` as a parameter, so client-local and server-UTC-plus-tolerance are both plain cases with no clock to mock ([[5-questionnaire-format#2.4 Relative date constraints resolve against two different clocks]], #38) |
| List endpoints return a deterministic order | Integration. Unspecified order is heap order and shifts after an `UPDATE`, which is what makes list assertions flaky rather than merely unordered ([[2-design-doc#17. Decisions Log]] #40) |

**The mandatory demo scenario is tested at two layers on purpose.** The brief asks for automated tests covering it specifically; the rule-engine cases cover it exhaustively and in milliseconds, and one Playwright spec proves the same thing is true of the running system. Neither alone is a satisfying answer to "show me it works."

## 4. Postgres for integration tests — Testcontainers

**Decision:** `@testcontainers/postgresql` starts one `postgres:16-alpine` container (matching compose) in a Vitest `globalSetup`. Migrations run once into a template database; each Vitest worker then creates its own database from that template.

```sql
CREATE DATABASE qp_test_3 TEMPLATE qp_test_template;
```

Three things fall out of that shape:

- **`npm test` stays one command.** No `docker compose up` first, no README step that only applies to contributors, and CI behaves identically to a laptop.
- **Parallelism survives.** Vitest runs test files in parallel workers. One shared database plus `TRUNCATE` between tests means workers clobber each other's rows mid-test; a database per worker is real isolation. `CREATE DATABASE ... TEMPLATE` is close to instant and skips re-running migrations per worker, so the isolation is nearly free.
- **Within a worker**, `TRUNCATE ... RESTART IDENTITY CASCADE` between tests. Wrapping each test in a transaction and rolling back is tidier in the abstract but breaks precisely where the important tests are: the publish path opens its own transaction and takes row locks, and the immutability bypass test needs a second connection that can see committed state.

**A worker's connection budget is bounded by its concurrency, never by its test count** ([[2-design-doc#17. Decisions Log]] #77). This is the flake that took the longest to find: nothing in the harness is racy, but `postgres:16-alpine` ships `max_connections = 100` with three slots reserved for superusers, and the suite used to spend them. Every `testDatabase.connect(role)` stayed open until the file's `afterAll`, so a file that opened a client per test held one connection per test — 23 in `response-shape`, 17 in `immutability` — on top of a ten-connection pool per role and a fresh owner connection for every `TRUNCATE`. Multiply by the worker count and the ceiling arrives partway through the run, on whichever file happens to be running: a dozen or so failures out of 328, a different dozen each time, none of them reproducible from the file alone. So three rules hold in `_tests/db/harness.ts`:

- **`connect(role)` is test-scoped.** Its clients close in `afterEach`, not `afterAll`. A test may open as many as it needs; a file may not accumulate them.
- **One maintenance connection per file.** The per-test `TRUNCATE` and `readAuditEvents` share a single lazily-opened `qp_owner` client instead of opening and closing one per test.
- **Pools are capped** at `CONNECTIONS_PER_POOL`, above what the concurrency tests need (two racing publishes plus a lock holder) and far below `pg`'s default of ten.

A file's peak falls from 18–28 connections to five or six, and stops growing when a test file does. Two numbers, because they answer different questions: the whole suite peaked at 28 with twelve workers, an average near two, because files do not hit their peaks at the same moment — but the number to size against is the per-file peak, since they *may* coincide. Five or six against the 97 slots `postgres:16-alpine` leaves a non-superuser puts the ceiling near nineteen simultaneous workers, so `maxWorkers = availableParallelism() - 1` has room to roughly a 20-core machine before the budget needs revisiting. That is the trade #77 took in exchange for not capping `maxWorkers`, and it is written down here so the next person meets a number rather than a surprise. The guard that matters most is the cheapest: `VITEST_POOL_ID` missing is a hard error rather than a fallback to `qp_test_1`, because the fallback is every worker silently sharing one database.

**The roles come from the same script compose uses.** The container needs `qp_owner`, `qp_definition`, `qp_execution` and `audit_owner` before migrations run, and compose's one-shot `roles` service, which provisions them there, does not run here. `globalSetup` runs `db/init/01-roles.sh` inside the container with `execInContainer` — the same file, not a copy, because a reimplementation would let the grant tests in §3 pass against roles that differ from what ships ([[9-database-schema#11.3 Roles are not schema, and must not be in a committed migration]]).

**Escape hatch, and it is not only a convenience:** if `TEST_DATABASE_URL` is set, the suite uses it and skips the container entirely. It must be an **admin** URL — a role that can create databases, such as the bootstrap superuser of a throwaway instance whose roles `db/init/01-roles.sh` has already created. The suite uses it only to create the template and per-worker databases, owned by `qp_owner`. Migrations still run as `qp_owner`, using `QP_OWNER_PASSWORD`, so default privileges attach to the principal that creates the tables, and neither application role nor the shipped `qp_owner` needs `CREATEDB`. That makes the suite portable to any CI that can supply a Postgres service container but not a Docker socket (§5.3), and locally it points at the compose `db` for a faster inner loop.

Stated honestly in the README: the default path needs a running Docker daemon.

## 5. Running headless — local, PR, deploy

### 5.1 One command

A root `vitest.config.ts` with `projects: ['packages/*', 'apps/*']` makes `vitest run` at the repo root execute the shared, backend and frontend suites together, each with its own environment. (Vitest 5 configures this in the root config; the standalone `vitest.workspace.ts` file no longer exists.)

- `npm test` → `vitest run`. Non-watch, proper exit code, no browser.
- `npm run test:e2e` → `playwright test`, deliberately separate. Playwright is not a Vitest project and folding it in would make the fast suite as slow as the slow one.

### 5.2 Pipeline shape

Two jobs, so a typo does not wait on an image build:

1. **Checks** — `lint`, `typecheck`, `vitest run`. Testcontainers supplies its own Postgres, so the job needs no service definition. The telemetry canary gates here.
2. **End-to-end** — build images, `docker compose -f docker-compose.yml up -d --wait`, `playwright test`.

`--wait` blocks until healthchecks pass, which is only as good as the healthchecks: `db` has one today, `backend` and `frontend` do not, so `--wait` currently proves those containers started rather than that they serve. Adding a `backend` healthcheck on `/health/ready` — which [[6-observability#8. SLOs and alerting]] wants for the Kubernetes probes anyway — is what makes step 2 deterministic instead of racy.

### 5.3 CI details that actually bite

- **Testcontainers needs a reachable Docker daemon.** Standard hosted Linux runners have one and need no extra configuration. Daemonless or Kubernetes-based runners need docker-in-docker or a remote Docker host. Testcontainers also starts a *Ryuk* reaper sidecar to clean up orphaned containers; where sidecars are disallowed, `TESTCONTAINERS_RYUK_DISABLED=true`. Pin the image tag so pulls cache rather than resolving a moving tag on every run.
- **Playwright browsers** come from either `npx playwright install --with-deps chromium` or the official Playwright container image — whose tag must match the installed Playwright version exactly, since the image ships the browsers that version expects.
- **Flake control:** retries in CI only, `trace: 'on-first-retry'`, `reducedMotion: 'reduce'`, and `playwright-report/` uploaded as a job artifact. The trace viewer turns a red pipeline into a DOM timeline of the failure, which is most of why Playwright is worth the setup (§8.5).
- **CI is the gate.** The `lefthook` pre-commit hook running lint and typecheck on staged files is convenience, because `--no-verify` exists — the same position [[6-observability#3.1 Enforcement ladder]] takes.

### 5.4 The end-to-end suite is also the deploy smoke test

The specs need only a `baseURL`, so the same three can run against a deployed environment after a rollout. That is the honest answer to "how would this work in Kubernetes" — a post-deploy job, not a second suite — and it is the same code the synthetic canary in [[6-observability#12. Change correlation and synthetics]] would put on a timer.

## 6. Test data and fixtures

- **The seeded demo questionnaire has one definition**, a builder in `packages/shared` used by both the `migrate`/seed container and the test fixtures. If the demo that ships and the fixture under test are separate literals, they drift, and the drift surfaces as a passing suite over a broken demo.
- **Factories, not fixture files.** Small builders with overrides — `aQuestionnaire({ items: [...] })` — so each test states only what it varies. Randomized data stays out of assertions; deterministic seeds only.
- **Freeze time** (`vi.setSystemTime`) for the `closes_at` cutoff tests. Nothing sleeps.
- **The telemetry sentinel is an exported constant**, not a string retyped per test, so the canary cannot pass because someone fixed a typo.
- **A predicate-change fixture, kept separate from the seeded demo.** The demo's version 2 is a relabel and deliberately does one thing ([[5-questionnaire-format#3.1 Version 2 — the demo change]]), which means its two versions have an identical reachable path — so it cannot prove that submit evaluates against the *pinned* definition rather than the latest. That claim gets its own two-version fixture, where v2 tightens `itm_03`'s predicate to `all: [ has_condition is yes, which_condition is not other ]`:
  - a session started **before** the publish, pinned to v1, submits `yes` / `other` / a diagnosis date and is **accepted** — the date is required on v1's path;
  - a session started **after** it, pinned to v2, submits the identical answers and is **rejected** `422` for an answer to an unreachable item, naming `itm_03` and never echoing the date ([[7-application-boundary#5.5 Error bodies must not echo answers]]).

  Same answers, opposite outcomes, and the only variable is which snapshot the session pinned. This is also the cheapest test of the republish-while-in-flight claim in [[2-design-doc#8. Sessions & Responses]].

## 7. Test case enumeration

**Empty — to fill in during the build.** One row per case, grouped by the layers in §2, each naming the invariant it defends and the requirement from §3 it discharges. Written alongside each feature rather than afterwards, per the repo's standing rule that a versioning or branching change arrives with its test.

Tracked as a Phase 2 item in [[4-implementation-plan]].

### Wave 1a — the contract

**Domain unit — `packages/shared`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `ResponseType` and `QuestionVersionSummary.type` are typed as exactly the five response types, never `never`, and the schema admits exactly `RESPONSE_TYPES` | `domain/schemas.test.ts` | A union mapped from an array is `never` to TypeBox's tuple-recursive `Static`; closed unions are `Type.Enum` over their value list, so no cast is needed | — contract |
| `validateDraft`'s item `code` is typed as exactly `DraftItemCode` and admits every draft item code and no submission code | `api/api.test.ts` | Same: handlers can return the validation result without a cast | — contract |
| `ProblemDetails.type` is typed as `ProblemType` (the URL of each slug), `items[].code` as the draft and submission codes; `problemType(slug)` is typed as that slug's URL; the schema admits exactly those values | `problems.test.ts` | Same, and the problem `type` stays closed at compile time as well as at runtime | — contract |
| The documented v1 snapshot validates as `PublishedDefinition` | `domain/schemas.test.ts` | The format in [[5-questionnaire-format#3. Serialization]] is what the schema accepts | Response meaning preserved across a republish |
| The v2 relabel (question version 4, same `opt_hyperten` id) validates | `domain/schemas.test.ts` | #26: v2 differs by one label and nothing structural | Response meaning preserved across a republish |
| Snapshot rejects `formatVersion` 2, `yes_no`, a `questionId`-keyed condition, `question.key`, a slug `questionnaireId`, a nested predicate, a mistyped operator, a missing `numberKind`, an impossible date bound, an empty option list | `domain/schemas.test.ts` | A stale or corrupt snapshot fails loudly on load (§6.5); #35, #36, #41 hold at the schema | Conditional navigation over one and several earlier answers |
| Each condition type accepts its own operators and operands and rejects content matching, list/scalar operand swaps, string number operands, cross-type operands, empty option lists | `domain/schemas.test.ts` | Conditions typed per response type (#9) | Conditional navigation over one and several earlier answers |
| `@ts-expect-error`: a date operator on a number condition, a text `is`, a `questionId`-keyed condition do not compile | `domain/schemas.test.ts` | Invalid comparisons are unrepresentable, not a runtime class (#9, #41) | Conditional navigation over one and several earlier answers |
| Empty `all` / `any` groups are representable; a predicate with both keys is not | `domain/schemas.test.ts` | Single grouping level; empty-group edges stay testable for the engine | Conditional navigation over one and several earlier answers |
| A number answer is a decimal string; a JSON number, a client-sent `unit`, and non-canonical decimals (`1e3`, `01`, `+1`, `1.`, `.5`) are rejected | `domain/schemas.test.ts` | #42: exactness survives the wire; the unit comes from the pinned version | Response validation: required, per-type value rules |
| Duplicate `optionIds` pass the answer schema | `domain/schemas.test.ts` | #34: duplicates are the submit validator's `422` naming the item, not a schema `400` | Duplicate `option_ids` inside one answer |
| Empty text answer rejected; answers keyed by `itemId` slug with explicit `null` allowed, uuid keys rejected | `domain/schemas.test.ts` | #41 keying; `response_shape`'s non-empty text rule | Response validation: required, per-type value rules |
| The validated `Answer` row stores `single_choice` as a one-element `optionIds` and carries the server-filled unit | `domain/schemas.test.ts` | The digest input is exactly the persisted row (#37) | Submit idempotency |
| The yes/no template is an ordinary `single_choice` with editable labels; a save request cannot carry `questionId` | `domain/schemas.test.ts` | #36; identity is server-assigned (#13) | Question versioning |
| The problem slug set and statuses are exactly §6.1's closed union; type URIs round-trip | `problems.test.ts` | Closed slug union (#20) | — contract |
| Every constructed problem body validates against the wire schema; a body carrying an extra `value` field does not | `problems.test.ts` | Error bodies never echo an answer (§5.5 of [[7-application-boundary]]) | No respondent answer in telemetry |
| `@ts-expect-error`: `submission/invalid` without items, a draft code on a submission item, `internal` without a correlation id, an invented slug | `problems.test.ts` | Extensions are required per slug and codes cannot cross slugs | — contract |
| `Sensitive` redacts under `JSON.stringify`, template, `String()`, concatenation, `util.inspect` (`showHidden`), `util.format`, `Error` messages, spread and `Object.entries`; `unwrap()` returns the value | `sensitive.test.ts` | Layer 0 of [[6-observability#3.1 Enforcement ladder]] | No respondent answer in telemetry |
| The route table is exactly the 18 + 3 routes of [[7-application-boundary]] §4.1 / §5.1, each with `4xx` / `5xx` problem schemas | `api/api.test.ts` | Contract completeness; error serialization is contractual | The definition/execution barrier |
| No execution route path names a questionnaire | `api/api.test.ts` | #18: no unpinned definition read | Sessions: start, resume, version pinning |
| `PUT /draft` and `POST /publish` require `If-Match`; other headers pass | `api/api.test.ts` | #43: a missing header is a schema `400`, never an unconditional write | Questionnaire versioning: publish, immutability, one draft |
| Submit body keyed by `itemId` and the typed receipt validate | `api/api.test.ts` | §5.4 receipt is the session row | Submit idempotency |
| Draft ETag round-trips `W/"<versionId>:<draftRevision>"` and rejects malformed forms | `api/api.test.ts` | #43 | Questionnaire versioning: publish, immutability, one draft |
| `isDraftEtagFor` holds for that draft version at any revision and in either case, and fails for a missing, malformed or strong ETag and for one naming another version | `api/api.test.ts` | One ETag check for the admin client and the e2e fixtures, so neither can write back an ETag it never read (#43) | Questionnaire versioning: publish, immutability, one draft |
| `snapshotEtag` is the strong `"<questionnaireId>:<version>:<formatVersion>"`, which the draft ETag parser refuses | `api/api.test.ts` | #44: the snapshot ETag carries `formatVersion`. It is strong, so if a client writes it into `If-Match` it cannot pass for a draft ETag | Questionnaire versioning: publish, immutability, one draft |
| `routePath` percent-encodes path parameters and throws naming a missing parameter, not a value; a parameter name may hold inner underscores but not a trailing one; `""`, `"."` and `".."` are refused, and no value survives `new URL()` outside its own segment | `api/request.test.ts` | A session id cannot break out of its path segment. `encodeURIComponent` leaves `.` and `..` alone, so the one helper every client now goes through is where that is caught | — contract |
| `routeSearch` drops undefined values and encodes byte for byte as `URLSearchParams` does, over every ASCII character and a sample of non-ASCII ones; `successSchemaOf` answers the route's schema for a declared status, `undefined` for an undeclared one, and `@ts-expect-error`: `"4xx"` and `"5xx"` are not statuses and do not compile | `api/request.test.ts` | One transport-free builder for every client's URLs and response checks; the problem schemas are unreachable by name rather than by convention ([[7-application-boundary#6.3 Input validation]]); L6 keeps `routePath` the only path builder | — contract |
| `problemFromWire` reads every slug and its extensions back into the typed `Problem`, takes title and status from the slug rather than the body, and refuses a non-problem body, an answer-carrying body, a malformed envelope, a type outside the union, a missing required extension, and a body carrying any code this build does not know — whether the code is newer than the build or simply wrong for the slug. The closed `ProblemDetails` stays closed, so a route still refuses to serialize an unknown code | `problems.test.ts` | One parser for the closed RFC 9457 union (#20), refusing a body it cannot read whole rather than rendering a partial error list (#81); L10 keeps it the only one | — contract |
| Every member `ProblemDetails` declares beyond `type`, `title`, `status`, `detail` and `instance` is read by some `ProblemExtensions` entry, and every entry names a slug in the closed set | `problems.test.ts` | `ProblemDetails` and `ProblemExtensions` are independent declarations: adding an optional member to the wire schema type-checks while `problemFromWire` silently discards it for every client. Nothing but this test connects them | — contract |
| The other option is the option whose id is `OTHER_OPTION_ID` and that is freeform, in either choice type; a non-freeform option under that id, one with `freeform: false`, a freeform option under another id and a question without options have none | `domain/question.test.ts` | One definition of the other option for the engine, the renderer and admin ([[2-design-doc#17. Decisions Log]] #82) | Response validation: required, per-type value rules |
| `isChoiceQuestion` holds for exactly `single_choice` and `multiple_choice`; `optionIdsOf` lists a question's option ids in authored order and none for a question without options; `questionInputOf` strips a pinned question's identity and what is left validates as `QuestionInput` | `domain/question.test.ts` | The shared vocabulary replaces the local copies, so it has to mean what they meant | Question versioning |
| `OPERATORS_BY_TYPE` is exactly format §4.2's operator list per type, in the editor's order, and lists every operator the `Condition` schema admits for each type and no other; `conditionsOf` reads an `all` group, an `any` group, an empty group and no predicate; `referencedOptionIds` names the options a choice condition compares against and none for text, number and date | `domain/condition.test.ts` | The editor, the validator and the schema cannot disagree about which operators exist (#9) | Conditional navigation over one *and* several earlier answers |
| `draftItemOf` places the same question version with the same placement, by reference; `draftForValidation` of a published definition carries every item by reference and every pinned version, and passes `validateDraft` | `domain/draft.test.ts` | One `Item` → `DraftItem` projection for the publish path, the seed and the fixtures | Publish-time rule validation: forward refs, unsatisfiable conditions |

**Domain unit — `packages/telemetry`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| Literal messages, closed context, typed domain events and closed span names are accepted | `index.test.ts` | Layer 1: a typed boundary with a closed field set | No respondent answer in telemetry |
| `@ts-expect-error`: an interpolated message, a message in a `string` variable, an unknown context field, a free-text rejection reason, an invented span name | `index.test.ts` | No shape exists that an answer value could ride in on | No respondent answer in telemetry |

**Repo configuration — `tests/`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `pino`, `pino/*`, `pino-*` and `@opentelemetry/*` (including type-only) are rejected outside `packages/telemetry`, including inside backend modules, which inherit the base patterns; allowed inside it | `tests/lint-boundaries.test.ts` | Layer 1 lint rule | No respondent answer in telemetry |
| `modules/definition` rejects sibling, deep-relative, `modules/`-path and type-only imports of `modules/execution`, including through `./` segments and doubled slashes, and the reverse; `@qp/shared`, in-module imports and look-alike paths pass | `tests/lint-boundaries.test.ts` | [[7-application-boundary#3.1 Module encapsulation]] | The definition/execution barrier |
| ESLint warns on a double type assertion — `as unknown as T`, `as any as T`, or `<unknown>x as T` — in every workspace; a single assertion, widening to `unknown` alone and `as const` pass; a disable comment with a reason silences it and counts as used | `tests/lint-assertions.test.ts` | A cast through `unknown` or `any` compiles for any two types; the two remaining exceptions (`@fastify/ajv-compiler`'s mistyped compiler, Web Crypto globals in `@qp/shared`) carry their reason at the call site | — code quality |
| A `:param` regex is rejected in the admin and respondent clients, the e2e fixtures and specs, the backend and the tool config files, anchored (`/:(\w+)/`) or behind a path separator (`/\/:(\w+)/`), and allowed only inside `routePath`'s own module, which still warns on a double assertion, a default export and a hand-built problem guard. A non-capturing group and the draft ETag pattern, which both contain `:(`, pass; a pattern built from a string is out of reach | `tests/lint-route-paths.test.ts` | L6: route paths are built by one helper, so `:param` has one spelling | — code quality |
| Checking a body against `ProblemDetails` by hand and a hand-built item-code or pointer-error guard are rejected across every `src` directory and across `e2e`, including `apps/backend/src`, whose raw SQL and connection restrictions still apply beside them and whose schema and connection constructor keep their own exemptions; `problemFromWire` passes and its own module is exempt, and the e2e entry points keep their default export. Building `resource/not-found` outside its one builder is rejected everywhere; in `apps/backend/src`, including the schema declaration and the connection constructor, only the body of `notFoundProblem` in `src/http/problems.ts` may build it; a second builder in that module and a function of the same name anywhere else are rejected, while every other restriction on those files still applies | `tests/lint-problems.test.ts` | L10: problem bodies are parsed in one place, and every `404` is built by one function | — code quality |
| `fetch` is rejected in screens, session code, the persistence seam, `packages/ui`, the backend, app tests and e2e specs — bare, as `globalThis.fetch` and as `window.fetch` — and allowed in `apps/*/src/api/**`, `e2e/fixtures/**` and `e2e/stack/**`; `typeof fetch` in a test double's type is not a use. L16 and L17 share `no-restricted-globals` without switching each other off: a plain source file confines both, the API clients keep `fetch` and still confine `localStorage`, and the persistence seam keeps `localStorage` and still confines `fetch` | `tests/lint-globals.test.ts` | L16: one transport per app, which is where trace headers and client spans will attach ([[6-observability]]). Flat config replaces a rule's options rather than merging them, so the two seams need one block per file set, and a global reached through an object needs `no-restricted-properties` beside `no-restricted-globals` | — code quality |
| No tracked `*.ts`, `*.tsx`, `*.js`, `*.mjs`, `*.json`, `*.md`, `*.sql` or `*.css` file contains a NUL byte; the selection includes known text files and no binary assets such as images | `tests/text-files.test.ts` | Source stays diffable text: a literal NUL made git and GitHub treat `engine/draft-validation.ts` as binary, hiding its diffs from review | — repo hygiene |
| TanStack Query, TanStack Router and `@dnd-kit/*` are rejected outside `apps/admin`, TanStack Form outside `apps/respondent` and `radix-ui` outside `packages/ui/src/primitives`, subpaths included, in each workspace's sources and tests; each library passes in its own home, unsplit libraries pass everywhere, and every home still rejects `pino` | `tests/lint-library-split.test.ts` | [[2-design-doc#17. Decisions Log]] #32: one rule splits the frontend libraries, so the respondent stays dependency-light and both apps share one component set | — code quality |
| A bare `localStorage`, `window.localStorage` and `globalThis.localStorage` are rejected in every `src` directory and pass in `apps/respondent/src/storage`; `_tests/**` and `e2e/**` are outside the rule, and an unrelated object with a `getItem` method passes | `tests/lint-storage.test.ts` | One persistence seam: the partial-answer envelope, its quota and failure handling, and later its telemetry have one place to live | — code quality |
| `process.env`, read as a property and by index, is rejected in every `src` directory and passes in `apps/backend/src/config.ts`; tool config files, `_tests/**` and `e2e/stack/**` are outside the rule, and the `fetch` and `localStorage` seams stay enforced in the files that own one of the other two | `tests/lint-environment.test.ts` | One environment reader per process, which is where telemetry configuration will land | — code quality |
| `console.log`, `console.error`, `console.warn` and `console.debug` are rejected in every `src` directory and across `e2e/`, and pass in the five entry points that write to a terminal — `db/migrate.ts`, `db/seed/seed.ts` and `e2e/stack/{stack-cli,global-setup,global-teardown}.ts`; `_tests/**` is outside the rule and a `log` method on another object passes | `tests/lint-console.test.ts` | Logging goes through the logger and later through telemetry, so a process writing to stdout is a deliberate entry point rather than a stray print | — code quality |
| A default export is warned on in sources, package tests and e2e fixtures, for a declaration and for a value, and passes in `*.config.*` and in the vitest and Playwright `globalSetup` and `globalTeardown` entry points the frameworks load by default export; named exports pass, and an exempted file loses only that selector: the backend's `drizzle.config.ts` and `vitest.config.ts` keep the raw-SQL and connection-construction warnings, the backend harness's `globalSetup` keeps raw SQL while keeping its connection-construction exemption, non-backend config files carry no backend selector, and all of them keep the double-assertion warning | `tests/lint-default-exports.test.ts` | Imports read the same everywhere, so a symbol is renamed in one place and searched for by one name | — code quality |
| An unnamed `export *` is warned on in every package entry point, in the shared domain, in an app and in the e2e fixtures' barrel; `export * as namespace` and named re-exports pass | `tests/lint-shared-surface.test.ts` | L8: a package's API is the names its entry point lists, so it cannot grow by accident | — code quality |
| Declaring any of the twelve shared vocabulary names — as a type alias, a `const` or a function — is warned on in admin, respondent, `packages/ui`, the backend and its tests, e2e, the shared package's own tests and its engine and API modules, and in `routePath`'s module and a tool config file, which keep their own exemptions; the one declaration in `packages/shared/src/domain` or `primitives.ts` passes and keeps every other warning; an import, a renamed import, a look-alike name and an object property pass; beside raw SQL in the backend both warnings appear | `tests/lint-shared-surface.test.ts` | L9: the domain vocabulary is declared once. The selectors travel with `syntax()` like the double-assertion ones, so a block appended later cannot drop them | — code quality |
| An explicit `any` — as a parameter type, an assertion, a type argument or an array element type — is an error in every workspace's sources and tests, e2e and `tests/`; `unknown` passes | `tests/lint-explicit-any.test.ts` | L15: types stay honest; test data uses typed builders | — code quality |
| `axe-core` and `vitest-axe`, subpaths included, are rejected in every workspace's sources and tests, `packages/telemetry` and `e2e` included, and pass only in `packages/ui/src/testing`, which still rejects `pino` and `radix-ui` | `tests/lint-test-support.test.ts` | L11: one axe runner and one rule config for every workspace, in `@qp/ui/testing` | — accessibility commitment |
| A `_tests` file may import the harness beside it, an enclosing directory's harness and the harness one directory below it (the test named after that directory), and nothing else: a sibling's, a cousin's or a deeper harness is rejected, type imports included, while support modules and a `harness-*` file pass; sources and the root `tests/` are outside the rule. The `_tests` blocks restate what they inherit — admin's and the respondent's library split, the backend's `openDatabase` restriction with the harness's exemption, and `pino` in every package but `packages/telemetry` | `tests/lint-test-support.test.ts` | L11: shared helpers live in support modules, so a harness stays private to the tests it serves. Flat config replaces a rule's options rather than merging them | — code quality |
| `@qp/ui/testing` and its subpaths are rejected in every `src` directory, and `packages/ui/src` also may not reach its `testing` directory by relative path; the testing module's own files, every `_tests` directory and look-alike paths pass | `tests/lint-test-support.test.ts` | L11: production code never pulls in the fake `fetch`, `vitest` or `axe-core` | — code quality |
| A line, a block, a JSDoc and a trailing comment are each rejected once in `packages/shared` and `packages/telemetry`, sources and `_tests` included; `eslint-disable`, `eslint-disable-next-line`, `eslint-disable-line` and `eslint-enable` pass, as does `@ts-expect-error — <reason>`, while a bare `@ts-expect-error`, one with a hyphen instead of an em-dash, and one with no reason after the em-dash are still rejected; the rule does not reach `apps/backend`, `apps/admin` or `eslint.config.mjs` | `tests/lint-comments.test.ts` | L5: comment content lives in docs/, not beside the code ([[11-structural-refactor#3. The PRs]], PR 18) | — code quality |
| A relative path reaching into `packages/ui/src` (including through a `./` segment) and a bare import deeper than the `@qp/ui/questionnaire` or `@qp/ui/testing` entry are both rejected from `apps/admin` and `apps/respondent`, sources and tests alike; the declared entry points — `@qp/ui/questionnaire`, `@qp/ui/primitives/*`, `@qp/ui/lib/*` and `@qp/ui/globals.css` — pass, as do `packages/ui`'s own relative imports; each home still rejects `pino` and its own split-off libraries | `tests/lint-ui-entry-points.test.ts` | L12: apps reach `@qp/ui` only through its declared entry points, so nothing outside `packages/ui/src/questionnaire/**` depends on where a file sits inside it | — code quality |

### Wave 1b — Track 1: engine and validators

**Domain unit — `packages/shared`** (milestone **M1**)

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| Every operator of every response type — 22 operators, each with an answer that makes it hold and one that does not, including `between` at both inclusive ends; a guard asserts the table covers the full operator set | `engine/conditions.test.ts` | One evaluator, typed per response type ([[5-questionnaire-format#4.3 Evaluation]], #9) | Conditional navigation over one and several earlier answers |
| Number conditions compare the decimal string exactly against the authored constant: `18.00` equals `18`, `0.1` is not `lt 0.1`, `1e21` and `1e-7` operands compare beyond double precision | `engine/conditions.test.ts` | #42: exactness survives into evaluation, not only storage | Conditional navigation over one and several earlier answers |
| `conditionHolds`: unanswered → `false` for every operator except text `answered` with `value: false`; not shown with an answer kept → `false` for every operator; an answer shaped for another type → `false`; the negative operators `isNot`, `isNoneOf`, `excludes`, `neq` do not fire for a respondent who never answered | `engine/conditions.test.ts` | A condition means the answer exists and satisfies the operator (§4.3) | Absent-answer semantics |
| Text `answered` truth table over shown × answered × `value`: `value: true` holds only when shown and answered, `value: false` only when shown and unanswered, and both are `false` when the referenced item is hidden, with or without a stale answer | `engine/conditions.test.ts` | One text operator with a boolean operand; a hidden reference is `false` for both values (§4.3, option b) | Absent-answer semantics |
| Text conditions are `{ op: "answered", value: boolean }`: both values accepted; `op: "notAnswered"` (with or without `value`), a missing `value` and a string `value` rejected by the schema; `@ts-expect-error` shows `notAnswered` and a bare `answered` no longer compile | `domain/schemas.test.ts` | The collapsed text operator is the only representable form (#9) | Conditional navigation over one and several earlier answers |
| Holds implies satisfiable: for every operator-table condition, on unbounded and bounded questions of its type, if some valid candidate answer makes it hold at runtime then `isTermSatisfiable` for that single condition is `true` (and on unbounded questions every condition can hold). Verified to fail when the publish-time `isNot` encoding is broken | `engine/conditions.test.ts` | The runtime (`holds`) and publish-time (`satisfiable`) encodings of operator meaning cannot drift silently | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Traversal: every operator-table case evaluated through a gated definition gives the same result as `conditionHolds`; with the gate closed and the stale answer kept, every operator is `false`; text `answered` with `value: false` follows the gate | `engine/visibility.test.ts` | A condition on a question that was not shown is `false` (§4.3); answers to hidden items are kept client-side ([[10-frontend#4.3 Local persistence]]) | Absent-answer semantics |
| An explicit `null`, an itemId named like an `Object.prototype` key, and a reference to a later item are all treated as not answered or not shown | `engine/visibility.test.ts` | Absent-answer semantics hold for malformed and adversarial input | Absent-answer semantics |
| `all` / `any` truth table over two conditions including unanswered, single-condition groups, empty `all` → `true`, empty `any` → `false` | `engine/visibility.test.ts` | Single grouping level (§4.1); the empty-group edges are defined | Conditional navigation over one and several earlier answers |
| Demo truth table for v1 and v2: yes → `itm_01..04`; no → `itm_01`, `itm_04`; unanswered → the branch stays closed; no with stale branch answers → still skipped | `engine/visibility.test.ts` | The mandatory branching demo, and v2's identical reachable path (#26) | Conditional navigation over one and several earlier answers |
| A rule over two earlier answers: has a condition AND it is diabetes | `engine/visibility.test.ts` | Rules over *one or more* previous responses | Conditional navigation over one and several earlier answers |
| `visibleAnswers` drops hidden answers transitively, and re-evaluating the filtered payload yields the same visible set | `engine/visibility.test.ts` | Client filtering at submit and the server's re-evaluation cannot disagree | Server as authority: an answer to an unreachable item is rejected |
| Per-type answer constraints: text length in code points; unknown option; `otherText` without a freeform `other`; too few / too many counted on distinct ids; integer accepts a zero fraction (`72.0`) and rejects a non-zero one; inclusive number and date bounds compared exactly; `not_future` / `not_past`; type mismatch | `engine/answer-validation.test.ts` | Each value satisfies its pinned question version's constraints ([[7-application-boundary#5.4 Submit: authority, validation, idempotency]]) | Response validation: required, per-type value rules |
| A selected freeform `other` with missing, empty or whitespace-only `otherText` → `choice/other-text-required`, for single and multiple choice; padded text passes; whitespace-only text without `other` stays `choice/other-text-without-other`; through `validateSubmission`, missing, `""` and whitespace-only all pass the answer schema and produce the same 422 item error | `engine/answer-validation.test.ts` | An `other` answer carries its text; every blank form is one item error the UI can render, never a schema 400 | Response validation: required, per-type value rules |
| The answer schema accepts `""` and whitespace-only `otherText`; `ResponseRow` rejects an empty `otherText` | `domain/schemas.test.ts` | Blank other text is the validator's 422, and no row ever carries it | Response validation: required, per-type value rules |
| The closed `SUBMISSION_ITEM_CODES` (now including `choice/other-text-required`) and `DRAFT_ITEM_CODES` lists; the new code validates on the `submission/invalid` wire body | `problems.test.ts` | Closed item codes the client can switch on exhaustively | — contract |
| Duplicate `optionIds` rejected as `choice/duplicate-option`, naming the item | `engine/answer-validation.test.ts` | #34: the one `response` invariant not enforced below the application | Duplicate `option_ids` inside one answer |
| Submit: yes and no paths accepted with rows in item order carrying `questionId` / `questionVersion`; missing visible required → `answer/required`; answer to a skipped item → `answer/not-visible` with no value in the result; unknown item key; one failure yields no rows | `engine/answer-validation.test.ts` | Server authority over the reachable path, all-or-nothing, errors never echo answers (§5.5) | Server as authority: an answer to an unreachable item is rejected |
| The server fills `unit` from the pinned question and omits it when the question has none; the row carries the canonical decimal (`72.50` / `72.500` → `72.5`, `72.0` → `72`, `-0` / `-0.0` → `0`, `-12.340` → `-12.34`); single choice is a one-element `optionIds`; every row validates as `ResponseRow` | `engine/answer-validation.test.ts` | #42, #12; the row equals what the column will hold | Response validation: required, per-type value rules |
| Predicate-change fixture at the engine: identical answers accepted under v1 and rejected under a v2 that tightens `itm_03`, naming `itm_03` | `engine/answer-validation.test.ts` | Submit evaluates the *pinned* definition ([[8-testing#6. Test data and fixtures]]) | Path re-evaluation uses the *pinned* definition, not the latest published |
| Calendar date of an instant in UTC, UTC+13, UTC−10 and across a year boundary; `addDays` across month, year and leap day | `engine/calendar.test.ts` | Both clocks resolve a calendar date without reading one | Relative date constraints across a timezone boundary |
| Years 0000–0099, which the date format admits, are not shifted by `Date.UTC`'s legacy 1900 offset: `0099-12-31` + 1 → `0100-01-01`, century and leap edges, a 0000–9999 day-number round trip agreeing with `Date.UTC` where it is correct, a relative-date case in year 99; a date satisfiability case spanning `0099-12-31` / `0100-01-02` | `engine/calendar.test.ts`, `engine/draft-validation.test.ts` | Day arithmetic is correct for every date the schema accepts | Relative date constraints across a timezone boundary |
| East of UTC: at UTC+13 on the 14th, today passes `not_future` in the browser (local, no tolerance) and on the server (UTC + 1 day), strict UTC would reject it, tomorrow is rejected by both. West: the mirror case for `not_past` at UTC−10 | `engine/calendar.test.ts` | #38 and [[5-questionnaire-format#2.4 Relative date constraints resolve against two different clocks]] | Relative date constraints across a timezone boundary |
| The tolerance admits exactly one day either side of UTC today; results are unchanged under three faked system times | `engine/calendar.test.ts` | The evaluator takes `today` as a parameter and never reads a clock | Relative date constraints across a timezone boundary |
| Exact decimal comparison (trailing zeros, `-0`, beyond 2^53), authored constants read as their shortest decimal including exponent forms, integer detection on the canonical form, canonicalization (trailing fractional zeros and point stripped, no negative zero) | `engine/decimal.test.ts` | #42 exactness in comparison and the stored row | Response validation: required, per-type value rules |
| Canonical form is exactly §5.4's: sorted by `itemId`, `optionIds` sorted, the row's (already canonical) decimal string, absent fields omitted, fixed key order | `engine/digest.test.ts` | #37 canonicalization, specified once in `@qp/shared` | Submit idempotency: same digest replays, different digest conflicts |
| Digest is SHA-256 of the canonical form (checked against `node:crypto`), 32 bytes, and identical under key reordering, row reordering and option click order | `engine/digest.test.ts` | A re-serialized retry is not a spurious `409` | Submit idempotency: same digest replays, different digest conflicts |
| A JSON round-trip of the stored columns digests identically; `72.5`, `72.50` and `72.500` digest identically through `validateSubmission` and match the digest recomputed from the stored `72.5` row, `72.51` does not; `-0` and `-0.0` match the stored `0`; null and omitted optional answers and a reordered request body digest identically | `engine/digest.test.ts` | The digest stays a pure function of the persisted rows (#37) | Submit idempotency: same digest replays, different digest conflicts |
| Changing an option, `otherText`, date, text, the number, the unit, or adding / removing an answer changes the digest | `engine/digest.test.ts` | Canonicalization loses no information, so no false `200` | Submit idempotency: same digest replays, different digest conflicts |
| Demo v1 and v2 pass publish validation | `engine/draft-validation.test.ts` | The shipped demo is publishable | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Duplicate `itemId`, a question placed twice and an unresolvable question version → their `draft/*` codes. `draft/question-archived` is not among them: archiving gates new placements only (#75), which only `replaceDraft` can tell, so the validator takes no archived-question input | `engine/draft-validation.test.ts` | §5.5, #41; [[7-application-boundary#6.1 Error format — RFC 9457 problem details]] | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Forward reference, self reference, unknown item, type mismatch, unknown option (scalar and list operands) → their `predicate/*` codes; a referential failure suppresses satisfiability verdicts on that item and its dependants | `engine/draft-validation.test.ts` | Forward-only references make cycles unconstructible (§5.1, §5.2); referential integrity (§5.4) | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Exact satisfiability per type: text `answered` with both `value: true` and `value: false` on one item is unsatisfiable, while repeating one value or splitting the two across `any` is satisfiable; single-choice set intersection; multiple-choice required/forbidden overlap, `maxSelections`, `minSelections`, and `includesAnyOf` as a minimum hitting set; number intervals with punctures, integer questions and the question's own bounds; dates as discrete days; relative constraints excluded; `any` groups and empty groups | `engine/draft-validation.test.ts` | Satisfiability checked exactly by domain intersection (§5.3) | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Reachability through the dependency closure: a predicate satisfiable alone but contradicting its references' gates → `draft/unreachable`, three deep, through an `any` with a second way in and one with none, dependants of an unsatisfiable item, and text `answered` with `value: false` inheriting its item's gate | `engine/draft-validation.test.ts` | Reachability is stronger than per-predicate satisfiability (§5.3) | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Problems are listed in item order, then closed-code order, each once | `engine/draft-validation.test.ts` | One deterministic result for publish and `/draft/validate` | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Question save rules: each `question/*` code with its JSON Pointer; the yes/no template and a freeform `other` pass | `engine/question-rules.test.ts` | Cross-field rules a schema cannot express, as closed codes | Question versioning: append-only, items pin a version at add time |
| An option with the id `other` that is not freeform, with `freeform` absent or `false`, is `question/other-not-freeform` at its `/options/N/optionId`; a plain `other` beside the freeform one is that and `question/duplicate-option-id` | `engine/question-rules.test.ts` | The id `other` is reserved for the freeform option in both directions (#83) | Question versioning: append-only, items pin a version at add time |

### Wave 1b — Track 2: database

**Backend integration — `apps/backend`, Testcontainers Postgres, roles from `db/init/01-roles.sh`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `UPDATE` of a published snapshot is `42501` for `qp_definition`, which has no `UPDATE` on that column, and `QP001` even for `qp_owner` — a direct client bypassing the API | `_tests/db/immutability.test.ts` | Layer 1 and layer 3 of [[5-questionnaire-format#6.4 Three layers of immutability enforcement]] | Questionnaire versioning: publish, immutability, one draft |
| `DELETE` of a published version, and demotion back to draft, are rejected `QP001`; a draft stays editable | `_tests/db/immutability.test.ts` | The `DELETE` trigger [[9-database-schema#4. Immutability in the data layer]] adds to §6.4 | Questionnaire versioning: publish, immutability, one draft |
| `qp_definition` inserting a complete, forged `published` version is rejected `QP001`; a draft carrying a version number is `23514` | `_tests/db/immutability.test.ts` | A published row exists only by promoting a draft in place (#8); the `INSERT` trigger closes the shortcut §11.6 rules out | Questionnaire versioning: publish, immutability, one draft |
| `UPDATE` and `DELETE` on `question_version` and `question_version_option` are rejected `QP001` even where no questionnaire uses the question | `_tests/db/immutability.test.ts` | Question versions are append-only (#13) | Question versioning: append-only, items pin a version at add time |
| Items of a published version reject `INSERT`, `UPDATE`, `DELETE` (from `qp_owner` and from `qp_definition`) and reparenting into a draft; `qp_definition` can update and delete draft items | `_tests/db/immutability.test.ts` | The item guard and its reparent check ([[9-database-schema#4.1 The item guard, and the two ways it fails naively]]) | Questionnaire versioning: publish, immutability, one draft |
| An item insert racing an uncommitted publish blocks, then fails `QP001` when the publish commits, leaving one item row | `_tests/db/immutability.test.ts` | `FOR SHARE` closes the snapshot/item-row drift race | Questionnaire versioning: publish, immutability, one draft |
| A session cannot pin a draft or another questionnaire's version; even as `qp_owner`, a questionnaire cannot point at its own draft, another questionnaire's version, or half a pair | `_tests/db/published-only-guard.test.ts` | A draft is structurally unreferenceable ([[9-database-schema#3.1 The published-only guard]]) | Sessions: start, resume, version pinning |
| A response whose `questionnaire_version_id` is another questionnaire's version or a draft is `23503`; one matching its session's pinned version inserts | `_tests/db/published-only-guard.test.ts` | `response (session_id, questionnaire_version_id)` references `session (id, questionnaire_version_id)`, so a response's provenance cannot disagree with its session (#14, Copilot review 4000925345) | Response meaning preserved across a republish |
| A second draft for one questionnaire is `23505`; a submitted session without a digest is `23514` | `_tests/db/published-only-guard.test.ts` | `questionnaire_one_draft`; `session_state` | Questionnaire versioning: publish, immutability, one draft |
| Every valid answer shape inserts; NULL option arrays, NULL elements, empty text, wrong cardinality, cross-type values, orphan `other_text`, `yes_no` and unknown types are `23514` | `_tests/db/response-shape.test.ts` | `response_shape` with `COALESCE(..., false)` ([[9-database-schema#6.2 `COALESCE(..., false)` is load-bearing]]) | Response validation: required, per-type value rules |
| `question_version_option` accepts the freeform `other` and a plain ordinary option, and rejects a plain `other` and a freeform option under another id with `23514`; a second freeform `other` in one version is `23505` from the primary key | `_tests/db/other-option.test.ts` | `freeform_exactly_when_other`, and the primary key standing in for the dropped `qvo_one_freeform` ([[9-database-schema#3.2 The question bank]], #83) | Response validation: required, per-type value rules |
| `createQuestion` given a plain `other` option, which skipped the question rules, fails `23514` and leaves no question | `_tests/db/other-option.test.ts` | The database holds the rule even for a caller that bypasses the route (#83) | Response validation: required, per-type value rules |
| Migration `0015`, run against a table still under `freeform_is_other` that holds a plain `other` option, fails `23514` at its first statement, naming the question version and option, instead of rewriting the row | `_tests/db/other-option.test.ts` | Question versions are append-only (#13), so the migration refuses bad data rather than changing a stored version | — migrations |
| Duplicate ids inside a `multiple_choice` answer insert cleanly | `_tests/db/response-shape.test.ts` | Pins the stated gap: #34 leaves duplicates to the submit validator | Duplicate `option_ids` inside one answer |
| `qp_execution` cannot read `question`, `question_version`, `question_version_option`, `questionnaire_item` or the base `questionnaire_version`, cannot write or delete from any definition table, and reads questionnaires, `version_question_index` and published versions through `published_questionnaire_version` | `_tests/db/grants.test.ts` | [[7-application-boundary#3.2 Database grants]]; execution cannot read a draft (§2.1, Copilot review 4000925328) | The definition/execution barrier |
| `published_questionnaire_version` returns a published version and never a draft; a session still pins through the composite foreign key without base-table access; `qp_definition` has no privilege on the view | `_tests/db/grants.test.ts` | A `security_barrier` view is the only execution read path to versions | The definition/execution barrier |
| `qp_execution` cannot `UPDATE` or `DELETE` a response or `DELETE` a session; the only `DELETE` either application role holds is `qp_definition` on `questionnaire_item`, and neither holds `TRUNCATE` | `_tests/db/grants.test.ts` | Collected responses are immutable by grant (#24); draft item removal is the one scoped exception, bounded to drafts by the item guard | The definition/execution barrier |
| `qp_definition` cannot read `execution.response`, `execution.session` or a response partition | `_tests/db/grants.test.ts` | Authoring is not a back door into answers (#17) | The definition/execution barrier |
| `qp_definition` gets `permission denied` for `SELECT`, `INSERT`, `UPDATE` and `DELETE` on `audit.event`; `qp_execution` cannot even call `audit.record` | `_tests/db/grants.test.ts` | Audit is reachable only through `audit.record` (#24) | — audit |
| `audit.record` commits with the domain change, is discarded with it on `ROLLBACK`, rejects an unlisted action, and is `SECURITY DEFINER` owned by `audit_owner` with `search_path = audit, pg_temp` | `_tests/db/grants.test.ts` | [[9-database-schema#9.1 A dedicated role, inside the publish transaction]] | — audit |
| `qp_owner` has no `USAGE` on `audit` and cannot call `audit.record` | `_tests/db/grants.test.ts` | The migration identity stays out of the audit schema ([[9-database-schema#9.1 A dedicated role, inside the publish transaction]]) | — audit |
| `audit.record` is executable by `qp_definition` and not by `qp_owner` or `qp_execution`; no function in `definition`, `execution` or `audit` grants `EXECUTE` to `PUBLIC` (catalog check over `proacl`, including the default ACL) | `_tests/db/grants.test.ts` | Definer functions are reachable only by the roles named in an explicit grant | — audit |
| A definition table created after the grant migration is reachable by `qp_definition` | `_tests/db/grants.test.ts` | `ALTER DEFAULT PRIVILEGES FOR ROLE qp_owner` covers later migrations | The definition/execution barrier |
| `response` has no `DEFAULT` partition and exactly the 36 monthly partitions from 2026-09; an insert outside them fails `23514`; one `created_at` lands in one partition | `_tests/db/partitions.test.ts` | [[9-database-schema#6.4 Partitioning]] | — archival |
| `DETACH PARTITION ... CONCURRENTLY` succeeds today, and fails `55000` once a `DEFAULT` partition is attached | `_tests/db/partitions.test.ts` | Why there is no default partition (#23) | — archival |
| The partition helper names months in UTC, creates only missing partitions, and is idempotent | `_tests/db/partitions.test.ts` | Rollover is a repeatable task, never a default partition | — archival |
| The harness puts a role and password into a connection URL encoded exactly once: `@`, `/`, `:`, `%`, spaces, `#` and non-ASCII decode back to the original | `_tests/db/server.test.ts` | `TEST_DATABASE_URL` and the per-worker URLs authenticate with any password | — harness |
| The roles script creates non-superuser `qp_owner`, `qp_definition`, `qp_execution`, `NOLOGIN` `audit_owner`; `qp_owner` joins `audit_owner` with `INHERIT FALSE`, owns the database and every non-audit table | `_tests/db/roles.test.ts` | Five identities, `qp_owner` is not `POSTGRES_USER` (#39) | The definition/execution barrier |
| After the full migration chain, `execution.response` is `relkind = 'p'` with range strategy on `created_at`, and `item_position_unique` is deferrable and initially immediate | `_tests/db/hand-edits.test.ts` | The two hand edits to the generated `0000_schema.sql` that drizzle-kit's snapshot cannot see | — migrations |
| Swapping two draft items' positions with separate `UPDATE`s succeeds under `SET CONSTRAINTS ... DEFERRED` and fails `23505` without it | `_tests/db/hand-edits.test.ts` | `item_position_unique` has to be `DEFERRABLE` ([[9-database-schema#3.3 Items and the reverse index]]) | Questionnaire versioning: publish, immutability, one draft |
| Every committed `.sql` migration matches its sha256 in `migrations.lock.json`, none is missing, and an unlocked new file fails with the line to add | `_tests/db/migration-lock.test.ts` | Applied migrations are never edited; drizzle never re-runs one | — migrations |
| `drizzle-kit generate` against a temporary copy of `drizzle/` reports no schema changes and writes nothing | `_tests/db/schema-drift.test.ts` | `schema.ts` and the committed migrations describe the same schema (§11.1) | — migrations |
| `replaceDraft` removes, reorders and adds items, renames the draft, bumps `draft_revision` and audits `edit_draft`; it can empty a draft | `_tests/db/definition/drafts.test.ts` | `PUT /draft` is whole-document replacement ([[7-application-boundary#4.1 Endpoints]]) | Questionnaire versioning: publish, immutability, one draft |
| `replaceDraft` refuses a stale revision, a questionnaire with no draft, a newly placed archived question and a nonexistent question version, and leaves the items untouched | `_tests/db/definition/drafts.test.ts` | #43 revision check; #75 archived questions refused as new placements only | Question versioning: append-only, items pin a version at add time |
| `qp_definition` promoting a draft with one `UPDATE` of the publish columns, or moving the current-version pointer, is `42501`; its `UPDATE` columns on `questionnaire_version` and `questionnaire` are exactly `title`, `draft_revision`, `updated_at`, `key`, `name`, `closes_at` | `_tests/db/promote-draft.test.ts` | Publishing cannot bypass validation, the reverse index, the pointer or the audit row (Copilot review 4000925311) | Questionnaire versioning: publish, immutability, one draft |
| `definition.promote_draft` promotes, indexes and moves the pointer in one call, and writes no audit row — calling it directly is the accepted, documented way to promote without auditing (Decisions Log #51); rejects a published version `QP001`; rejects a snapshot naming the wrong version, another questionnaire, or items that differ from the draft rows with `22023`, writing nothing | `_tests/db/promote-draft.test.ts` | The only path to a published row keeps the snapshot, item rows, index and audit trail consistent (#24 pattern) | Questionnaire versioning: publish, immutability, one draft |
| `promote_draft` is `SECURITY DEFINER` owned by `qp_owner` with `search_path = definition, pg_temp`; `qp_definition` can execute it and `qp_execution` cannot | `_tests/db/promote-draft.test.ts` | Same hardening as `audit.record` ([[9-database-schema#9.1 A dedicated role, inside the publish transaction]]) | The definition/execution barrier |
| `publishDraft` promotes in place, writes the snapshot, `version_question_index`, the current-version pointer and a `publish` audit row in one transaction | `_tests/db/definition/publish.test.ts` | Publish as the seam ([[7-application-boundary#4.3 Publish and retire]]) | Questionnaire versioning: publish, immutability, one draft |
| `publishDraft` writes exactly one `publish` audit row, as `qp_definition` after `promote_draft`; when that audit write fails, the promotion, pointer and index roll back with it | `_tests/db/definition/publish.test.ts` | The audit record joins the publish transaction ([[6-observability#5.1 Isolation — separate schema with a restricted role]], #52) | — audit |
| A stale draft revision returns an outcome and writes nothing, audit included | `_tests/db/definition/publish.test.ts` | #43 revision check | Questionnaire versioning: publish, immutability, one draft |
| Publishing a draft with a forward reference or an unsatisfiable predicate returns `validateDraft`'s items (`predicate/forward-reference`, `predicate/unsatisfiable`) and leaves the version a draft, the pointer empty, and no index or audit row | `_tests/db/definition/publish.test.ts` | Publish runs the one publish-time validator inside its transaction ([[5-questionnaire-format#5. Publish-time validation]]); a failed publish leaves no audit record | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Two concurrent publishes of one draft: exactly one publishes, the other sees no draft | `_tests/db/definition/publish.test.ts` | The questionnaire row lock plus the compare-and-swap promote ([[9-database-schema#5. Concurrency control]]) | Questionnaire versioning: publish, immutability, one draft |
| The seed builds from `intakeDefinition(1)`, passes publish validation and stores exactly that snapshot under the hardcoded ids, with `qst_which_condition` at question version 3, through the audited authoring path; a second run is a no-op | `_tests/db/seed/demo-questionnaire.test.ts` | #35 hardcoded ids; one demo definition shared by seed and fixtures (§6); [[9-database-schema#11.6 The seed is an integration test wearing a disguise]] | Response meaning preserved across a republish |

### Wave 1b — Track 3: UI package and renderer

**Frontend component — `packages/ui`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `QuestionnaireForm` over `intakeDefinition(1)` with a parent holding the answers: it starts on `itm_01` and `itm_04`; answering *yes* reveals `itm_02` and `itm_03` between them and the live region announces both prompts; answering *no* removes them and announces the removal | `questionnaire/questionnaire-form.test.tsx` | The mandatory branching demo, rendered: pruning happens in place and is announced (#28, [[10-frontend#7. Accessibility]]) | Conditional navigation over one and several earlier answers |
| For no answers, *yes*, *no*, and *no* with a stale branch answer, the rendered items equal the shared engine's `visibleItems`; a hidden answer still in the `answers` prop reappears when its branch reopens | `questionnaire/questionnaire-form.test.tsx` | Visibility is the shared evaluator imported, not a second opinion ([[10-frontend#3. `packages/ui` — primitives and the renderer]]); hidden answers are kept (#29) | Conditional navigation over one and several earlier answers |
| Without the parent updating `answers`, choosing *yes* reports `onChange` and reveals nothing | `questionnaire/questionnaire-form.test.tsx` | `packages/ui` holds no form state (#27) | Conditional navigation over one and several earlier answers |
| Exactly the visible items render, in list order; items leaving the set lose their controls while the rest stay in place; an empty set renders only the live region | `questionnaire/questionnaire-items.test.tsx` | All visible items on one page, pruning visible in place (#28) | Conditional navigation over one and several earlier answers |
| A polite live region exists and is empty before any change; opening the demo branch announces the added prompts in order, closing it announces the removed ones, a mixed change announces both | `questionnaire/visibility-announcer.test.tsx` | Revealed and removed questions are announced ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| An answer change that leaves the visible set unchanged does not re-announce; the announcement never contains an answer value | `questionnaire/visibility-announcer.test.tsx` | Announcements track visibility only; answers stay out of copy that assistive tech reads aloud | No respondent answer in telemetry |
| `focusItem` scrolls an item's `data-item-id` wrapper into view and moves focus to its first keyboard-reachable control, skipping a disabled control and one inside an `aria-hidden` ancestor | `questionnaire/focus-item.test.tsx` | The `data-item-id` attribute is the one contract `focusItem` and the e2e specs both rely on to locate an item ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| An itemId the container does not have, or an item with no keyboard-reachable control, focuses nothing and reports it rather than throwing | `questionnaire/focus-item.test.tsx` | Callers such as the respondent's error summary fall back to their own focus target | — accessibility commitment |
| The date control is a native `<input type="date">` named exactly by the prompt, with the question's `min` / `max` | `questionnaire/controls/date-control.test.tsx` | Native date input ([[10-frontend#7. Accessibility]]) | Response validation: required, per-type value rules |
| A chosen date reports `{ type: "date", date }` and a cleared one `null` through `onChange` keyed by `itemId`; the control shows the `answers` prop and holds no value of its own; an answer of another type is not shown | `questionnaire/controls/date-control.test.tsx` | Renderer controlled by props, no form state (#27, [[10-frontend#3. `packages/ui` — primitives and the renderer]]); `itemId` keying (#41) | Response validation: required, per-type value rules |
| `readonly` mode sets `readonly` and reports no change | `questionnaire/controls/date-control.test.tsx` | Admin preview renders a snapshot without a form library | Response meaning preserved across a republish |
| Required items carry `aria-required`; an error code for the item sets `aria-invalid` and names the catalogue message as the accessible description; no error leaves both attributes off; another item's error is ignored | `questionnaire/controls/date-control.test.tsx` | ARIA driven off the `errors` prop | Response validation: required, per-type value rules |
| `single_choice` is a Radix radio group rendered as a `<fieldset>` named by its `<legend>` (with "(required)" read aloud when required), one radio per option named by its label; choosing reports `{ optionId }` keyed by `itemId`, re-choosing the chosen option reports nothing, and the checked radio follows the `answers` prop only | `questionnaire/controls/single-choice-control.test.tsx` | Radio group in a fieldset; controlled props (#27, #41) | Response validation: required, per-type value rules |
| The freeform option's text box is visible before other is chosen, sits beside the other radio; typing selects other with `otherText`, clearing keeps other with no `otherText`, whitespace passes through to the validator, choosing another option omits `otherText` from the answer; the box is seeded from the answer's `otherText`; no text box without a freeform option | `questionnaire/controls/single-choice-control.test.tsx` | Typing selects other; a blank other is `choice/other-text-required`, the validator's to reject | Response validation: required, per-type value rules |
| A freeform option whose id is not `other` gets no text box, in a radio group and in a checkbox group | `questionnaire/controls/single-choice-control.test.tsx`, `questionnaire/controls/multiple-choice-control.test.tsx` | The renderer uses the one definition of the other option ([[2-design-doc#17. Decisions Log]] #82) | Response validation: required, per-type value rules |
| With a parent holding the answers: typed other text stays visible after switching to another option while the answer omits `otherText`; choosing other again puts the retained text back as `otherText`; text cleared while other was selected is not resurrected on reselect | `questionnaire/controls/single-choice-control.test.tsx` | Moving off other loses no typed text, yet the answer never carries `otherText` without other (`choice/other-text-without-other`) | Response validation: required, per-type value rules |
| For both choice types, the other text box shows the option label as a hint that is hidden while the box has focus (`focus:placeholder:text-transparent`, so it reappears on blur while still empty), and its accessible name comes from `aria-label` even with the placeholder removed | `questionnaire/controls/other-text-input.test.tsx` | A hint is not a label: the name never depends on placeholder text | — accessibility commitment |
| `multiple_choice` is a checkbox group in a `<fieldset>` named by its `<legend>`; selections report `optionIds` in option order whatever the click order; unchecking the last option reports `null`; the text box checks other while keeping the rest, keeps `otherText` while other stays checked and omits it from the answer when other is unchecked | `questionnaire/controls/multiple-choice-control.test.tsx` | Checkbox group in a fieldset; `optionIds` has `minItems: 1` on the wire | Response validation: required, per-type value rules |
| With a parent holding the answers: unchecking other keeps its text in the box and omits `otherText`, checking it again restores `otherText`; unchecking every option reports `null` and still keeps the text | `questionnaire/controls/multiple-choice-control.test.tsx` | Moving off other loses no typed text, yet the answer never carries `otherText` without other (`choice/other-text-without-other`) | Response validation: required, per-type value rules |
| Choice groups in `readonly` mode disable their options, make the other text box read-only and report nothing; an error marks the group `aria-invalid` and describes it with the catalogue message | `questionnaire/controls/single-choice-control.test.tsx`, `questionnaire/controls/multiple-choice-control.test.tsx` | ARIA driven off the `errors` prop; admin preview needs no form library | Response validation: required, per-type value rules |
| `text` is an `<input type="text">`, or a `<textarea>` when `multiline`; typed text reports `{ text }`, cleared text `null`; controlled, read-only in `readonly` mode, ARIA from `required` and the error code | `questionnaire/controls/text-control.test.tsx` | Controlled props; `response_shape`'s non-empty text rule | Response validation: required, per-type value rules |
| `number` is a text input with `inputMode` `decimal` for float and `numeric` for integer questions, the unit shown after it and used as its description; typed text reports a decimal-string `value` with no unit, cleared text `null`; controlled, read-only in `readonly` mode, an error described before the unit | `questionnaire/controls/number-control.test.tsx` | #42: numbers travel as decimal strings and the server fills the unit | Response validation: required, per-type value rules |
| The catalogue has a message for exactly `SUBMISSION_ITEM_CODES` from `@qp/shared` minus `answer/not-visible` and `answer/unknown-item` (so `choice/other-text-required` included); messages read bounds from the question and fall back when a bound is absent | `questionnaire/messages.test.tsx` | One code-to-message catalogue for client and server errors | Response validation: required, per-type value rules |
| An item with several codes shows the first; codes that never attach to a rendered item are skipped and on their own render no error and no `aria-invalid`; messages are built from the question alone, never the answer | `questionnaire/messages.test.tsx` | Error bodies and messages never echo an answer ([[7-application-boundary#5.5 Error bodies must not echo answers]]) | No respondent answer in telemetry |
| axe-core finds no violations for the demo after *no* and after *yes*; for all five response types unanswered, answered with both other text boxes filled, each carrying an error, and in `readonly` mode; for a required checkbox group, a unitless integer and a single-line text question; and after a reveal has been announced | `questionnaire/accessibility.test.tsx` | The committed accessibility minimum at the component layer; `color-contrast` is disabled because jsdom does not lay out | — accessibility commitment |
| `urlOf` fills a route's path parameters and query through `routePath` and `routeSearch`; `FakeServer` records each request's method, URL, headers and parsed body, answers from the replies queued for its method and URL in order, hands anything unqueued to its fallback or rejects naming the request, and passes a network failure through; `contractResponse` throws on a body or status the route does not declare | `testing/fake-server.test.ts` | One fake-fetch server for admin and the respondent, whose URLs cannot drift from the shared route table | — conventions |

### Wave 2 — Track 4: definition plugin

One heading per group ([[4-implementation-plan#Wave 2 — API plugins *(two parallel tracks)*]]), so groups working in parallel add rows in different places. Each group adds rows only under its own heading.

#### G0 — the definition plugin skeleton

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| Path and query strings are coerced to their schema types; the body is not, so `"1"` for an integer is `schema/type`; an additional body property is `schema/additionalProperties` rather than stripped; missing and out-of-range values point at `/body/…` and `/params/…` | `_tests/http/validation.test.ts` | [[7-application-boundary#6.3 Input validation]]: the schema is the contract, and a `400` is always a client bug (#20) | — conventions |
| Unparseable JSON and a schema failure are `400 request/invalid`; an unhandled error is `500 internal` with the request id as `detail` and none of the error message; an unknown route is `404 resource/not-found`, built by `notFoundProblem`; every one is `application/problem+json` with the requested URL, query included, as `instance`, and `sendProblem` replaces any `instance` a body carried | `_tests/http/problems.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]; `500` bodies never carry a message or a stack (§6.2) | — conventions |
| `applyHttpDefaults` installs the exact request validator, so a body is not coerced, and the error handler it is given | `_tests/http/problems.test.ts` | The root app and both modules take their validator, error handler and not-found handler from one helper, so a module cannot drift from the conventions | — conventions |
| `registerRoute` registers a shared `defineRoute` object's method, URL and schema: path and query arrive typed and coerced, a schema failure is `400` before the handler runs, the returned status and headers are sent, a returned `Problem` is sent as `application/problem+json` with its status and the request URL as its `instance`, and a body field the response schema does not declare is dropped | `_tests/http/routes.test.ts` | One route contract in `@qp/shared` for server and client ([[7-application-boundary#6.3 Input validation]]); handlers cannot widen a response | — conventions |
| Type-level: a `RouteHandler` returning a status the route does not declare, a body that does not match the schema, an undeclared path param, or reading a body the route does not declare is a compile error; any `Problem` is a valid return | `_tests/http/routes.test.ts` | The handler's types come from the shared schema, so the contract and the handler cannot disagree ([[7-application-boundary#6.3 Input validation]]) | — conventions |
| `buildApp` serves `/health`, mounts the definition module at `/api/definition`, and answers an unknown path outside any module with a problem body | `_tests/app.test.ts` | [[7-application-boundary#8.1 Now — two plugins, one process]] | The definition/execution barrier |
| Through `buildApp`, an unknown path inside the definition module, inside the execution module (also `no-store`) and outside both is a `404` problem whose `instance` is the full prefixed URL; a malformed percent-encoding under either prefix or at the root is `400 request/invalid` with `errors: []` and the URL as `instance`, not Fastify's own JSON body | `_tests/app.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]: `application/problem+json` with an `instance` on every non-2xx, including errors raised before routing | — conventions |
| The author hook attaches `prototype-author`; `authorOf` on a request that did not pass through the hook fails as `500` instead of writing no author | `_tests/modules/definition/author.test.ts` | One hook on the whole plugin, so a route cannot forget authentication ([[7-application-boundary#7. Access model and data barriers]]); the placeholder is one constant (#57) | — access model |
| The draft ETag round-trips through `draftPreconditionOf`; `*`, a strong ETag, a missing revision and a negative revision are answered with `request/invalid` pointing at `/headers/if-match` rather than treated as unconditional | `_tests/modules/definition/if-match.test.ts` | #43: a revision from an earlier draft, which restarts at `0`, must not match the next draft | Questionnaire versioning: publish, immutability, one draft |
| `withCurrentDraft` runs its work on the locked open draft, is `no-draft` when none is open, and is `stale` when either the draft version id or the revision differs; the commands take the pair together as one `DraftPrecondition`, so it cannot be passed half-filled | `_tests/db/definition/questionnaire-version-rows.test.ts` | #43: a revision from an earlier draft, which restarts at `0`, must not match the next draft | Questionnaire versioning: publish, immutability, one draft |
| The immutability trigger raised through drizzle (deleting a published version's items as `qp_definition`) is `409 version/immutable`; a duplicate `question_version_pkey` is `409 question/version-conflict`; any other unique violation stays `500 internal`; each carries the request URL as `instance` | `_tests/modules/definition/errors.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]: `version/immutable` can only surface from the trigger, and the version-conflict mapping is a safety net scoped to the one constraint it names | Questionnaire versioning: publish, immutability, one draft |
| `definitionProblem` answers every refusal a definition command returns: each not-found outcome is `404 resource/not-found`, `stale` and `draft-exists` are `409`, invalid draft content is `422 questionnaire/draft-invalid` naming its items, and a changed response type is `400 request/invalid` pointing at `/body/question/type`; none carries an `instance`, which `sendProblem` adds | `_tests/modules/definition/problems.test.ts` | One mapping from outcome to problem for every definition route, so `PUT /draft` and `POST /publish` cannot drift apart again ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| `GET /questionnaires` is `[]` when empty; lists newest first by `id`, each with `currentVersion`, `closesAt` and `hasDraft`, matching `QuestionnaireSummary`; the order survives an `UPDATE` to an earlier row; an unknown definition path is a `404` problem | `_tests/modules/definition/routes/questionnaires.test.ts` | #40: every list has an explicit `ORDER BY` on a unique, monotonic key | List endpoints return a deterministic order |

#### G1 — question bank

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `POST /questions` is `201` with a `Question` at version 1, its options in authored order, `createdBy` `prototype-author`, and one `create_question_version` audit row whose actor is `prototype-author` | `_tests/modules/definition/routes/questions.test.ts` | Creating a question writes version 1 (#13); every authoring write records the placeholder author, never `null` (#57) | Question versioning: append-only, items pin a version at add time |
| `POST /questions` with a question-rule failure (`min` above `max`) is `400 request/invalid` at `/questions` with `{ pointer: "/body/question/min", code: "question/min-exceeds-max" }`, and writes no question and no audit row | `_tests/modules/definition/routes/questions.test.ts` | Question-rule codes surface in the `request/invalid` `errors` extension ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| `POST /questions` with a non-freeform `other` option is `400 request/invalid` at `/questions` with exactly `errors: [{ pointer: "/body/question/options/1/optionId", code: "question/other-not-freeform" }]`, and writes no question and no audit row | `_tests/modules/definition/routes/questions.test.ts` | The contract change #83 makes: a plain `other` option was accepted before | — conventions |
| `POST /questions/:questionId/versions` with a non-freeform `other` option is `400 request/invalid` at that path with exactly `errors: [{ pointer: "/body/question/options/1/optionId", code: "question/other-not-freeform" }]`, and saves no version and writes no audit row | `_tests/modules/definition/routes/questions.test.ts` | #83 applies to both question-save routes | — conventions |
| `GET /questions` is `[]` when the bank is empty | `_tests/modules/definition/routes/questions.test.ts` | [[7-application-boundary#4.1 Endpoints]] | — conventions |
| `GET /questions` lists the latest version of each question newest first by `id`, matching `Question`, with options in `position` order; the order survives an `UPDATE` to the earlier row and a new version of it | `_tests/modules/definition/routes/questions.test.ts` | #40: the bank has an explicit `ORDER BY id DESC`, so an edit does not reshuffle it | List endpoints return a deterministic order |
| An archived question is hidden from `GET /questions` by default and with `includeArchived=false`, and shown with `includeArchived=true` | `_tests/modules/definition/routes/questions.test.ts` | #15: questions are archived, never deleted — hidden from the bank, still readable | Question versioning: append-only, items pin a version at add time |
| `GET /questions/:questionId` returns the latest version with its metadata, including an archived question | `_tests/modules/definition/routes/questions.test.ts` | #15: archiving hides from the bank list, not from a direct read | — conventions |
| `POST /questions/:questionId/versions` is `201` with version 2 by `prototype-author` and audited; version 1 reads back byte-for-byte as before, and the question's latest is now version 2 | `_tests/modules/definition/routes/questions.test.ts` | #13: saving appends a version and never alters an earlier one | Question versioning: append-only, items pin a version at add time |
| `POST /questions/:questionId/versions` with a question-rule failure (duplicate option id) is `400 request/invalid` pointing at `/body/question/options/1/optionId`, and saves no version | `_tests/modules/definition/routes/questions.test.ts` | Question-rule codes surface in the `request/invalid` `errors` extension ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| Two concurrent saves of one question are both `201`, as versions 2 and 3 | `_tests/modules/definition/routes/questions.test.ts` | The `FOR UPDATE` row lock serializes version numbering, so neither save is lost and `question/version-conflict` does not fire ([[9-database-schema#5. Concurrency control]]) | Question versioning: append-only, items pin a version at add time |
| `GET /questions/:questionId/versions` lists metadata only, matching `QuestionVersionSummary`, newest first by `version` | `_tests/modules/definition/routes/questions.test.ts` | #40: version history is `ORDER BY version DESC` | List endpoints return a deterministic order |
| `POST /questions/:questionId/archive` is `200` with `archivedAt` set; archiving again is `200` with the same body and writes no second `archive_question` audit row; the one row's actor is `prototype-author` | `_tests/modules/definition/routes/questions.test.ts` | `archived_at` is set once, and the audit trail records the archive once (#15, #57) | Question versioning: append-only, items pin a version at add time |
| Archiving a question leaves the published versions that embed it listed in its usage | `_tests/modules/definition/routes/questions.test.ts` | Archiving means "not for new placements"; existing placements are unaffected ([[7-application-boundary#4.1 Endpoints]]) | Questionnaire versioning: publish, immutability, one draft |
| `GET /questions/:questionId/usage` lists only published versions embedding the question — not an open next draft, not a never-published draft, not another question's placements — ordered by `questionnaire_id`, then `version DESC`, with the pinned `questionVersion` | `_tests/modules/definition/routes/questions.test.ts` | The reverse lookup reads `version_question_index`, which only publish writes; #40 ordering | List endpoints return a deterministic order |
| `GET /questions/:questionId/usage` is `[]` for a question no published version embeds | `_tests/modules/definition/routes/questions.test.ts` | An existing question with no usage is not a `404` | — conventions |
| An unknown question id is `404 resource/not-found` with `instance` on every question route — get, versions, one version, usage, archive, save a version — and writes no audit row; an unknown version of a known question is `404`; a non-uuid id is `400` pointing at `/params/questionId` | `_tests/modules/definition/routes/questions.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]: unknown ids are `404`, schema failures `400` | — conventions |

#### G2 — draft lifecycle

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `POST /questionnaires` is `201` with a `QuestionnaireSummary` (`currentVersion` and `closesAt` null, `hasDraft` true); its draft reads back with no items, `ETag` at revision `0` and `Cache-Control: no-store` | `_tests/modules/definition/routes/questionnaires.test.ts` | Creating a questionnaire opens draft version 1 ([[7-application-boundary#4.1 Endpoints]]); the draft ETag is built from the revision counter (#43) | Questionnaire versioning: publish, immutability, one draft |
| Create, save and open-next-draft record `prototype-author` as the audit `actor_id` and as `questionnaire_version.created_by` | `_tests/modules/definition/routes/drafts.test.ts` | #57: every authoring write stores the placeholder through `authorOf`, never `null` | — access model |
| `GET /draft` returns items in `position` order and each pinned question version once, in first-placement order, with its own options in `position` order; `ETag` and `no-store` | `_tests/modules/definition/routes/drafts.test.ts` | #31: the draft carries the pinned `questionVersion`, never "current"; [[7-application-boundary#6.4 Caching]] | Question versioning: append-only, items pin a version at add time |
| `GET /draft` is `404` for an unknown questionnaire and for one with no open draft | `_tests/modules/definition/routes/drafts.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]] | Questionnaire versioning: publish, immutability, one draft |
| `PUT /draft` replaces title, items and order, and responds with the saved draft and the next `ETag`, which a re-read matches | `_tests/modules/definition/routes/drafts.test.ts` | Whole-document replacement is atomic ([[7-application-boundary#4.1 Endpoints]]) | Questionnaire versioning: publish, immutability, one draft |
| Two tabs read the same `ETag`; the first `PUT` is `200`, the second `409 questionnaire/draft-stale`, and the first tab's write survives | `_tests/modules/definition/routes/drafts.test.ts` | #43: a second tab cannot silently clobber the first | Questionnaire versioning: publish, immutability, one draft |
| A missing `If-Match` is `400` `schema/required` and a malformed one `400` `schema/pattern`, both pointing at `/headers/if-match` with the draft URL as `instance`, and neither writes | `_tests/modules/definition/routes/drafts.test.ts` | A draft write is never unconditional; `400` is always a client bug (#20) | Questionnaire versioning: publish, immutability, one draft |
| A newly added archived question, a question version that does not exist and a question that does not exist are `422 questionnaire/draft-invalid` at the draft URL, naming each offending item; the draft, its `ETag` and the audit log are unchanged | `_tests/modules/definition/routes/drafts.test.ts` | #31, #75: adding an archived question is rejected at add time; an unknown version is refused, not resolved to latest | Question versioning: append-only, items pin a version at add time |
| A `PUT /draft` body repeating an `itemId` is `422 questionnaire/draft-invalid` naming each duplicated id once as `draft/duplicate-item-id`, detected before the insert so no database error surfaces; the draft, its `ETag` and the audit log are unchanged | `_tests/modules/definition/routes/drafts.test.ts` | Draft content that fails validation is `422`, never a `500` from the item primary key ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | Questionnaire versioning: publish, immutability, one draft |
| A stale `If-Match` is `409 questionnaire/draft-stale` even when the items would also be refused as a newly placed archived question or as duplicated; the precondition is checked under the draft row lock before any content check, and nothing is written | `_tests/modules/definition/routes/drafts.test.ts` | #43: a request against a draft the author has not seen conflicts with current state before its content is judged (`409` before `422`, #20) | Questionnaire versioning: publish, immutability, one draft |
| `PUT /draft` on a questionnaire with no open draft is `404` | `_tests/modules/definition/routes/drafts.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]] | Questionnaire versioning: publish, immutability, one draft |
| `POST /draft` after v2 copies v2's title, items, order and predicates, pinning the same question versions although the bank has moved on; `201`, `ETag` at revision `0`, audited `create_draft` with `copiedFromVersion` | `_tests/modules/definition/routes/drafts.test.ts` | #8: the next draft is an explicit copy of the latest published version; #31: nothing auto-upgrades | Question versioning: append-only, items pin a version at add time |
| Two concurrent `POST /draft` opens: exactly one `201`, the other `409 questionnaire/draft-exists`, one draft row and one audit row | `_tests/modules/definition/routes/drafts.test.ts` | [[9-database-schema#5. Concurrency control]]: the questionnaire `FOR UPDATE` lock is the first statement of create-draft | Questionnaire versioning: publish, immutability, one draft |
| `POST /draft` is `409 draft-exists` while a draft is open and `404` for an unknown questionnaire | `_tests/modules/definition/routes/drafts.test.ts` | At most one draft per questionnaire (#8) | Questionnaire versioning: publish, immutability, one draft |
| `POST /draft/validate` on a forward reference is `200 { valid: false }` with the same items `publishDraft` then refuses with; no audit row, and the draft `ETag` is unchanged | `_tests/modules/definition/routes/drafts.test.ts` | [[7-application-boundary#4.1 Endpoints]]: validate runs exactly the publish read-and-validate path and never writes | Publish-time rule validation: forward refs, unsatisfiable conditions |
| `POST /draft/validate` is `{ valid: true, items: [] }` for a publishable draft, and `404` with no open draft or for an unknown questionnaire | `_tests/modules/definition/routes/drafts.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]: validate with no draft is `404` | Publish-time rule validation: forward refs, unsatisfiable conditions |

| `readQuestionVersions` with `pinnedByDraft` loads exactly the question versions one draft pins — not another version of the same question, not another draft's — each with its own options in position order; `questionVersionIn` loads named versions; an empty list loads nothing | `_tests/db/definition/question-versions.test.ts` | One builder-only loader behind the draft read, publish, draft validation and the bank reads, so a question version and its options are assembled one way | Question versioning: append-only, items pin a version at add time |
| `questionInputToColumns` splits each of the five response types into type, prompt, flat constraints and options; `storedQuestionToContent` round-trips every one back to the input with its identity, gives `options` only to choice types, and keeps `freeform` only on the option where it was set; `storedQuestionToVersion` adds `createdAt` as ISO 8601 and `createdBy` | `_tests/db/definition/question-content.test.ts` | One pure mapper between stored question-version rows and the wire, so the bank, the draft read and the snapshot serialize a question one way ([[5-questionnaire-format#3. Serialization]]) | Question versioning: append-only, items pin a version at add time |
| `mustExist` returns what the database answered with, falsy values included, and throws naming the row when a write cannot read its own result back or a published version's column is null; `databaseErrorOf` reads the SQLSTATE and constraint name through a wrapped `cause` chain and is undefined without one | `_tests/db/errors.test.ts` | Every write builds its wire result the same way, after its audit row and inside its transaction, and an impossible empty read-back is one invariant error rather than a malformed `2xx` | — code quality |
| `readDraftContents` lists a question version placed twice once, in first-placement order, while every item keeps its own content | `_tests/db/definition/draft-contents.test.ts` | The draft read's `questions` and publish's snapshot items come from the same contents | Questionnaire versioning: publish, immutability, one draft |
| `replaceDraft` refuses a newly placed archived question and a nonexistent question version as `invalid` with `publishDraft`'s item shape (`draft/question-archived`, `draft/question-version-unknown`), naming the offending item ids | `_tests/db/definition/drafts.test.ts` | One refusal shape for `questionnaire/draft-invalid`, built where the items are known, so the route does not rebuild item errors from the request | Question versioning: append-only, items pin a version at add time |
| `replaceDraft` reports only the first failing category — duplicated item ids over newly placed archived questions over unknown versions — and names every new placement of an archived question, in request order | `_tests/db/definition/drafts.test.ts` | The `PUT /draft` `422` keeps its precedence and item order when the refusal moves into the db layer | Questionnaire versioning: publish, immutability, one draft |
| ESLint warns on raw SQL in the backend — a drizzle `sql` template (typed or not) and any `sql.*` call such as `sql.raw` — in sources and tests; the query builder, `exists` and `max` pass; `src/db/schema.ts` is exempt because checks and defaults are SQL expressions; the rule does not reach other workspaces, still carries the double-assertion warning in the backend, and a reasoned disable counts as used | `tests/lint-raw-sql.test.ts` | Queries go through the typed builder; the remaining exceptions (calls to `audit.record`, `definition.promote_draft` and `to_regclass`, partition DDL, `jsonb_array_length`, the database clock's `now()`) carry their reason at the call site | — code quality |
| `modules/execution` rejects importing `db/definition`, `db/seed` and `db/audit` (nested, extensionless, type-only, re-exported, and through `./` segments or doubled slashes) while `db/client`, `db/schema` and look-alikes pass; `db/definition` rejects importing any backend module; both inherit the telemetry patterns | `tests/lint-db-boundary.test.ts` | [[7-application-boundary#3.1 Module encapsulation]] extended to the db layer: execution reaches no definition-side persistence, and persistence sits below the modules | — code quality |
| `db/execution` rejects every backend module and the definition side of the db layer, by sibling path as well as through `db/`, while `db/client`, `db/schema`, its own files and `@qp/shared` pass | `tests/lint-db-boundary.test.ts` | Execution's persistence sits below the modules on the same rule as the definition side ([[2-design-doc#17. Decisions Log]] #80) | — code quality |
| `modules/definition` and `db/definition` reject importing `db/execution` (sibling, through `db/`, nested, type-only, re-exported, `./` segments, doubled slashes), `execution-order.js` look-alikes pass, and `modules/execution` and `db/execution` still reach their own persistence | `tests/lint-db-boundary.test.ts` | The grant barrier in code: the definition half never reads or writes session and response rows ([[7-application-boundary#3.2 Database grants]]) | The definition/execution barrier |
| ESLint warns on constructing a Postgres connection in the backend — `new pg.Client`, `new pg.Pool` and the named `new Client` — in sources and tests, while `db/client.ts`, the harness and its `globalSetup` are exempt, `openDatabase` itself passes, and the rule does not reach other workspaces; separately, importing `openDatabase` inside `_tests/` is rejected while a type import, the harness and production pass | `tests/lint-connections.test.ts` | Pool sizing has one place to land ([[2-design-doc#17. Decisions Log]] #77), and a test connection the harness did not hand out is one it cannot release; the exceptions (the factory, and the harness needing a raw client to `CREATE` the per-worker database) carry their reason in the lint test that allows them, the one test exception at its call site | — code quality |
| `withLockedQuestionnaire` hands its work the locked row's id and `closesAt` and returns the work's result; for an unknown questionnaire it returns `questionnaire-not-found` without running the work | `_tests/db/definition/questionnaire-rows.test.ts` | The questionnaire lock is taken only by the wrapper that opens the transaction, so it is always the first statement of publish, open-next-draft and retire ([[9-database-schema#5. Concurrency control]]) | Questionnaire versioning: publish, immutability, one draft |
| While one transaction holds the questionnaire through `withLockedQuestionnaire`, a second `withLockedQuestionnaire` on the same row is seen waiting on the questionnaire `FOR UPDATE` in `pg_stat_activity` and runs its work only after the first commits | `_tests/db/definition/questionnaire-rows.test.ts` | [[9-database-schema#5. Concurrency control]]: the questionnaire `FOR UPDATE` serializes publish against create-next-draft, the race that silently reverts a version | Questionnaire versioning: publish, immutability, one draft |
| `questionnaireExists` is true for a questionnaire and false for an unknown id | `_tests/db/definition/questionnaire-rows.test.ts` | The unlocked existence check version history uses to tell `404` from an empty history | Questionnaire versioning: publish, immutability, one draft |
| `readOpenDraft` finds nothing when only a published version exists, then returns the next draft's id, title, `updatedAt` and revision | `_tests/db/definition/questionnaire-version-rows.test.ts` | "Open draft" is defined once as `status = 'draft'` for the questionnaire; a published version is never read as the draft (#8) | Questionnaire versioning: publish, immutability, one draft |
| While one transaction holds the draft row through `withCurrentDraft`, `readOpenDraft` returns at once and a second `withCurrentDraft` waits until the first commits | `_tests/db/definition/questionnaire-version-rows.test.ts` | Locking the draft row needs a `Transaction` by type, so publish and replace keep their draft row lock and plain reads never block | Questionnaire versioning: publish, immutability, one draft |
| While another transaction holds the questionnaire lock, `publishDraft` is seen in `pg_stat_activity` waiting on the questionnaire `FOR UPDATE` itself, not on a later statement, and publishes only after the holder commits | `_tests/db/definition/publish.test.ts` | [[9-database-schema#5. Concurrency control]]: the questionnaire lock is publish's first statement; without it publish would still block, but later, at `promote_draft` | Questionnaire versioning: publish, immutability, one draft |
| While another transaction holds the questionnaire lock, `createNextDraft` is seen waiting on the questionnaire `FOR UPDATE` itself, not on the draft insert, and creates the draft only after the holder commits | `_tests/db/definition/drafts.test.ts` | [[9-database-schema#5. Concurrency control]]: create-next-draft reads "latest published" only under the questionnaire lock | Questionnaire versioning: publish, immutability, one draft |
| While another transaction holds the questionnaire lock, `setClosesAt` is seen waiting on the questionnaire `FOR UPDATE` itself, not on its `UPDATE`, and updates only after the holder commits | `_tests/db/definition/questionnaires.test.ts` | [[9-database-schema#5. Concurrency control]]: retire takes the questionnaire lock first, so the audited `from` is the committed value | Questionnaire versioning: publish, immutability, one draft |
| `readQuestionnaireSummary` reports `hasDraft` for a draft-only, a published-only and a republished-with-draft questionnaire, equal to the matching `listQuestionnaireSummaries` entry, and is `undefined` for an unknown id | `_tests/db/definition/questionnaires.test.ts` | `QuestionnaireSummary` is built by one select, so the list, `POST /questionnaires` and `setClosesAt` cannot disagree | Questionnaire versioning: publish, immutability, one draft |

#### G3 — publish, version history, retirement

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `publishDraft` refuses the current revision when the command names a different draft version id, as `stale` | `_tests/db/definition/publish.test.ts` | #43: a draft precondition is the version id **and** the revision, because a new draft restarts at `0` | Questionnaire versioning: publish, immutability, one draft |
| `publishDraft` returns the published `VersionSummary` (version, `publishedAt`, `publishedBy: null`, `itemCount`, `formatVersion`), read inside its own transaction after the audit write | `_tests/db/definition/publish.test.ts` | Every command builds its wire result inside its transaction, so `POST /publish` makes no read after commit | Questionnaire versioning: publish, immutability, one draft |
| `POST /publish` answers v1 then v2 with `201 VersionSummary`; v1's stored `snapshot::text` and its served `GET /versions/1` payload are byte-identical after v2 is published; both publish audit rows carry `prototype-author` | `_tests/modules/definition/routes/drafts.test.ts` | A published version is immutable (#8, [[5-questionnaire-format#6.4 Three layers of immutability enforcement]]); the author is the placeholder (#57) | Questionnaire versioning: publish, immutability, one draft |
| A forward reference and an unsatisfiable predicate each return `422 questionnaire/draft-invalid` at the publish URL, naming the offending item in `items`, and the draft stays a draft | `_tests/modules/definition/routes/drafts.test.ts` | Publish refuses on `validateDraft`'s result ([[7-application-boundary#4.3 Publish and retire]], #52) | Publish-time rule validation: forward refs, unsatisfiable conditions |
| A stale `If-Match` revision is `409 questionnaire/draft-stale` and leaves the draft open | `_tests/modules/definition/routes/drafts.test.ts` | Optimistic concurrency on the draft ([[7-application-boundary#4.1 Endpoints]], #43) | Questionnaire versioning: publish, immutability, one draft |
| A malformed `If-Match` is `400 request/invalid` pointing at `/headers/if-match`, with the publish URL as its `instance`, and the draft stays open | `_tests/modules/definition/routes/drafts.test.ts` | #43: a malformed precondition is refused, never treated as unconditional; `draftPreconditionOf` returns the problem instead of throwing | Questionnaire versioning: publish, immutability, one draft |
| Publish with no open draft, and publish on an unknown questionnaire, are `404 resource/not-found` | `_tests/modules/definition/routes/drafts.test.ts` | [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]: no open draft is a `404` | Questionnaire versioning: publish, immutability, one draft |
| `GET /versions` lists published versions `version DESC`, metadata only (`publishedAt`, `publishedBy: null`, `itemCount` from the snapshot, `formatVersion`), leaving out the open draft | `_tests/modules/definition/routes/versions.test.ts` | #40: every list has an explicit `ORDER BY`; history is metadata only ([[7-application-boundary#4.2 Reading version history — this is a definition-side concern]]); `publishedBy` is unrecorded, deferred with authentication ([[2-design-doc#17. Decisions Log]] #64) | List endpoints return a deterministic order |
| `GET /versions` is `[]` for a questionnaire with only a draft and `404` for an unknown questionnaire | `_tests/modules/definition/routes/versions.test.ts` | A draft is not a version ([[9-database-schema#3.1 The published-only guard]]) | Questionnaire versioning: publish, immutability, one draft |
| `GET /versions/:v` returns the stored snapshot verbatim with `ETag: "<questionnaireId>:<version>:<formatVersion>"` and `Cache-Control: private, max-age=31536000, immutable` | `_tests/modules/definition/routes/versions.test.ts` | #44: the pinned definition is immutable and privately cacheable ([[7-application-boundary#6.4 Caching]]) | Questionnaire versioning: publish, immutability, one draft |
| `GET /versions/:v` is `404` for the open draft's would-be version, an unknown version and an unknown questionnaire | `_tests/modules/definition/routes/versions.test.ts` | A draft is structurally unreadable as a version ([[9-database-schema#3.1 The published-only guard]]) | Questionnaire versioning: publish, immutability, one draft |
| `GET /versions/:v` for a stored snapshot matching no known format (a format 1 document with a blank title, published through `promote_draft`) is `500 internal` with no `ETag` and a body naming no schema or format | `_tests/modules/definition/routes/versions.test.ts` | The admin endpoint serves a snapshot as stored only once it matches a known format, with no cast (Decisions Log #56) | Questionnaire versioning: publish, immutability, one draft |
| `PUT /closes-at` sets (normalizing an offset to UTC), reschedules and clears `closesAt`, each `200 QuestionnaireSummary`; audited `retire`, `retire`, `reopen` with `{ from, to }` and `prototype-author` as actor | `_tests/modules/definition/routes/questionnaires.test.ts` | Retirement is one nullable `closes_at`, and clearing it undoes a premature retirement ([[2-design-doc#8.1 Questionnaire lifecycle and retirement]], [[7-application-boundary#4.3 Publish and retire]]) | Retirement: start and submit rejected past `closes_at` — the write that sets it |
| `PUT /closes-at` on a never-published questionnaire reports `currentVersion: null` and `hasDraft: true` | `_tests/modules/definition/routes/questionnaires.test.ts` | The response is the same `QuestionnaireSummary` as `GET /questionnaires` | Retirement: start and submit rejected past `closes_at` — the write that sets it |
| `PUT /closes-at` on an unknown questionnaire is `404` and writes no audit row | `_tests/modules/definition/routes/questionnaires.test.ts` | The audit row commits only with the domain change ([[9-database-schema#9.1 A dedicated role, inside the publish transaction]]) | Retirement: start and submit rejected past `closes_at` — the write that sets it |

#### G4 — integration

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres** (milestone **M4**)

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The definition module registers exactly the 18 `definitionRoutes`, each at its method and `/api/definition`-prefixed URL with the shared `defineRoute` schema object itself (identity, not a copy), and no route the contract does not declare | `_tests/modules/definition/definition-api.test.ts` | One route contract in `@qp/shared` for server and client; no URL or schema is written in the backend ([[7-application-boundary#6.3 Input validation]]) | — conventions |
| Every `definitionRoutes` entry is routable on the `useDefinitionApp` harness app at its method and prefixed URL | `_tests/modules/definition/definition-api.test.ts` | The contract and the mounted module cannot drift apart unnoticed | — conventions |
| One flow through `inject()` alone, no database function called: create two questions → create a questionnaire (`201`, no version, draft open) → `GET /draft` (`ETag` revision `0`) → `PUT /draft` with that `ETag` (`200`, `ETag` revision `1`) → `validate` (`valid: true`) → `publish` with the pre-save `ETag` is `409 questionnaire/draft-stale`, with the saved one `201` v1 → the questionnaire lists `currentVersion: 1` with no draft → save a question revision (`201` question version 2) → open the next draft (`201`, a new version id, `ETag` revision `0`, items still pinned to question version 1) → re-pin to question version 2 (`200`, `ETag` revision `1`) → `publish` with the previous draft's `ETag` is `409`, with the new one `201` v2 → `GET /versions` is `[2, 1]` → v1's served snapshot is byte-identical to its payload before v2 and still pins question version 1, v2 pins question version 2 with the relabel, each with its `ETag` → set `closesAt` (`200`, listed) and clear it (`200`, listed `null`) | `_tests/modules/definition/definition-api.test.ts` | The authoring → publish → revise → republish → retire path composes across G1–G3 over HTTP alone: If-Match threads through every draft write, a published version is immutable (#8), items pin a question version and never auto-upgrade (#31), history is `version DESC` (#40), and clearing `closesAt` reopens | Questionnaire versioning: publish, immutability, one draft |

### Wave 2 — Track 5: execution plugin

**Backend integration — `apps/backend`, `inject()` against Testcontainers Postgres, the module on `qp_execution`** (milestones **M5**, **M6**)

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `POST /sessions` pins the current published version, returns exactly its snapshot with the session, stores `in_progress` against a published row, and is `no-store` | `_tests/modules/execution/sessions.test.ts` | Version resolution happens once, at session creation, and the rendered bytes are the pinned bytes ([[7-application-boundary#5.1 Endpoints]]) | Sessions: start, resume, version pinning |
| Session ids are distinct random UUIDv4s | `_tests/modules/execution/sessions.test.ts` | A session id is a bearer capability and must not be enumerable ([[7-application-boundary#7. Access model and data barriers]]) | Sessions: start, resume, version pinning |
| An unknown questionnaire and a never-published one both return the same `404 resource/not-found` and create no session | `_tests/modules/execution/sessions.test.ts` | The public surface cannot enumerate drafts (§5.2) | The definition/execution barrier |
| Starting is `201` while `closes_at` is in the future and `409 questionnaire/closed` at and after the instant, with time frozen by `vi.setSystemTime` | `_tests/modules/execution/sessions.test.ts` | One `closes_at` comparison is the retirement lifecycle ([[2-design-doc#8.1 Questionnaire lifecycle and retirement]]) | Retirement: start and submit rejected past `closes_at` |
| A malformed questionnaire id and an extra body field are `400 request/invalid` with `/body/...` pointers and `schema/*` codes | `_tests/modules/execution/sessions.test.ts` | `400` is schema failure only (#20); the pointer extension of [[7-application-boundary#6.1 Error format — RFC 9457 problem details]] | — contract |
| `GET /sessions/:sessionId` returns the session and its pinned definition identical to the create response, `no-store`; unknown id `404`, malformed id `400` | `_tests/modules/execution/sessions.test.ts` | Resume returns the pin, never a newer version (#18) | Sessions: start, resume, version pinning |
| Resume changes no column of the session row | `_tests/modules/execution/sessions.test.ts` | Nothing is written between session start and submit (#25) | Sessions: start, resume, version pinning |
| Resuming an in-progress session past `closes_at` is `409 questionnaire/closed`; a session submitted before the close still returns `200` with `submitted` and its `submittedAt`, so the app can show the receipt | `_tests/modules/execution/sessions.test.ts` | Hard cutoff for work in progress; a submitted session is history, not in-flight ([[10-frontend#4.2 Entry and resume]]) | Retirement: start and submit rejected past `closes_at` |
| Submitting the demo's yes path returns the receipt and writes one row per answer carrying `question_id` and the pinned `question_version`, every `created_at` equal to the session's `submitted_at`, and the session `submitted` | `_tests/modules/execution/submit.test.ts` | Responses store what you aggregate on and what you render with; one literal timestamp keeps a session in one partition ([[9-database-schema#6.4 Partitioning]]) | Sessions: start, resume, version pinning |
| Submitting the no path, with explicit `null`s for the skipped branch, writes rows for `itm_01` and `itm_04` only | `_tests/modules/execution/submit.test.ts` | The mandatory branching demo at the API; unanswered is absent, not null (#37) | Conditional navigation over one and several earlier answers |
| `72.50` is stored as `72.5` with the unit copied from the pinned question; the stored `response_digest` equals the digest recomputed from the stored rows | `_tests/modules/execution/submit.test.ts` | #42 exactness and server-filled unit; the digest is a pure function of the persisted rows (#37) | Submit idempotency: same digest replays, different digest conflicts |
| A `not_future` date one day ahead of UTC today is accepted and two days ahead is `422 date/in-future` | `_tests/modules/execution/submit.test.ts` | The server clock is UTC today with one day of tolerance (#38) | Relative date constraints across a timezone boundary |
| The same answers re-serialized (key order, explicit `null`) an hour later replay the original receipt with `200` and write nothing; a multiple-choice answer in another click order with `72.500` for `72.5` replays too | `_tests/modules/execution/submit.test.ts` | Idempotency on the session via the digest over validated rows, never the body ([[7-application-boundary#5.4 Submit: authority, validation, idempotency]]) | Submit idempotency: same digest replays, different digest conflicts |
| Different answers, and answers that no longer validate, on a submitted session are `409 session/already-submitted` and the original rows stand | `_tests/modules/execution/submit.test.ts` | A genuine second submission is an error, never an overwrite of medical history | Submit idempotency: same digest replays, different digest conflicts |
| A retry of the same answers after `closes_at` has passed still replays `200` with the original `submittedAt` | `_tests/modules/execution/submit.test.ts` | The cutoff applies to submitting, and the submit already happened; the replay validates as of the original `submitted_at` so relative dates cannot turn a retry into a `409` | Submit idempotency: same digest replays, different digest conflicts |
| Two concurrent submits with the same answers both return the same receipt and write one set of rows; two concurrent different submissions produce exactly one `200` and one `409`, with only the winner's rows | `_tests/modules/execution/submit.test.ts` | `SELECT ... FOR UPDATE` on the session plus the `in_progress → submitted` transition makes a second write impossible ([[9-database-schema#5. Concurrency control]]) | Submit idempotency: same digest replays, different digest conflicts |
| Answers to the two items the no path hides are `422 submission/invalid` naming both with `answer/not-visible`; the body contains neither sentinel value; no row is written and the session stays `in_progress` | `_tests/modules/execution/submit.test.ts` | The server is the authority on the reachable path; error bodies never echo answers (§5.5) | Server as authority: an answer to an unreachable item is rejected |
| An unknown option, a type-mismatched answer, a missing required answer and an unknown item key are reported together in item order, with nothing written | `_tests/modules/execution/submit.test.ts` | All-or-nothing in one transaction | Response validation: required, per-type value rules |
| Duplicate `optionIds` in one multiple-choice answer are `422 choice/duplicate-option` naming the item, with nothing written | `_tests/modules/execution/submit.test.ts` | #34: the one `response` invariant the database does not hold | Duplicate `option_ids` inside one answer |
| Submitting an in-progress session at `closes_at` is `409 questionnaire/closed` and the session stays `in_progress`; an unknown session is `404` with the submit URL as `instance` | `_tests/modules/execution/submit.test.ts` | Hard cutoff ([[2-design-doc#8.1 Questionnaire lifecycle and retirement]]) | Retirement: start and submit rejected past `closes_at` |
| A JSON number, a client-sent `unit`, a non-canonical decimal and a uuid item key are `400 request/invalid` with a pointer naming the answer, no value in the body and nothing written; a body that is not JSON is `400` too | `_tests/modules/execution/submit.test.ts` | #42 on the wire: the module uses the shared `requestValidatorCompiler`, which validates bodies exactly, so Fastify's defaults cannot turn `72.5` into `"72.5"` or drop a `unit` | Response validation: required, per-type value rules |
| A session started before v2 is published keeps returning v1 on resume, and a session started after gets v2 | `_tests/modules/execution/version-pinning.test.ts` | Republish is invisible to in-flight sessions ([[2-design-doc#8. Sessions & Responses]]) | Sessions: start, resume, version pinning |
| Predicate-change fixture: the same yes / other / date answers are accepted for a session pinned to v1 and `422` naming `itm_03` for one pinned to a v2 that hides it, without echoing the date; a v1 session omitting the date is `answer/required` | `_tests/modules/execution/version-pinning.test.ts` | Submit re-evaluates the *pinned* definition ([[8-testing#6. Test data and fixtures]]) | Path re-evaluation uses the *pinned* definition, not the latest published |
| v1 and v2 answers of `opt_hyperten` count together on `question_id` with question versions 3 and 4; each submitted session renders its own label through its pinned snapshot | `_tests/modules/execution/version-pinning.test.ts` | #26: the relabel keeps the option id, so meaning is preserved across the republish | Response meaning preserved across a republish |
| The module registered alone on a bare Fastify instance drives a session to submission and exposes no definition route | `_tests/modules/execution/boundary.test.ts` | Plugin encapsulation is the test boundary (§2.2) | The definition/execution barrier |
| The module's database handle is `qp_execution` and is `42501` reading `question`, `question_version`, `question_version_option`, `questionnaire_item` and the base `questionnaire_version`, and updating `questionnaire` | `_tests/modules/execution/boundary.test.ts` | Execution cannot reach authoring tables ([[7-application-boundary#3.2 Database grants]]) | The definition/execution barrier |
| Execution's sources, `src/modules/execution` and `src/db/execution` together, import only `session`, `response`, `questionnaire` and `definitionSchema` from the schema, read versions through `published_questionnaire_version`, and import nothing from `db/definition`, `db/seed` or `db/audit` — by sibling path as well as through `db/`, with the pattern first proven against each spelling so the scan cannot pass vacuously | `_tests/modules/execution/boundary.test.ts` | Execution never joins to an authoring table in code either (§2.1) | The definition/execution barrier |
| An unhandled failure is `500 internal` at the request URL, whose `detail` is the request id and carries no error text | `_tests/modules/execution/boundary.test.ts` | `500` bodies are a correlation id, never a message or stack (§6.2) | No respondent answer in telemetry |
| A pinned snapshot the loader refuses — published through `promote_draft` under `formatVersion` 2, or in format 1 with a blank title — makes `POST /sessions` a `500 internal` problem naming no schema, and starts no session | `_tests/modules/execution/published-definitions.test.ts` | Execution loads every pinned snapshot through `readStoredDefinition` (#56), so a stale or corrupt snapshot fails loudly instead of rendering ([[5-questionnaire-format#6.5 Snapshot format version]]) | Sessions: start, resume, version pinning |
| `buildApp` mounts the execution module at `/api/run` beside the definition module: a schema failure on `POST /api/run/sessions` is its `400`, and an unknown path inside it is a `404` problem | `_tests/app.test.ts` | Two encapsulated plugins in one process, each on its own pool ([[7-application-boundary#8.1 Now — two plugins, one process]]) | The definition/execution barrier |
| The execution module registers exactly the 3 `executionRoutes`, each at its method and `/api/run`-prefixed URL with the shared `defineRoute` schema object itself (identity, not a copy), and no route the contract does not declare | `_tests/modules/execution/plugin.test.ts` | One route contract in `@qp/shared` for server and client; no URL or schema is written in the backend ([[7-application-boundary#6.3 Input validation]]) | — conventions |
| Every `executionRoutes` entry is routable on the `useExecutionApp` harness app at its method and prefixed URL | `_tests/modules/execution/plugin.test.ts` | The contract and the mounted module cannot drift apart unnoticed | — conventions |

**Domain unit — `packages/shared`: the snapshot loader**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `readStoredDefinition` returns intake v1 and v2, stored as JSON in the current format, as the `PublishedDefinition` they hold | `domain/stored-definition.test.ts` | One loader for every reader of a stored snapshot (#56, [[7-application-boundary#3.3 What stays shared]]) | Sessions: start, resume, version pinning |
| Stored as the current format, a blank title, an extra field, a document naming `formatVersion` 2, an item without a question, `{}`, a JSON string and `null` are each refused with the unsupported-snapshot error | `domain/stored-definition.test.ts` | The upgraded result is checked against `PublishedDefinition`, so a corrupt snapshot fails loudly on load ([[5-questionnaire-format#6.5 Snapshot format version]]) | Sessions: start, resume, version pinning |
| A valid format 1 document stored under a future, zero, negative or fractional format version is refused | `domain/stored-definition.test.ts` | Only formats the upgrade chain reaches load; the chain is empty while format 1 is the only format (#56) | Sessions: start, resume, version pinning |
| `readStoredDefinition`'s return type is exactly `PublishedDefinition` | `domain/stored-definition.test.ts` | Callers get the wire type without a cast | — contract |

### Wave 3 — contract commit

**Domain unit — `packages/shared`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `QUESTION_RULE_CODES` is the closed set of eight question rules, with `question/other-not-freeform` after `question/freeform-not-other`, ending with `question/type-changed` | `problems.test.ts` | A question's response type is locked after its first save, and the lock is one of the question rules (#61); the id `other` is reserved for the freeform option (#83) | Question versioning: append-only, items pin a version at add time |
| A `request/invalid` body carrying `{ pointer: "/body/question/type", code: "question/type-changed" }` satisfies the wire schema | `problems.test.ts` | The type lock surfaces in the `errors` extension like every other question rule ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — contract |

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `appendQuestionVersion` with a type different from the latest version returns `type-changed` and writes no version and no audit row; the same type saves version 2; an unknown question is `question-not-found` | `_tests/db/definition/questions.test.ts` | #61: the type lock is checked in the repository, not only in the editor | Question versioning: append-only, items pin a version at add time |
| While another transaction holds the question row lock after appending a same-type version, a type-changing `appendQuestionVersion` waits on the question `FOR UPDATE` as its first statement, then returns `type-changed`; the question ends at version 2 | `_tests/db/definition/questions.test.ts` | #61: the type is compared under the question row lock, against the version the new one would follow ([[9-database-schema#5. Concurrency control]]) | Question versioning: append-only, items pin a version at add time |
| `POST /questions/:questionId/versions` with a different response type is `400 request/invalid` with exactly the request URL as `instance` and `errors: [{ pointer: "/body/question/type", code: "question/type-changed" }]`, saves no version and writes no audit row | `_tests/modules/definition/routes/questions.test.ts` | #61, [[5-questionnaire-format#6.2 Question identity and versioning]]: a different type is a different question | Question versioning: append-only, items pin a version at add time |
| After two `single_choice` versions, a `multiple_choice` save is `400 question/type-changed` and a `single_choice` save is `201` as version 3 | `_tests/modules/definition/routes/questions.test.ts` | #61: the comparison is against the latest version, and a near type is still a different type | Question versioning: append-only, items pin a version at add time |
| A save that fails a cross-field rule reports that rule, not a type change | `_tests/modules/definition/routes/questions.test.ts` | Question-rule codes surface in the `request/invalid` `errors` extension ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| A concurrent same-type save and type-changing save of one question: the same-type save is `201` as version 2, the other `400 question/type-changed` | `_tests/modules/definition/routes/questions.test.ts` | #61: the row lock serializes the type check with the append | Question versioning: append-only, items pin a version at add time |
| `readQuestionnaireSummary.updatedAt` is the draft's backdated `updated_at`, is unchanged by `setClosesAt`, and after `replaceDraft` equals the draft row's new `updated_at` | `_tests/db/definition/questionnaires.test.ts` | #59: `updatedAt` is the latest `questionnaire_version.updated_at`; a `closesAt` change is not an edit | — contract |
| `POST /questionnaires` returns `updatedAt` equal to the new draft's `updatedAt`; after the draft is backdated, `PUT /draft` moves the list's `updatedAt` to the written draft's `updatedAt` | `_tests/modules/definition/routes/questionnaires.test.ts` | #59, #53: the questionnaire list's "most recently edited" sort has a field to sort on, which draft writes bump | — contract |
| Setting and clearing `closesAt` on a backdated draft leaves `updatedAt` unchanged in both `PUT /closes-at` responses and on `GET /questionnaires` | `_tests/modules/definition/routes/questionnaires.test.ts` | #59: retiring changes whether respondents can answer, not what the questionnaire says | — contract |
| A published questionnaire's `updatedAt` is its version's `updated_at`; once the next draft opens it is that draft's `updatedAt` | `_tests/modules/definition/routes/questionnaires.test.ts` | #59: the latest version across published versions and the open draft | — contract |
| After an older questionnaire's draft is written, `GET /questionnaires` still lists the newer questionnaire first, with the older one carrying the later `updatedAt` | `_tests/modules/definition/routes/questionnaires.test.ts` | #40, #53: the list keeps `ORDER BY id DESC` and sorting on `updatedAt` is the client's | List endpoints return a deterministic order |

**Frontend component — `packages/ui`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `errorsByItemId` groups a `submission/invalid` body's `items` by `itemId`, keeping the server's order within each item | `questionnaire/errors-by-item-id.test.tsx` | One grouping of the problem body for both apps (#55, #62) | Response validation: required, per-type value rules |
| A body typed as the `ProblemDetails` wire schema groups the same as one built with `problem()` | `questionnaire/errors-by-item-id.test.tsx` | The apps can pass the parsed wire body straight in, with no cast | — contract |
| `answer/not-visible` and `answer/unknown-item` are dropped, and an item carrying only those has no entry; every other submission code is kept | `questionnaire/errors-by-item-id.test.tsx` | #55: the two codes never attach to a rendered item ([[10-frontend#3. `packages/ui` — primitives and the renderer]]), revisited in gh#23 | Response validation: required, per-type value rules |
| A `DraftItemCode` such as `predicate/forward-reference`, carried on a `submission/invalid` wire body alongside a real submission code, is dropped rather than passed through | `questionnaire/errors-by-item-id.test.tsx` | The kept filter is an allowlist against `SUBMISSION_ITEM_CODES`, not a denylist of the two dropped codes, so a code from the sibling `DraftItemCode` catalogue never reaches the renderer's message lookup | Response validation: required, per-type value rules |
| A `questionnaire/draft-invalid`, `request/invalid`, `session/already-submitted` or unknown-type body, and a `submission/invalid` body with no items, yield `{}` | `questionnaire/errors-by-item-id.test.tsx` | #55: the output is the renderer's `SubmissionItemCode` shape; `DraftItemCode`s go to the admin catalogue and summary panel (#54, #60), not the renderer | — contract |
| The grouped result passed as the renderer's `errors` prop shows each item's first code as its message, and nothing for a dropped code | `questionnaire/errors-by-item-id.test.tsx` | Client and server errors read the same through one catalogue ([[10-frontend#3. `packages/ui` — primitives and the renderer]]) | Response validation: required, per-type value rules |
| `Dialog` opens a dialog named by its title and described by its description with focus inside and a "Close" button; Escape closes it and returns focus to the trigger; `DialogClose` closes it; axe finds nothing closed or open | `primitives/dialog.test.tsx` | Radix carries dialog focus trapping and restoration for the question editor ([[10-frontend#7. Accessibility]], #66) | — accessibility commitment |
| `NativeSelect` is a select named by its label, reporting the chosen value; the wrapper reflects `disabled`; axe finds nothing | `primitives/native-select.test.tsx` | #66: the predicate editor uses the primitive directly, replacing admin's own `NativeSelect` | — accessibility commitment |
| `Popover` toggles `aria-expanded` and `aria-controls` on its trigger; Escape closes it and returns focus; axe finds nothing closed or open | `primitives/popover.test.tsx` | #66 | — accessibility commitment |
| `Tooltip` opens on hover and on focus, naming itself through `aria-describedby`; Escape closes it; axe finds nothing closed or open | `primitives/tooltip.test.tsx` | #66: `InfoTip` is built on this primitive instead of hand-rolled hover/focus/Escape state | — accessibility commitment |
| `Table` renders a native `<table>` named by its caption, with column headers, body and footer rows, and passes `className` to the table; axe finds nothing | `primitives/table.test.tsx` | #66 | — accessibility commitment |

### Wave 3 — Track 6: admin app

#### PR0

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `/questionnaires`, `/questionnaires/:id/draft`, `/questionnaires/:id/versions`, `/questionnaires/:id/versions/:v` and `/questions`, under the `/admin` basepath, each render their screen's stub inside the shell, with the path's questionnaire id and version reaching the screen | `_tests/router.test.tsx` | Every admin screen is registered once in `router.tsx`, so no screen PR edits the route tree ([[4-implementation-plan#How Track 6 runs]]) | — conventions |
| `/` redirects to `/questionnaires`; an unknown path renders the not-found screen inside the shell; the tree holds exactly those six paths and no question-editor route | `_tests/router.test.tsx` | The question editor is a dialog, not a route ([[10-frontend#5.3 The question editor, and re-pinning]]) | — conventions |
| `callDefinition` takes the method and `/api/definition` URL from the shared route object, fills and encodes path parameters and the query string, sends a JSON body only for a route that declares one, and returns the body typed and checked against the route's response schema | `_tests/api/client.test.ts` | One route contract in `@qp/shared` for server and client; no URL or schema is written in the app ([[7-application-boundary#6.3 Input validation]]) | — contract |
| A problem+json `404`, `400 request/invalid` and `422 questionnaire/draft-invalid` become a `ProblemError` holding the shared `Problem` for that slug, `errors` and `items` typed; an HTML `502`, a problem `type` outside the union, a body failing the route schema and an undeclared success status are each an `UnexpectedResponseError` | `_tests/api/client.test.ts` | Errors are the closed RFC 9457 union, switched on by slug and never by message (#20, [[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| The `ETag` from `GET /draft` is sent as `If-Match` on `PUT /draft`, and the `ETag` that response carries is sent as `If-Match` on `POST /publish`; opening a draft captures its `ETag`; a draft response with no `ETag`, a strong one, or one naming another draft version is refused; the four draft routes do not compile through the generic call | `_tests/api/client.test.ts` | The draft `ETag` has one code path from read to write (#43, [[10-frontend#5.2 The draft editor]]) | — conventions |
| `useDraftMutation` applies a change to the cached draft before the `PUT`, sends the cached `ETag` as `If-Match`, and stores the saved draft with its new `ETag`; two quick changes run one after the other, the second sending the first response's `ETag` | `_tests/api/mutations/use-draft-mutation.test.tsx` | Every draft write goes through one optimistic hook, and an author's own writes never read as someone else's (#43) | — conventions |
| On `409 questionnaire/draft-stale` the hook restores the pre-change draft, refetches it, exposes a `stale` rejection, and never sends a change queued behind the rejected one | `_tests/api/mutations/use-draft-mutation.test.tsx` | The author is never shown a draft the server did not accept, and a queued change built on it cannot overwrite another author's edit ([[10-frontend#5.2 The draft editor]], `AdminDraftConflict`) | — conventions |
| On `422 questionnaire/draft-invalid` the hook rolls the change back and exposes an `invalid` rejection carrying the `items`, with no conflict and no refetch; any other failure is a `failed` rejection; a `409` and a `422` on publish are told apart the same way; the example `422` is a missing question version on a reorder | `_tests/api/mutations/use-draft-mutation.test.tsx` | A `422` about the draft's own contents, such as a missing version or a newly added archived question (#75), is not reported as another author's edit ([[4-implementation-plan#How Track 6 runs]] PR4 note) | — conventions |
| A `4xx` problem is not retried and a `5xx` or network failure is retried up to three times; `refetchOnWindowFocus` keeps TanStack Query's default | `_tests/api/query-client.test.ts` | Another tab's edit shows up when the author returns (#69) | — conventions |
| axe finds no violations in the shell on the list, a preview, the bank and the not-found screen; the shell has a banner, a named main navigation and a main landmark; the nav marks Questionnaires current on questionnaire screens and Question bank on the bank, with `/admin`-based links | `_tests/shell/app-shell.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — conventions |

#### PR1

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The list sorts rows by `updatedAt`, newest first, whatever order `GET /questionnaires` returned them in; the "Last edited" header carries `aria-sort="descending"`; there is no search box and no filter control | `_tests/screens/questionnaire-list.test.tsx` | One client-side sort axis, "most recently edited", and nothing else in Wave 3 (#53, #59) | — conventions |
| Each row shows its status (published v*N*, never published, closed at v*N*), draft open, closing date or open-ended, and key when it has one; only published questionnaires link to their version history; closed and last edited are read as of the list query's `dataUpdatedAt`, so a `closesAt` that passes after the list loaded reads closed once a refetch lands; with fake timers the list is re-requested after 60 seconds, not before, and a row whose `closesAt` that refreshed load passes turns from published to closed | `_tests/screens/questionnaire-list.test.tsx` | Retirement is a `closesAt` in the past, not a separate state ([[7-application-boundary#4.3 Publish and retire]]); the list refreshes every minute through the query's `refetchInterval` while the screen is open, and on window focus (#69), not through a clock | — conventions |
| A pending list shows a loading status; an empty list shows the empty state; a failed list shows an alert whose "Try again" loads it | `_tests/screens/questionnaire-list.test.tsx` | The screen never renders a blank or a stale-looking table for a list it does not have | — conventions |
| Creating a questionnaire requires a name and a title, focusing the first missing field without a request; `POST /questionnaires` sends `{ name, title }` trimmed and no `key`, then the list is refetched and the app navigates to the new draft; a failed create keeps the dialog open with an alert | `_tests/screens/questionnaire-list.test.tsx` | `key` is not presented as required, since a taken key is a `500` and the field may be removed (gh#15, #65) | — conventions |
| Opening a questionnaire with a draft navigates to the draft route with no write; without one, `POST /draft` opens the next draft, caches it with its `ETag` and navigates to it; any other failure shows an alert and stays on the list | `_tests/screens/questionnaire-list.test.tsx` | One open-draft action, `src/api/mutations/use-open-draft.ts`, shared with the history screen | — conventions |
| On `409 questionnaire/draft-exists` the open-draft action refetches the list and the draft that now exists, caches that draft's `ETag` and navigates to it, with no alert | `_tests/screens/questionnaire-list.test.tsx` | The losing tab wanted a draft and one exists, so the race is not an error (#67) | — conventions |
| Retiring sets `closesAt` through `PUT /closes-at` from the dialog's local date and time as an ISO instant and the row turns closed from the returned summary without refetching the list; reopening clears it with `{ closesAt: null }` and the row turns open-ended; a future `closesAt` stays published and offers "Reschedule"; a failed write keeps the dialog open with an alert | `_tests/screens/questionnaire-list.test.tsx` | Retirement is one nullable `closesAt`, and clearing it is how a premature retirement is undone ([[7-application-boundary#4.3 Publish and retire]]); a `closesAt` change does not move `updatedAt` (#59) | — conventions |
| axe finds no violations in the populated list, the empty state, the create dialog and the closing-date dialog | `_tests/screens/questionnaire-list.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| Status is closed from the instant `closesAt` is reached; the closing-date input round-trips a local date and time through an ISO instant and treats an empty value as missing | `_tests/screens/questionnaire-list/summary-display.test.ts` | The hard cutoff at `closesAt` reads the same in the list as it does on the server ([[2-design-doc#8.1 Questionnaire lifecycle and retirement]]) | — conventions |
| The last-edited label reads just now, minutes, hours, yesterday and days before a calendar date; `fullTimestamp` formats a calendar date and time in the app's one locale; `calendarDateLabel` formats the same calendar date alone, with no time of day | `_tests/lib/dates.test.ts` | One locale policy per app (L21, [[11-structural-refactor]]) | — conventions |

#### PR2

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The dialog shows `text`'s min and max length and multiline, `single_choice`'s options list and Other checkbox, `multiple_choice`'s options with min and max selections, `number`'s required Whole number / Decimal kind with min, max and unit, and `date`'s native earliest and latest with Any / Not in the future / Not in the past, swapping them as the type changes; the type row says in create mode that only the type is fixed, there is no save notice, and in edit mode Save names version N+1 | `_tests/features/question-editor/question-editor-dialog.test.tsx` | The fields follow `QuestionFields`, and saving is publishing (#58, #13, [[10-frontend#5.3 The question editor, and re-pinning]]) | Question versioning |
| Raising a min length, number min, earliest date or min selections above its max drags the max with it; a max typed below its min is clamped on commit; selection bounds are capped by the option count and fall when an option is removed | `_tests/features/question-editor/form-edits.test.ts` | `question/min-length-exceeds-max-length`, `question/min-exceeds-max`, `question/min-selections-exceeds-max-selections` and `question/selections-exceed-options` cannot be entered (#58) | — conventions |
| Letters cannot be typed into a numeric bound; the latest date field's `min` attribute tracks the earliest value | `_tests/features/question-editor/question-editor-dialog.test.tsx` | The input mask and the native date range stay in sync with the form the clamping unit tests exercise directly (#58) | — conventions |
| Added options get distinct generated `opt_` ids with no id input; the freeform mark exists only on the Other row, and the saved body carries `freeform` on `other` alone, as the trailing option — so the editor cannot produce `question/duplicate-option-id`, `question/freeform-not-other` or `question/other-not-freeform` | `_tests/features/question-editor/question-editor-dialog.test.tsx` | `question/duplicate-option-id` and `question/freeform-not-other` cannot be entered (#58, [[5-questionnaire-format#2.3 The `other` option]]) | — conventions |
| A form whose max has not been clamped yet still serializes to a question `validateQuestionRules` accepts; empty constraint fields and other types' fields are left out of the body | `_tests/features/question-editor/form-serialize.test.ts` | A cross-field `400` reaching the author is an editor bug (#58, #20) | — conventions |
| A question's freeform other option becomes the Other toggle and its label and saves back unchanged | `_tests/features/question-editor/form-state.test.ts` | The editor uses the one definition of the other option, and the id `other` is reserved for it, so the toggle round-trips ([[2-design-doc#17. Decisions Log]] #82, #83) | Question versioning |
| `generateOptionId` returns `opt_` and eight random base-36 characters matching the shared slug pattern, and draws again on an id the question already holds | `_tests/features/question-editor/option-ids.test.ts` | A generated id never repeats one in the question, and never reissues one an earlier version used, since the editor sees only the latest version ([[5-questionnaire-format#2.1 Option ids are stable across question versions]], #12) | Response meaning preserved across a republish |
| Each option's id is shown in its row as text, not an input, and describes its label; relabelling leaves every id in place | `_tests/features/question-editor/question-editor-dialog.test.tsx` | Ids are set once and relabelling is safe (#12, #58) | Response meaning preserved across a republish |
| The Yes / No checkbox appears above the prompt only while Single choice is selected, described and reachable by Tab from the type row; checked, the options are exactly `yes` and `no` labelled Yes and No, with no add, remove, reorder or Other control, and relabelled True / False the body keeps the reserved ids | `_tests/features/question-editor/question-editor-dialog.test.tsx` | The template is the only way a question gets the reserved ids, and its labels stay editable (#36, #58, [[5-questionnaire-format#2. Question types]]) | — conventions |
| Unchecking Yes / No restores the options and Other choice the author had before, or one blank generated option; switching away from Single choice unchecks it | `_tests/features/question-editor/question-editor-dialog.test.tsx` | Trying the template costs the author nothing they typed | — conventions |
| A yes / no question is never saved with `other`: the Other control is gone while it is checked, a tick from before is dropped, and the body builder leaves `other` out whatever the form holds | `_tests/features/question-editor/question-editor-dialog.test.tsx`, `_tests/features/question-editor/form-serialize.test.ts` | A yes / no question is two options, so its answers aggregate on `yes` / `no` across questionnaires (#36) | — conventions |
| In edit mode the Yes / No checkbox is disabled: checked, in the limited format, for a saved single choice whose options are exactly `yes` and `no`; unchecked, with the full options editor, for any other single choice | `_tests/features/question-editor/question-editor-dialog.test.tsx` | The template creates a question and is locked afterwards like the type, so an edit cannot drop the reserved ids answers aggregate on (#36, #61) | — conventions |
| Space on an option's handle picks it up and announces it, ArrowDown twice announces each move, Space drops it third of three, and the order changes; Escape during a drag does not close the dialog | `_tests/features/question-editor/question-editor-dialog.test.tsx` | dnd-kit's keyboard sensor and announcements make reordering usable without a pointer ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| Editing a saved number or single choice question disables all five response types, describes the group as "Response type cannot be changed." and offers no Yes / No; a `400 request/invalid` with `question/type-changed` is shown on the type row and the dialog stays open | `_tests/features/question-editor/question-editor-dialog.test.tsx` | The type is fixed by the first save, and the server's refusal is surfaced rather than swallowed (#61) | Question versioning |
| Creating sends `POST /questions` with `{ question }` and hands `latest` to `onSaved`; editing sends `POST /questions/:id/versions` with the whole question, ids and order kept; both close the dialog | `_tests/features/question-editor/question-editor-dialog.test.tsx` | One save writes one version, through the shared route contract (#13, [[7-application-boundary#4.1 Endpoints]]) | Question versioning |
| While a save is in flight Save reads "Saving…" and is disabled with Cancel, and Escape does not close; a `500` keeps the dialog open with the author's changes and an alert; a `400` pointer lands on the option row it names, which gets focus, and a pointer no field owns goes to the alert; an empty prompt or label is named and nothing is sent | `_tests/features/question-editor/question-editor-dialog.test.tsx` | A failed save loses no work, and a `request/invalid` maps to the field its pointer names ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]) | — conventions |
| axe finds no violations in the open dialog creating a text question and editing a choice question with Other, a number question, a yes / no question and a date question | `_tests/features/question-editor/question-editor-dialog.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| The bank's New question button opens the dialog with focus inside, Tab and Shift+Tab stay inside it, and Escape or Cancel closes it and returns focus to the button | `_tests/screens/question-bank.test.tsx` | Radix focus trapping and restoration for the question editor ([[10-frontend#7. Accessibility]], #66) | — accessibility commitment |

#### History

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The versions table lists `GET /versions` in the order the server returns it, newest first, each row with its version, question count and published time | `_tests/screens/version-history.test.tsx` | The history shows the server's `version DESC` order and never re-sorts it ([[7-application-boundary#4.1 Endpoints]], #40) | Questionnaire versioning |
| The Published by column is present and a `null` `publishedBy` renders as "—", never as "unknown" or a placeholder author | `_tests/screens/version-history.test.tsx` | `publishedBy` is rendered as absent until authentication exists, and nothing presents the placeholder as a user (#64, #57) | — conventions |
| The columns are exactly Version, Contents, Published, Published by and the row actions; there is no response-count column | `_tests/screens/version-history.test.tsx` | The definition service has no read access to responses, so the history cannot show response counts ([[7-application-boundary#3.2 Database grants]]) | The definition/execution barrier |
| One paragraph under the table says a published version cannot be edited and sessions stay on their version; no text on the screen mentions response counts or reporting | `_tests/screens/version-history.test.tsx` | Published versions are immutable and sessions are pinned ([[2-design-doc#6. Versioning & Immutability]]); a link to responses waits for their screen (gh#18) | — conventions |
| Each version row's Preview link points at `/admin/questionnaires/:id/versions/:v` for its own version, and following it opens that version's preview | `_tests/screens/version-history.test.tsx` | Each published version is openable from the history ([[10-frontend#5.1 Screens]]) | — conventions |
| With `hasDraft` true a Draft row sits above the versions with the questionnaire's `updatedAt` as its edited time and a link to the draft editor; with `hasDraft` false there is no Draft row | `_tests/screens/version-history.test.tsx` | The open draft is shown only when one exists, from the list read rather than a second draft request (#59, `AdminHistory`) | — conventions |
| A "Back to questionnaires" icon link to `/admin/questionnaires` sits beside the `h1` "Version history", with the questionnaire's name in a muted line under it and no breadcrumb; following it opens the list; the not-found state keeps the same back link | `_tests/screens/version-history.test.tsx` | The same back-arrow header as the preview screen, with the questionnaire still named | — conventions |
| An empty `GET /versions` shows the draft and a "Never published" row and no Preview link | `_tests/screens/version-history.test.tsx` | A never-published questionnaire has no version to open ([[2-design-doc#6. Versioning & Immutability]]) | — conventions |
| A pending `GET /versions` shows a loading status that is gone once the table renders | `_tests/screens/version-history.test.tsx` | — | — conventions |
| `404 resource/not-found` shows "This questionnaire does not exist." with a link back to the list and no table; any other failure shows an alert, not the not-found message, and Try again refetches into the table | `_tests/screens/version-history.test.tsx` | Errors are switched on by problem slug, never by message (#20) | — conventions |
| axe finds no violations with versions and a draft, never published, not found and a failed load | `_tests/screens/version-history.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — conventions |

#### Preview

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `/questionnaires/:id/versions/2` over the intake v2 snapshot renders it through the shared `QuestionnaireForm` with the title and publish date, and a left-arrow link named "Back to version history" to `/admin/questionnaires/:id/versions` immediately before the page title in every state: `itm_01` and `itm_04` only, radios disabled, the text box read-only, submit disabled, and clicking a renderer radio changes nothing | `_tests/screens/version-preview.test.tsx` | Preview renders the snapshot through the renderer in `readonly` mode, and `packages/ui` holds no form state (#27, #68) | Conditional navigation over one and several earlier answers |
| Before any sample answer the panel offers an input for each shown item, numbered by list position, with the gating question on Unanswered, and "Hidden by rules" lists `2. Which condition?` and `3. When were you diagnosed?` | `_tests/screens/version-preview.test.tsx` | Sample answers are plain inputs owned by the admin panel; the hidden list is the shared engine's complement of the visible set (#68) | Conditional navigation over one and several earlier answers |
| Setting the gating sample answer to *yes* reveals `itm_02` and `itm_03` in the renderer and the panel and empties the hidden list; *no* closes the branch again and lists both as hidden by rules | `_tests/screens/version-preview.test.tsx` | An author can walk both branches of the mandatory demo before a respondent does (#68, `AdminPreview`) | Conditional navigation over one and several earlier answers |
| Branch sample answers show in the renderer and survive closing and reopening the branch | `_tests/screens/version-preview.test.tsx` | Hidden answers are kept, as in the respondent app (#29) | Conditional navigation over one and several earlier answers |
| Reset clears every sample answer in the panel and the renderer, closes the branch and relists it as hidden; the control is disabled while nothing is answered | `_tests/screens/version-preview.test.tsx` | The panel is the single owner of preview answers (#68) | — conventions |
| Loading the preview and walking *yes*, *no* and reset sends exactly `GET /api/definition/questionnaires/:id/versions/2` and `GET …/versions`, and nothing to `/api/run` | `_tests/screens/version-preview.test.tsx` | Previewing is an authoring activity: it creates no session and records nothing ([[7-application-boundary#5.2 There is no unpinned definition read]]) | The definition/execution barrier |
| A pending snapshot shows a loading status that gives way to the preview; a `404 resource/not-found` shows "Version 9 is not published" with no retry; any other failure shows an alert with no problem detail and a retry that recovers; a snapshot failing the shared `PublishedDefinition` schema is an error, never a preview | `_tests/screens/version-preview.test.tsx` | An unknown or unpublished version is indistinguishable from a missing questionnaire; the client reads only the formats `getVersion` declares (#56) | Questionnaire versioning: publish, immutability, one draft |
| axe finds no violations with no sample answers, with the branch open, with it closed by *no*, and in the not-published and error states | `_tests/screens/version-preview.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| Each response type gets its own plain sample input: a text box reporting text and `null` when cleared; a radio group with an Unanswered choice; a checkbox per option reporting `optionIds` in option order and `null` when none; a decimal text box described by its unit reporting a decimal string; a native date input reporting the ISO date and `null` when cleared | `_tests/screens/version-preview/sample-answer-input.test.tsx` | Sample answers take the renderer's answer shapes without a second renderer (#42, #68) | — conventions |

#### PR3

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The bank requests `GET /questions?includeArchived=true` once and sorts rows by `latest.createdAt`, newest first, whatever order the server sent; the "Last changed" header carries `aria-sort="descending"`; there is no search box and no filter control | `_tests/screens/question-bank.test.tsx` | One client-side sort axis, "most recently edited", and nothing else in Wave 3 (#53, #59) | — conventions |
| Each row shows its prompt, type label, key only when it has one, latest version and that version's saved time in `<time datetime>` | `_tests/screens/question-bank.test.tsx` | The bank shows each question's latest version ([[10-frontend#5.1 Screens]]) | Question versioning |
| An archived question stays in the list with an Archived marker and no Edit or Archive action, and the count names how many are archived | `_tests/screens/question-bank.test.tsx` | Questions are archived, never deleted, and archived rows are marked rather than hidden behind a filter (#15, #53) | — conventions |
| A pending bank shows a loading status; an empty bank shows the empty state; a failed load shows an alert whose Try again loads it | `_tests/screens/question-bank.test.tsx` | The screen never renders a blank or a stale-looking table for a list it does not have | — conventions |
| The Used by column reads `GET /questions/:id/usage` per row and groups published versions under each questionnaire's name, versions oldest first, each linking to `/admin/questionnaires/:id/versions/:v` and naming the question version it pins, with the full questionnaire name as the title of a name clamped to two lines; a question in no published version says so; a failed usage read shows Retry in that row only, and Retry loads it | `_tests/screens/question-bank.test.tsx` | One question used by several questionnaires is visible on the bank, the reuse the screen exists to show ([[10-frontend#5.1 Screens]]) | Question versioning |
| Archive opens a confirmation and sends nothing until confirmed; confirming posts `POST /questions/:id/archive` and the row turns archived from the response without refetching the bank; Cancel sends nothing | `_tests/screens/question-bank.test.tsx` | Archiving is deliberate and hides a question from new placements while its published versions stay untouched (#15) | — conventions |
| A failed archive keeps the dialog open with an alert and the row active, and a second confirm archives; `404 resource/not-found` says the question no longer exists and refetches the bank | `_tests/screens/question-bank.test.tsx` | Errors are switched on by problem slug, never by message (#20) | — conventions |
| New question opens the editor with focus inside, Tab and Shift+Tab stay inside it, and Escape or Cancel closes it with no write and returns focus to the button | `_tests/screens/question-bank.test.tsx` | Radix focus trapping and restoration for the question editor ([[10-frontend#7. Accessibility]], #66) | — accessibility commitment |
| Saving a new question posts `POST /questions` and the bank is refetched with it; Edit opens the dialog on the row's latest version, Save names version N+1, posts `POST /questions/:id/versions`, and the refetched row shows the new version | `_tests/screens/question-bank.test.tsx` | Create and edit both go through `QuestionEditorDialog`, and a save invalidates every question query (#13, [[10-frontend#5.3 The question editor, and re-pinning]]) | Question versioning |
| axe finds no violations in the populated list with usage loaded, the archive confirmation, the empty state and a failed load | `_tests/screens/question-bank.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| `bankCountLabel` names archived questions only when there are some; `groupUsage` groups usage rows by questionnaire in server order, versions oldest first, with a `null` name for a questionnaire the list does not hold | `_tests/screens/question-bank/bank-display.test.ts` | Seeded questions share one `createdAt`, so ties must fall back to the server's `id DESC` order (#40, #59) | — conventions |
| `sortByLatestEdit` orders by `latest.createdAt` descending and keeps the server order for ties without mutating its input; `isArchived` is true exactly when `archivedAt` is set | `_tests/lib/question.test.ts` | Seeded questions share one `createdAt`, so ties must fall back to the server's `id DESC` order (#40, #59) | — conventions |

#### PR4

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| A default condition for each of the five types with bounds, and every operator switch of it, passes the shared `Condition` schema; switching between one and many options keeps the option, and between a point and `between` keeps the bound | `_tests/screens/draft-editor/conditions.test.ts` | Comparing a date against a number, or a malformed operand, cannot be built in the editor (#9, [[10-frontend#5.4 The predicate editor]]) | Conditional navigation over one *and* several earlier answers |
| A number or date operand defaults to the question's min, else its max; for an unbounded question it is left unset and the condition reads as incomplete, including a `between` with one bound unset | `_tests/screens/draft-editor/conditions.test.ts` | The editor never invents an operand the author did not choose | — conventions |
| Only items above the dependant are offered; a condition's reference reads as earlier, later (equal or higher index), missing, or type-mismatched; a reordered item reports the positions of the later items its conditions use | `_tests/screens/draft-editor/draft-selectors.test.ts` | A forward reference is unrepresentable in the picker and visible when a reorder creates one ([[5-questionnaire-format#5.2 Forward references]]) | — conventions |
| A condition being edited that is still incomplete never commits; a complete condition unchanged from what was saved settles without committing; a complete, changed condition commits | `_tests/screens/draft-editor/predicate-editor/condition-row.test.ts` | `stepCondition` is the one place the row decides whether to write, extracted so the decision is testable without a DOM (PR6) | — conventions |
| `publishWaitsFor` orders its checks: a save in progress, then the checks running, then the checks unavailable, then a positive problem count, singular and plural, else nothing blocks publishing | `_tests/screens/draft-editor/publish-checks.test.ts` | Publish is disabled for exactly one reason at a time, and the reason names what to wait for (PR6) | — conventions |
| `itemDomId` is a stable prefix of the item id; `rulesEditorOf` finds the open rules editor nested under a row and returns null when the row or the editor is absent | `_tests/screens/draft-editor/item-dom.test.ts` | The DOM-id contract used to scroll to and focus a row lives in one place (PR6) | — conventions |
| Generated item ids are `itm_` slugs that never repeat one in the draft; adding pins exactly the version given, required and always shown; moving ignores an out-of-range index; removing an item removes the conditions that reference it and clears an emptied group; re-pinning touches only the named item of the same question | `_tests/screens/draft-editor/draft-changes.test.ts` | Items pin the version the author saw (#31, [[10-frontend#6. Authoring concurrency]]); a reference to a removed item is never left behind | Question versioning |
| The editor shows a loading status, then items in draft order with prompt, type, pinned version and visibility summary, and the publish facts from the list | `_tests/screens/draft-editor.test.tsx` | — | — conventions |
| The editor's heading is the questionnaire's name from the list, never the draft's respondent-facing title; while the list loads the heading is a placeholder named Draft editor, and if the list fails it reads Draft editor | `_tests/screens/draft-editor.test.tsx` | Authors see the name everywhere they manage a questionnaire, as on the list and history; the title is what respondents see, and the preview keeps it | — conventions |
| The Add question dialog lists `GET /questions?includeArchived=false`, marks questions already placed as "In this draft", and adding sends `PUT /draft` with `If-Match` and a new item pinned to the version the picker showed | `_tests/screens/draft-editor.test.tsx` | Add-item carries the displayed `questionVersion`, never "current" (#31); one placement per question (#41) | Question versioning |
| New question inside the Add question dialog, above the list and in its empty state, opens the question editor over it; saving posts `POST /questions`, adds an item pinned to the version just created, and closes both dialogs | `_tests/screens/draft-editor.test.tsx` | A question created from the draft editor is an ordinary bank question placed at the version it was saved as ([[10-frontend#5.1 Screens]], #31) | Question versioning |
| There are no Move up / Move down buttons; the drag handle is a labelled button with the sortable role description and the keyboard instructions as its description; Space picks up and announces the position, ArrowDown announces each move, Escape cancels with an announcement and no `PUT`, and Space drops, announcing the new position and sending the new order | `_tests/screens/draft-editor.test.tsx` | Every draft write goes through `useDraftMutation`; dnd-kit's keyboard sensor makes reordering usable without a pointer ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| Removing a question nothing depends on sends the shorter list; removing one that conditions use asks first, sends nothing until confirmed, then sends the list without it and without those conditions | `_tests/screens/draft-editor.test.tsx` | A removal never leaves a condition pointing at an item that no longer exists | — conventions |
| The first question's Add condition is disabled; a later question's picker offers only earlier questions; single choice offers `is`, `is not`, `is any of`, `is none of` and number the seven comparisons with the unit shown; switching to `between` and typing an upper bound send typed conditions | `_tests/screens/draft-editor/predicate-editor.test.tsx` | Only earlier items and the referenced type's operators can be chosen ([[10-frontend#5.4 The predicate editor]], [[5-questionnaire-format#4.2 Conditions are typed per response type]]) | Conditional navigation over one *and* several earlier answers |
| Add condition references the nearest earlier question; `is any of` keeps the option and its last ticked option cannot be unticked; the all / any selector rewrites the group | `_tests/screens/draft-editor/predicate-editor.test.tsx` | `optionIds` is never empty; one flat `all` / `any` level ([[5-questionnaire-format#4.1 Representation]]) | — conventions |
| A condition that a reorder left pointing below its item is named in the row, and its picker is `aria-invalid`, described by the warning, and shows the reference as its only, disabled option | `_tests/screens/draft-editor/predicate-editor.test.tsx` | A reorder may break a rule, but the editor shows it and the validator rejects it (`predicate/forward-reference`) rather than silently rewriting the author's rule | — conventions |
| Edit opens the question editor on the latest version from the bank list the editor already holds, with no per-question request, and saving re-pins that item to the version the save returned, leaving other items' pins alone | `_tests/screens/draft-editor.test.tsx` | Editing a question from the draft editor re-pins that item ([[10-frontend#5.3 The question editor, and re-pinning]]) | Question versioning |
| An item whose bank question has a newer version shows "Newer version available" and Use vN in a right-aligned status group; other items show neither; Use vN re-pins it to that version | `_tests/screens/draft-editor.test.tsx` | A stale pin is visible in the item list (§5.3) | Question versioning |
| An archived question shows no "Newer version available", no re-pin and a disabled Edit, even when the bank holds a newer version (its "Archived in bank" badge is removed by PR5) | `_tests/screens/draft-editor.test.tsx` | Re-pinning an archived question to a newer version is a new placement, which the server refuses (#75, [[10-frontend#6. Authoring concurrency]]) | — conventions |
| Adding a condition on an unbounded number question sends no `PUT`: the value is empty, `aria-invalid` and described as not saved yet, leaving the field sends nothing, and typing a number then Enter sends the condition; switching an existing condition to that question likewise sends nothing until a value is typed | `_tests/screens/draft-editor/predicate-editor.test.tsx` | A condition value is only ever one the author entered | — conventions |
| On `409 questionnaire/draft-stale` an alert says the last change was undone and the draft reloaded with the other author's version, claiming nothing about earlier changes; the refetched draft replaces the list, and Dismiss clears it | `_tests/screens/draft-editor.test.tsx` | The client half of the draft `ETag` ([[10-frontend#5.2 The draft editor]], `AdminDraftConflict`) | Questionnaire versioning: publish, immutability, one draft |
| On `422 questionnaire/draft-invalid` an alert says the draft cannot be saved and lists the refused item by position and code, the change (a Required toggle) is rolled back, nothing is refetched, and no text mentions someone else; a publish refused as invalid says it was not published, also without blaming another author | `_tests/screens/draft-editor.test.tsx` | A `422` is never reported as another author's edit ([[4-implementation-plan#How Track 6 runs]] PR4 note). The save half is superseded by PR5's save-refused row, and the publish notice's wording by PR5's publish-refused row | — conventions |
| Publish checks from `POST /draft/validate` list each problem's code with a "Question N" link that focuses the item; Publish is disabled and described while problems stand; with none, Publish sends `POST /publish` with the draft's `ETag` and navigates to version history | `_tests/screens/draft-editor.test.tsx` | Validation runs while editing through the code publish runs, so the editor cannot show a green state publish disagrees with (§5.2, #54); PR5's panel host | Questionnaire versioning: publish, immutability, one draft |
| Publish is disabled and described while a change is saving and again while `POST /draft/validate` reruns on the saved draft, so clicking it sends no `POST /publish`; it is enabled once the rerun returns valid | `_tests/screens/draft-editor.test.tsx` | A green result from before a change cannot publish a draft the checks have not seen (regression) | Questionnaire versioning: publish, immutability, one draft |
| While a publish is in flight the Required checkbox, drag handle, Edit, Remove, predicate controls, Add condition and Add question are all disabled, so no change queues behind the publish; a refused publish then re-enables them | `_tests/screens/draft-editor.test.tsx` | A rejection is reported for the write that failed, never relabelled by a later one | — conventions |
| With `GET /draft` `404` and the questionnaire listed without a draft, Open the next draft sends `POST /draft` and loads the editor; unlisted, it reads "does not exist"; any other failure shows an alert whose Try again loads the draft | `_tests/screens/draft-editor/no-open-draft.test.tsx`, `_tests/screens/draft-editor.test.tsx` | One open-draft action shared with the list and history (#67) | — conventions |
| axe finds no violations with a predicate editor open, a conflict notice and publish problems, and in the Add question dialog | `_tests/screens/draft-editor.test.tsx` | Accessibility is designed in, not audited afterwards ([[10-frontend#7. Accessibility]]) | — accessibility commitment |

#### gh#17 core — archiving gates new placements only

**Backend integration — `apps/backend`, Fastify `inject()`, Testcontainers Postgres**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `replaceDraft` saves a retitle, a reorder and an edit to another item's predicate while two placed questions have since been archived, and the saved draft validates clean | `_tests/db/definition/drafts.test.ts` | #75: archiving means "not for new placements"; an existing placement never blocks a save | Question versioning: append-only, items pin a version at add time |
| `replaceDraft` saves the removal of one of two existing archived placements | `_tests/db/definition/drafts.test.ts` | #75: removing an archived placement is an edit to existing placements, not a new one | Question versioning: append-only, items pin a version at add time |
| `replaceDraft` refuses a newly added archived question beside existing archived placements, naming only the new item, and writes nothing | `_tests/db/definition/drafts.test.ts` | #75: a placement is new when its `(questionId, questionVersion)` pair is not in the stored draft, read under the draft row lock | Question versioning: append-only, items pin a version at add time |
| `replaceDraft` refuses an archived question placed again in a later save after its item was removed | `_tests/db/definition/drafts.test.ts` | #75: the prior item set is the stored draft being replaced, not the draft's history | Question versioning: append-only, items pin a version at add time |
| `replaceDraft` refuses re-pinning an existing archived placement to a newer version of its question as `draft/question-archived` | `_tests/db/definition/drafts.test.ts` | #75, pair-based: a different pinned version is a new placement | Question versioning: append-only, items pin a version at add time |
| `createNextDraft` copies an item whose question was archived after publishing; the next draft saves with another item added, validates clean and publishes as version 2 | `_tests/db/definition/drafts.test.ts` | #75: items copied from the published version are existing placements once stored | Questionnaire versioning: publish, immutability, one draft |
| `publishDraft` publishes a draft whose placed question was archived after it was placed | `_tests/db/definition/publish.test.ts` | #75: `draft/question-archived` never reports an existing placement at publish | Questionnaire versioning: publish, immutability, one draft |
| `PUT /draft` is `200` for a reorder and retitle keeping an item whose question was archived after placement, then `422 questionnaire/draft-invalid` naming only a newly added archived item, leaving the `ETag` and audit log unchanged | `_tests/modules/definition/routes/drafts.test.ts` | #75: archived questions are rejected at add time only | Question versioning: append-only, items pin a version at add time |
| `POST /draft/validate` reports a draft whose placed question was archived after it was placed as valid | `_tests/modules/definition/routes/drafts.test.ts` | #75: validate and publish agree, and neither reports an existing placement | Question versioning: append-only, items pin a version at add time |

#### PR5

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| Every `DraftItemCode` has a title and an explanation that never contains a code; exactly the five `predicate/*` codes and `draft/unreachable` open Rules | `_tests/screens/draft-editor/draft-item-messages.test.ts` | The author catalogue is a total `Record<DraftItemCode, …>` that names the problem and the fix, and authors never see a raw code (#60) | — conventions |
| A forward reference names the later questions, or says the condition reads its own question; an unknown option names the referenced question, its prompt and pinned version but never the option id; a type mismatch names both response types by their pill labels; unsatisfiable tells `all` from `any`; unreachable names the questions its rules read; a duplicate points at the first placement; a missing version gives its number; an archived question is described as one that cannot be placed, not as a draft that cannot be saved; each falls back to a plain sentence when the draft has nothing to interpolate | `_tests/screens/draft-editor/draft-item-messages.test.ts` | Interpolation is derived from the loaded draft, and `draft/question-archived` describes the gh#17 fix, where only a new placement is refused (#60, gh#17) | — conventions |
| Problems from `POST /draft/validate` are grouped by question in item order under a "Go to question N, “prompt”" link, with each problem's catalogue title and detail, a count chip and a "Fix N problems in K questions" line; a group whose item is not in the draft has no link; codes live only in `data-code`; Publish is described by "N problems under Publish checks are blocking publishing.", whose Publish checks link focuses the panel heading; axe finds nothing. Replaces PR4's code-and-"Question N" panel row | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | The summary panel with jump-to-item links (#54), readable without codes (#60) | — accessibility commitment |
| A link for a rule problem opens that item's Rules and focuses its fieldset; a link for any other problem focuses the row, named "Question N, prompt", without opening Rules; the jumped row and its group are marked until focus leaves the row | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | Jump-to-item lands on the control that fixes the problem (#54) | — accessibility commitment |
| The panel reads "Checking the draft…" before the first result; a failing validate shows "Not run", the could-not-run copy and Run checks again, with Publish disabled and described as waiting on Publish checks; the retry that returns valid shows Ready and "Publishing creates version 3", announces "no problems. Ready to publish." and passes axe | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | The editor never shows a green state publish disagrees with, and a failed check is never shown as a pass ([[10-frontend#5.2 The draft editor]]) | — reliability |
| While a change saves and its recheck runs, the previous problems stay with `aria-busy` and the old count, the status reads "Checking your latest change…", Publish waits, and the polite live region stays silent; when the recheck lands it announces "1 fixed, 1 left" and the count updates; axe finds nothing while rechecking | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | The live region announces count changes only, not every check ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| An empty draft reads "No problems found, but this draft has no questions yet." with no Ready chip, and Publish stays enabled | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | An empty draft passes the checks and is not described as ready (PR5 design decision) | — conventions |
| A publish refused as `422 questionnaire/draft-invalid` puts its items in the panel before the recheck returns, including when the panel last said the draft was clear, and moves focus to the Publish checks heading. The notice says "Publishing found N problems" and points to Publish checks, lists no items, carries `data-problem` and shows no slug; Show problems focuses the heading again; axe finds nothing. Replaces the item list and code assertions in PR4's publish-refused rows | `_tests/screens/draft-editor/publish-checks-panel.test.tsx`, `_tests/screens/draft-editor.test.tsx` | Focus never falls to `<body>` when Publish is refused, and the panel never trails the server's answer (#54) | — accessibility commitment |
| A `409 questionnaire/draft-stale` on a save refetches `POST /draft/validate` along with the draft | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | Checks are never shown for a draft that someone else's version has replaced ([[10-frontend#5.2 The draft editor]]) | — conventions |
| A many-option condition on an option its pinned version no longer has shows a ticked, disabled "Unknown option" checkbox, and ticking a known option sends only known ids | `_tests/screens/draft-editor/publish-checks-panel.test.tsx` | "Rule uses a removed option" in the panel is visible in Rules too | — conventions |
| Adding a question from the picker that the server refuses as a newly added archived question rolls the list back, and the notice reads "Your last change was not saved", names it "The question being added · Archived in the question bank" (an item still in the draft reads "Question N · …"), shows no slug and no Show problems, and never blames another author. Replaces PR4's save-refused row | `_tests/screens/draft-editor.test.tsx` | Archiving gates new placements only, so a refused save is about the change just undone, which is not another author's edit; authors never see codes (#75, #60) | — conventions |
| An archived question shows no badge, no re-pin and a disabled Edit. Removing it always asks first; the confirmation's info button carries the "cannot be added back" note as its accessible description and shows it as a tooltip on hover and on focus, and Escape hides it; axe finds nothing; confirming sends the list without it. Replaces PR4's archived-badge row | `_tests/screens/draft-editor.test.tsx` | A placed archived question is not an error, so the editor shows no archived state (#75); re-pinning it to a newer version is still refused as a new placement, so no re-pin or Edit is offered; removal cannot be undone, so it warns first | — accessibility commitment |
| `publish()` resolves `published` with the version, or `refused` with the rejection; a publish `422` writes its items into the validation query, and a `409` on a save or a publish invalidates it | `_tests/api/mutations/use-draft-mutation.test.tsx` | The seeded result the panel shows is the server's own answer (#54) | — conventions |

#### PR6

**Frontend component — `apps/admin`, Vitest + RTL in jsdom, `fetch` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| One run through the router against an in-memory definition API that checks every body against the shared route schemas: create a questionnaire and land in its draft, headed with its name rather than its title; add a bank question, then write one with New question from the picker, with focus back on Add question after each; add a condition; publish and land on version history; preview version 1; back to history and Open the next draft; back to the list, which shows the version, the open draft and History; the bank's usage link opens the preview. Questionnaires stays the current navigation item on every nested route, no link inside a screen is marked current, each screen sets its document title, and axe finds nothing on the list, the built draft and the history | `_tests/authoring-flow.test.tsx` | Every screen is reachable from the others without typing a URL, and a draft only reaches history through publish ([[10-frontend#5.1 Screens]]) | Questionnaire versioning: publish, immutability, one draft |
| A questionnaire id that is not a UUID, and a version of `0`, `01` or `latest`, render the not-found screen with a way back and send no request | `_tests/router.test.tsx` | A malformed address is not a load failure to retry | — conventions |
| Each route titles the document `<page> · Questionnaire admin`, and the not-found screen `Page not found · Questionnaire admin` | `_tests/router.test.tsx` | Each screen is identifiable from its title ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| On the draft editor, history, preview and an unknown questionnaire, only the main navigation's Questionnaires link carries `aria-current`; the back links do not | `_tests/shell/app-shell.test.tsx` | A back link is never announced as the current page | — accessibility commitment |
| Version history of a published questionnaire with no open draft offers Open the next draft, which opens it and lands in the draft editor; a `409 questionnaire/draft-exists` lands in the existing draft with no error; any other failure says the draft could not be opened and stays; the button is absent while a draft is open or before anything is published; axe finds nothing | `_tests/screens/version-history.test.tsx` | History leads back into editing, and losing the next-draft race is not an error (#67) | Questionnaire versioning: publish, immutability, one draft |
| `generateUnusedId` prefixes eight lowercase base-36 characters and draws again while the candidate is taken; option ids and item ids both come from it | `_tests/lib/generated-id.test.ts` | Generated option and item ids never collide with ones already in use ([[5-questionnaire-format#2.1 Option ids are stable across question versions]]) | — conventions |
| `InfoTip` is a labelled button that reveals its content on focus, through `aria-describedby`; axe finds nothing closed or open | `_tests/components/info-tip.test.tsx` | #66: built on the shared `tooltip` primitive, replacing hand-rolled hover/focus/Escape state | — accessibility commitment |
| A private screen's own top-level file and its own subdirectory import each other freely; every other file rejects one private screen reaching into another; `lib`, `components`, `api` and `features` reject every import of `screens/`, including a screen's own top-level file, not only a private subdirectory; admin's tests, and paths that merely contain a screen's name, are untouched | `tests/lint-screen-privacy.test.ts` | L3: a screen directory is private to its own screen, and screens are downstream of lib/components/api/features, never the reverse ([[11-structural-refactor]]) | — code quality |
| `useMutation` is rejected everywhere in `apps/admin/src` outside `src/api/mutations`, and allowed inside it; the rest of `@tanstack/react-query`'s surface, and admin's tests, are untouched | `tests/lint-mutations.test.ts` | L4: mutations live in one directory, so their cache invalidation lives in one place ([[11-structural-refactor]]) | — code quality |
| `toLocaleDateString`, `toLocaleString` and `new Intl.DateTimeFormat` are rejected everywhere in admin's `src` except `src/lib/dates.ts`, where they are allowed; manual date-part math and the inherited problem-parsing restriction are unaffected | `tests/lint-dates.test.ts` | L21: dates are formatted only in one module per app, so one locale policy governs every date shown ([[11-structural-refactor]]) | — code quality |

### Wave 3 — Track 7: respondent app

#### PR 1

**Frontend component — `apps/respondent`, Vitest in jsdom with `fetch` and `localStorage` stubbed**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `createSession` posts `{ questionnaireId }` to `/api/run/sessions` and returns the session and definition on `201`; `getSession` gets `/api/run/sessions/:sessionId` with no body and returns an in-progress or a submitted session with its definition on `200`; `submitSession` posts `{ answers }` to `.../submit` and returns the receipt on `200`. Every success body is checked against the route's shared response schema | `_tests/api/execution-client.test.ts` | URLs, bodies and response schemas come from `executionApi` in `@qp/shared`, never rewritten in the app ([[7-application-boundary#6.3 Input validation]]) | Sessions: start, resume, version pinning |
| Each problem a route can return parses into `{ kind: "problem", slug, problem }`: `request/invalid`, `resource/not-found`, `questionnaire/closed` and `internal` for start and resume, plus `session/already-submitted` and `submission/invalid` for submit; switching on the slug reaches `submission/invalid`'s `items` | `_tests/api/execution-client.test.ts` | The closed problem union is exhaustive on the client ([[7-application-boundary#6.1 Error format — RFC 9457 problem details]]), so later PRs switch on it for the closed page (§4.2), `errorsByItemId` (#55) and the `409` receipt (#72) | Submit idempotency: same digest replays, different digest conflicts |
| A thrown `fetch`, and a body that fails to arrive, are `{ kind: "network-error" }` on each route | `_tests/api/execution-client.test.ts` | A network failure is distinguishable from any server answer, so it can get a manual retry (#73) | — reliability |
| A success status with a body failing its schema, a success body under another status, a non-JSON proxy page, a problem whose status disagrees with its slug, a type outside the union, a shared slug the route cannot return, a submission code that is not a submission code, and a problem missing its required extension are each `{ kind: "unexpected-response", status }` | `_tests/api/execution-client.test.ts` | Nothing unvalidated is typed as a contract body | — contract |
| Across a receipt, both submit problems, a proxy error and a network failure, no outcome serializes a planted answer value; the answers argument is `Sensitive` and stringifies redacted | `_tests/api/execution-client.test.ts` | Answer values never reach an error or a log ([[7-application-boundary#5.5 Error bodies must not echo answers]]) | No respondent answer in telemetry |
| Partials are keyed `qp:respondent:<questionnaireId>` and round-trip as `{ formatVersion: 1, sessionId, questionnaireId, answers, updatedAt }`, keeping answers to hidden items and explicit `null`s | `_tests/storage/partials.test.ts` | One key per questionnaire in a versioned envelope (#71); hidden answers are kept, filtered once at submit (#29) | Sessions: start, resume, version pinning |
| Unparseable JSON, a `formatVersion` other than `1`, and foreign shapes — `null`, a string, an array, `{}`, a missing or non-uuid `sessionId`, non-object answers, a non-item-id answer key, a non-timestamp `updatedAt`, an extra field, and an envelope naming another questionnaire — each read as empty without throwing | `_tests/storage/partials.test.ts` | A mismatched or corrupt value is discarded, not a crash (#71) | Sessions: start, resume, version pinning |
| A stored answer that is not a valid client answer, such as a half-typed number `72.`, is dropped while the rest of the envelope is kept | `_tests/storage/partials.test.ts` | The renderer reports number text as typed, so one unfinished field must not discard every answer on reload (#71) | Sessions: start, resume, version pinning |
| Clearing after submit leaves `{ sessionId, questionnaireId }` with empty answers and no answer value in storage | `_tests/storage/partials.test.ts` | A successful submit clears the answers and keeps the session, so the link reopens to the receipt (#70) | Sessions: start, resume, version pinning |
| Removing deletes the key and leaves other questionnaires' partials untouched | `_tests/storage/partials.test.ts` | A stale session id (`404` on resume) is cleared before starting again ([[10-frontend#4.2 Entry and resume]]) | Sessions: start, resume, version pinning |
| With every `Storage` operation throwing, with a quota error on write (the previous envelope still reads back), and with `localStorage` absent, no operation throws and reads are empty | `_tests/storage/partials.test.ts` | Private mode and full storage degrade to no persistence, not a white screen ([[3-scaling#7. Known tradeoffs of browser-held partial answers]]) | — reliability |

#### PR 2

**Frontend component — `apps/respondent`, Vitest in jsdom with `fetch` stubbed by an in-test execution server and real `localStorage`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `transition` over every state × every event: `entering → resuming \| starting`, `resuming → ready \| done \| starting \| closed \| failed`, `starting → ready \| closed \| notFound \| failed`, `ready → submitting`, `submitting → done \| closed \| failed`, and a submit-stage `failed` back to `submitting`; every other pair leaves the state unchanged, and `done`, `closed`, `notFound` and an entry-stage `failed` accept nothing | `_tests/session/respondent-state.test.ts` | One URL and a state machine behind it, with closed and not-found terminal ([[10-frontend#4.2 Entry and resume]]); a submit failure keeps the form's context for PRs 3 and 4 to extend | Sessions: start, resume, version pinning |
| A resumed envelope with at least one answer shows the restore strip; one holding only `null`s does not | `_tests/session/respondent-state.test.ts` | The strip claims restored answers only when there are some | Sessions: start, resume, version pinning |
| `/q/:questionnaireId` yields the id, with or without a trailing slash; `/`, `/q/`, a non-uuid, an extra segment, another prefix and a malformed escape yield nothing | `_tests/entry/questionnaire-path.test.ts` | One entry URL parsed without a router (§4.2, #32) | — contract |
| Entering with nothing stored posts one `POST /sessions`, even under `StrictMode`, and writes `{ sessionId, questionnaireId, answers: {} }` before any answer, with no restore strip | `_tests/app.test.tsx` | Start, then persist, so a reload resumes instead of orphaning the session (§4.2) | Sessions: start, resume, version pinning |
| Answering yes reveals *Which condition?* and *When were you diagnosed?*; storage holds every answer after each change; submit shows the receipt with the session id, title and version and `submittedAt` verbatim in `<time datetime>`; storage then holds `{ sessionId, questionnaireId }` with empty answers and no answer text | `_tests/app.test.tsx` | The mandatory branching demo end to end in the app; a successful submit clears answers and keeps the ids (#70) | Conditional navigation over one *and* several earlier answers |
| Switching back to no removes both branch questions while storage keeps their answers, and the submit body carries only `itm_01` and `itm_04` | `_tests/screens/questionnaire-screen.test.tsx` | Hidden answers are kept and filtered once at submit with `visibleAnswers` (#29, [[10-frontend#4.4 Submit]]) | Server as authority: an answer to an unreachable item is rejected |
| While submit is in flight the button is disabled and reads *Submitting…*; a second click and a direct form submit send nothing more, and exactly one request is made | `_tests/screens/questionnaire-screen.test.tsx` | Submit is disabled in flight so a second click cannot race the first (#73) | Submit idempotency: same digest replays, different digest conflicts |
| A diagnosis date in the future and unanswered required items show their catalogue messages with `aria-invalid` and send no request; correcting the date clears its message | `_tests/screens/questionnaire-screen.test.tsx` | The client pre-check with the shared validator: an obviously invalid answer never reaches the network (§4.4) | Response validation: required, per-type value rules |
| `precheckAnswers` accepts local tomorrow-in-UTC in `Pacific/Auckland` and rejects it in `UTC` at the same instant; reports every unanswered visible required item; ignores answers held for hidden or unknown items | `_tests/answers/precheck.test.ts` | Relative dates resolve against the browser's local date (#38, [[5-questionnaire-format#2.4 Relative date constraints resolve against two different clocks]]) | Relative date constraints across a timezone boundary |
| Resuming a stored in-progress session calls `GET /sessions/:id` and never `POST`, restores the answers (including a hidden date that reappears on yes) and shows the restore strip with no *Start over* button; a stored session with no answers resumes without the strip | `_tests/screens/questionnaire-screen.test.tsx` | Storage-first resume (§4.2); no start over in Wave 3 (#74) | Sessions: start, resume, version pinning |
| Resuming a submitted session shows its receipt without `POST`, and clears any answers still stored | `_tests/screens/receipt-screen.test.tsx` | Reopening the link after submit shows the receipt, not a blank form (#70) | Sessions: start, resume, version pinning |
| A stored session answering `404` is removed, a new session is started and stored, and its old answers are not restored | `_tests/session/respondent-session.test.ts` | A stale session id is cleared before starting again (§4.2) | Sessions: start, resume, version pinning |
| `409 questionnaire/closed` on start, on resume and on submit shows the closed screen, and on submit leaves the answers stored; `404` on start shows the not-found screen, as does an unparseable path without any request | `_tests/app.test.tsx`, `_tests/screens/terminal-screens.test.tsx` | Closed and not-found are terminal pages, not raw errors ([[2-design-doc#8.1 Questionnaire lifecycle and retirement]]) | Retirement: start and submit rejected past `closes_at` |
| A network failure, an unexpected response and `internal` on start, and a network failure on resume, show the generic error screen with stored answers untouched; on submit, a network failure, an unexpected response, `409 session/already-submitted` and `422 submission/invalid` keep the form, its values and its stored answers, re-enable submit, and show an alert that does not echo a planted answer | `_tests/screens/terminal-screens.test.tsx`, `_tests/screens/questionnaire-screen.test.tsx`, `_tests/screens/receipt-screen.test.tsx` | Answers survive until a genuine `2xx` (#73); one generic failure state for PRs 3 and 4 to extend; no answer in an error surface ([[7-application-boundary#5.5 Error bodies must not echo answers]]) | — reliability |
| axe finds nothing on the loading screen, the form with the branch revealed, the resumed form with the strip, the form with pre-check errors and after a failed submit, the receipt, and the closed, not-found and generic error screens | `_tests/app.test.tsx`, `_tests/screens/questionnaire-screen.test.tsx`, `_tests/screens/receipt-screen.test.tsx`, `_tests/screens/terminal-screens.test.tsx` | Accessibility designed in on every respondent screen ([[10-frontend#7. Accessibility]]) | — accessibility commitment |

#### PR 3

**Frontend component — `apps/respondent`, Vitest in jsdom with `fetch` stubbed by an in-test execution server and real `localStorage`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `transition` adds `submitting → ready` with a rejection on `submissionRejected`, `submitting → fetchingRecordedReceipt` on `alreadySubmitted`, `fetchingRecordedReceipt → done` (already submitted) or an entry-stage `failed`, and `answerChanged` dropping that item's server errors from a rejected `ready`; an answer change on an item without a server error, or with no rejection, leaves the state identical | `_tests/session/respondent-state.test.ts` | A `422` returns to the form rather than a dead end (§4.4); a `409` leads to the recorded receipt (#72) | Response validation: required, per-type value rules |
| `submissionRejectionOf` places `errorsByItemId`'s groups on items the answers show, and flags unplaced errors when a code was dropped (`answer/not-visible`, `answer/unknown-item`), when an error names a hidden item, or when nothing could be placed, including an empty `items`; `withoutItemError` removes one item and keeps the flag | `_tests/answers/submission-rejection.test.ts` | One grouping for both apps (#55, #62), and the codes it drops still produce a message rather than silence | Server as authority: an answer to an unreachable item is rejected |
| `errorSummaryEntries` lists shown items in form order with the catalogue message for each item's first code and leaves out hidden items; the title counts entries, and reads *Your answers could not be submitted* with none | `_tests/screens/error-summary.test.ts` | One summary drawn from the same code catalogue as the inline errors ([[10-frontend#3. `packages/ui` — primitives and the renderer]]) | Response validation: required, per-type value rules |
| The `ErrorSummary` region titles itself by entry count and lists each entry as a jump button naming its prompt and message, in form order; clicking one calls `onJump` with its item id; with no entries and the unplaced flag set it shows only the generic note; axe finds nothing on either shape | `_tests/screens/error-summary.test.tsx` | The summary component itself is accessible and wired to its callback in isolation from the form it sits in | — accessibility commitment |
| A `422` naming two shown items returns to the form with values and stored answers intact and submit enabled: a region titled *2 answers need attention* lists each prompt and message in form order, both controls carry `aria-invalid` and their message, focus lands on the first invalid item, and a polite status announces that the answers were not submitted | `_tests/screens/questionnaire-screen.test.tsx` | Focus moves to the first invalid item on a rejected submit and the summary is reachable ([[10-frontend#7. Accessibility]]); answers survive until a `2xx` (#73) | Response validation: required, per-type value rules |
| Each summary entry is a button that moves focus to its item, for a text input and for a radio group | `_tests/screens/questionnaire-screen.test.tsx` | Jump-to-item links from the summary ([[4-implementation-plan#How Track 7 runs]] PR 3) | — accessibility commitment |
| Changing an answer clears that item's server error and leaves the others; a corrected resubmit reaches the receipt | `_tests/screens/questionnaire-screen.test.tsx` | A stale server error never outlives the answer it named | Response validation: required, per-type value rules |
| A `422` carrying only dropped codes, an error on an item the form is not showing, or no items at all shows *Your answers could not be submitted* with a generic description, focuses the summary and echoes no planted answer; a `422` mixing a placed and a dropped code lists the item and keeps the generic line | `_tests/screens/questionnaire-screen.test.tsx` | Codes with no rendered item still tell the respondent something (#55); no answer in an error surface ([[7-application-boundary#5.5 Error bodies must not echo answers]]) | Server as authority: an answer to an unreachable item is rejected |
| The client pre-check's missing answers use the same summary, focus the first invalid item, announce, and jump from the summary, with no request sent | `_tests/screens/questionnaire-screen.test.tsx` | One error presentation for client and server errors ([[10-frontend#3. `packages/ui` — primitives and the renderer]]) | Response validation: required, per-type value rules |
| `409 session/already-submitted` keeps submit disabled while `GET /sessions/:id` is in flight, then shows *This form was already submitted* with a note, the recorded session id and `submittedAt`, and leaves `{ sessionId, questionnaireId }` with empty answers in storage after one submit request | `_tests/screens/receipt-screen.test.tsx` | The `409` shows the receipt actually recorded, never a dead end (#72); answers cleared, ids kept (#70) | Submit idempotency: same digest replays, different digest conflicts |
| When that session fetch meets a network failure, an in-progress session or an `internal` problem, the generic error screen shows and the stored answers are kept | `_tests/screens/receipt-screen.test.tsx` | The failure stays representable for PR 4's retry (#73) | — reliability |
| axe finds nothing on the error summary after a `422` with placed and generic errors, and on the already-submitted receipt | `_tests/screens/questionnaire-screen.test.tsx`, `_tests/screens/receipt-screen.test.tsx` | Accessibility designed in on every respondent screen ([[10-frontend#7. Accessibility]]) | — accessibility commitment |

#### PR 4

**Frontend component — `apps/respondent`, Vitest in jsdom with `fetch` stubbed by an in-test execution server and real `localStorage`**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| `transition` over every state × every event, with the in-flight states (`starting`, `resuming`, `submitting`, `fetchingRecordedReceipt`) both fresh and retrying, and `failed` at each step, retryable and not. `requestFailed` records the step and counts the attempt. `retryRequested` returns a retryable `failed` to its step, carrying the failure. `submitRequested` does the same for a failed submit. A stale resume carries the failure into `starting`. A rejection forgets earlier failures. A non-retryable entry or receipt-fetch failure accepts nothing | `_tests/session/respondent-state.test.ts` | A failure on any of the three calls gets a manual retry of the same step, never an automatic one (#73) | — reliability |
| `viewOf` maps every `RespondentState` to its screen: loading while entering, starting or resuming with no prior failure; the load-failed, saved-answers-resume-failed and recorded-receipt-failed screens with the right attempt and in-flight flags; the questionnaire with its submit failure and rejection; and the receipt, closed and not-found screens | `_tests/session/respondent-view.test.ts` | One pure selector decides which failure screen shows, so the state machine and the screens never re-derive that decision separately (§4.2, §4.4) | — reliability |
| `isRetryable` is true for a network error, an unexpected response (a `503`, an unparseable `200`) and `internal`; false for `request/invalid`, `resource/not-found`, `questionnaire/closed`, `session/already-submitted` and `submission/invalid`. `hasAnyAnswer` is false for an empty or all-`null` answer map and true once one answer is not `null` | `_tests/session/failure.test.ts` | A transport failure or a server fault is worth repeating; a known problem keeps its dedicated handling (#72, #73); the two predicates behind the failure screen and the state machine's retry guards have one shared home | — reliability |
| Start after a network failure, a `503` page or `internal` shows *The questionnaire could not be loaded* in an alert, with **Try again** described by the message and not focused on first load. Nothing is stored. While the retry is in flight, Try again is `aria-disabled` and reads *Trying again…*, and further clicks and Enter send nothing. The retry then opens the form, sends exactly two `POST`s and stores the new session | `_tests/screens/terminal-screens.test.tsx` | Start failure shows a manual retry ([[10-frontend#4.2 Entry and resume]] step 5); a retry cannot double-fire (#73) | Sessions: start, resume, version pinning |
| A start retry that fails again keeps the screen, remounts the alert so it is announced again, and moves focus to an enabled Try again | `_tests/screens/terminal-screens.test.tsx` | Failure is announced and Retry is reachable without hunting for it ([[10-frontend#7. Accessibility]]) | — accessibility commitment |
| Resume after each of those failures shows the same screen, noting that the started answers are still saved, with storage untouched and no `POST`. The retry makes a second `GET` and restores the answers with the strip. A stored session with no answers makes no saved-answers claim. A retried resume that meets `404` removes the stale id, and a start failing after it also retries, reaching a new stored session | `_tests/screens/terminal-screens.test.tsx` | Resume failure shows a manual retry and never orphans or creates a session (§4.2); stored answers survive until a `2xx` (#73) | Sessions: start, resume, version pinning |
| Submit after each of those failures keeps the form, its values and its stored answers. An alert says the answers were not submitted and are still saved, and focus moves to Try again. While the retry is in flight, Submit is disabled, and Try again is `aria-disabled` and keeps focus; further clicks on either, and a direct form submit, send nothing. On `200` the retried body equals the first (visible answers only), the receipt shows, and storage holds the ids with no answer text | `_tests/screens/questionnaire-screen.test.tsx` | Submit is disabled in flight and a retry is safe because submit is idempotent on the digest (#19, #73); answers cleared only on a genuine `2xx` (#70) | Submit idempotency: same digest replays, different digest conflicts |
| A submit retry that fails again (a `502` page) remounts the alert so it is announced again, returns focus to Try again, echoes no planted answer and keeps the answers stored. A retry after an edit sends the edited visible answers | `_tests/screens/questionnaire-screen.test.tsx` | A retry repeats what the respondent sees, never a stale copy; no answer in an error surface ([[7-application-boundary#5.5 Error bodies must not echo answers]]) | — reliability |
| A submit retry meeting `409 session/already-submitted` shows *This form was already submitted* with its note and the recorded `submittedAt`, and clears the answers but keeps the ids. A retry meeting `422` shows the error summary with no failure alert and focuses the invalid item | `_tests/screens/questionnaire-screen.test.tsx` | A lost response followed by a changed resubmit lands on the recorded receipt (#72); a retry reaches PR 3's rejection handling | Submit idempotency: same digest replays, different digest conflicts |
| `transition`: `newSessionRequested` takes a retryable resume failure to `startingNewSession`, carrying the stored envelope and the failure. That state reaches `ready` with the stored answers restored, or a start-stage `failed` that keeps those answers for its own retry. `retryRequested` on that failure returns to `starting` with the answers still carried. Every other state ignores the event, including a non-retryable resume failure and one whose stored envelope holds no answers | `_tests/session/respondent-state.test.ts` | A failed resume can be abandoned for a new session without losing the answers typed so far; this is not a general start over (gh#35, #74) | Sessions: start, resume, version pinning |
| A failed resume whose stored envelope holds answers offers **Try again** then **Start a new session**, described as keeping the answers so far. A failed start offers no such button. While it runs, both buttons are `aria-disabled`, it reads *Starting a new session…* and keeps focus, further clicks on either send nothing, and storage still holds the old id and answers. On `201` the form shows the carried answers with the restore strip, storage holds the new session id with the same answers, and the old session was fetched once. Start a new session is blocked while Try again is in flight | `_tests/screens/terminal-screens.test.tsx` | Nothing in storage changes before a genuine `2xx` (#73); the new session replaces the stored id in the one envelope per questionnaire (#71) | Sessions: start, resume, version pinning |
| A failed resume whose stored session id has no stored answers shows only Try again, with no mention of saved answers or a new session | `_tests/screens/terminal-screens.test.tsx`, `_tests/session/respondent-state.test.ts` | An envelope with an id and no answers may be a submitted session (#70), so the respondent is never offered a blank form that could submit twice | Submit idempotency: same digest replays, different digest conflicts |
| When starting the new session meets a `503` page, the start-failure screen replaces the resume screen with Try again focused and no Start a new session button; storage still holds the old id and answers. Its retry reaches the form with the carried answers under the new id after two `POST`s | `_tests/screens/terminal-screens.test.tsx` | A failed new start gets the same manual retry as any start (§4.2 step 5, #73) | — reliability |
| A submit `404` shows the alert without Try again or the connection explanation, leaves Submit enabled and keeps the answers stored | `_tests/screens/questionnaire-screen.test.tsx` | Only failures that repeating can fix offer a retry (#73) | — reliability |
| After `409`, a recorded-receipt fetch meeting a network failure, an in-progress session or `internal` shows *This form was already submitted* with an alert that the submission on record could not be loaded, and focuses Try again. Answers stay stored. A retry that cannot double-fire shows the receipt with its note, clears the answers and keeps the ids, after two `GET`s and one submit. A fetch meeting `404` shows the generic error screen with no button | `_tests/screens/receipt-screen.test.tsx` | The recorded-receipt fetch gets the same manual retry (#72, #73); answers cleared only when the receipt arrives (#70) | — reliability |
| axe finds nothing on the load-failure screen, the resume-failure screen and its in-flight retry, the submit failure alert with its retry in flight, the recorded-receipt failure screen, and the generic error screen for a `request/invalid` start | `_tests/screens/terminal-screens.test.tsx`, `_tests/screens/questionnaire-screen.test.tsx`, `_tests/screens/receipt-screen.test.tsx` | Accessibility designed in on every respondent screen ([[10-frontend#7. Accessibility]]) | — accessibility commitment |

### Wave 3 — Track 9: end-to-end harness

**End-to-end — Playwright against `docker-compose.yml` brought up by Testcontainers, each run on its own compose project and ephemeral host ports**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| The seeded demo loads at `/q/<demo id>` with `itm_02` and `itm_03` absent, the respondent envelope appears under `qp:respondent:<id>`, and its session reads back from `execution.session` as `in_progress` on version 1, with no console or page error | `e2e/specs/smoke.spec.ts` | The stack under test is the production compose file behind nginx, and the harness reaches it through the browser, storage and the mapped database port (§2.4) | Sessions: start, resume, version pinning |
| `/admin/` redirects to the questionnaire list and lists the seeded demo, with no console or page error | `e2e/specs/smoke.spec.ts` | Both apps are served from one origin with the admin base path intact (§2.4) | — harness |
| A demo-shaped questionnaire created and published through the definition API renders for a respondent, reveals its branch on Yes, and a submission with an `other` answer persists four `response` rows carrying questionnaire version 1, each question version, the option ids and the other text | `e2e/specs/smoke.spec.ts` | Specs isolate themselves by authoring through the definition API instead of sharing the seeded demo (§2.4) | Conditional navigation over one *and* several earlier answers |

**End-to-end — Tier 1, the graded spine**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| A questionnaire authored entirely in the admin UI (four new bank questions, two `visibleWhen` rules set in the rules editor, publish) renders for a fresh respondent with `itm_02`/`itm_03` absent from the DOM; answering Yes reveals both in definition order with no navigation; submit returns a receipt, and four `response` rows carry the session's questionnaire version and each item's own question version | `e2e/specs/tier-1/e01-author-publish-yes-path.spec.ts` | The mandatory demo holds through the running system, authoring included (§2.4 spec 1) | Conditional navigation over one *and* several earlier answers |
| Answering No keeps the branch out of the accessibility tree and out of the DOM for the whole session (checked with a mutation observer); focus moves from No to the next common question; exactly two `response` rows are written | `e2e/specs/tier-1/e02-no-path-skips-branch.spec.ts` | Hiding is removal, not CSS, and the no path lands on the next common question (§2.4 spec 1) | Conditional navigation over one *and* several earlier answers |
| Closing an answered branch removes its items from the DOM but keeps their answers in `qp:respondent:<id>`; reopening restores the choice, the other text and the date; the submit body and the `response` rows contain only the visible items | `e2e/specs/tier-1/e03-answers-prune-when-branch-closes.spec.ts` | Hidden answers are kept in the browser and filtered exactly once, at submit ([[10-frontend]], #71) | Server as authority: an answer to an unreachable item is rejected |
| A reload and a new context with the same `storageState` each resume the stored session from one `GET /sessions/:id` carrying the pinned definition, with no `POST /sessions` and no definition request; answers are restored, one session row exists for the questionnaire, and one submit writes one response set | `e2e/specs/tier-1/e04-resume-from-storage.spec.ts` | Storage-first resume: nothing is written between start and submit, and resume never forks a session (§2.4 spec 2) | Sessions: start, resume, version pinning |
| Session A submits `opt_hyperten` on v1; the admin UI relabels it and publishes v2; session B sees the new label and submits; A's rows are unchanged field for field, both answers carry `opt_hyperten` and the same question id with question versions 1 and 2, version history lists both versions, and the v1 preview still renders "Hypertension" | `e2e/specs/tier-1/e05-v2-relabel-preserves-meaning.spec.ts` | A published version is immutable, and a relabel keeps collected answers joined on option id and question id (§2.4 spec 3) | Response meaning preserved across a republish |
| A form open while v2 is published keeps v1's labels and version footer; submitting without a reload is accepted, and the session and all its `response` rows point at v1's questionnaire version | `e2e/specs/tier-1/e06-republish-keeps-in-flight-session.spec.ts` | In-flight sessions stay pinned to the version they started on | Path re-evaluation uses the *pinned* definition, not the latest published |

**End-to-end — Tier 2, respondent negatives**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| Submitting the seeded demo with required items unanswered sends no request; the error summary lists each item with a jump button that moves focus; focus lands on the first invalid control; `aria-invalid` and `aria-describedby` name the message | `e2e/specs/tier-2/e07-required-blocks-submit.spec.ts` | The client pre-check runs the shared validator before any request, and errors are reachable by keyboard and screen reader ([[10-frontend]]) | Response validation: required, per-type value rules |
| A submit body rewritten in flight to answer the hidden `itm_02` gets `422 submission/invalid` naming `itm_02` with `answer/not-visible`; the body never contains the injected text, the summary renders, and the session has zero `response` rows | `e2e/specs/tier-2/e08-server-rejects-tampered-client.spec.ts` | The server re-evaluates visibility on the pinned definition and persists all-or-nothing; error bodies never echo answers (#20) | Server as authority: an answer to an unreachable item is rejected |
| A double-click sends one submit; after a reload the receipt screen shows the original receipt; replaying the identical answers returns `200` with the same receipt, and the rows and `submitted_at` are unchanged | `e2e/specs/tier-2/e09-identical-resubmit-replays-receipt.spec.ts` | Same digest replays the original receipt; a retry is never a second response set (#37) | Submit idempotency: same digest replays, different digest conflicts |
| A second tab submits the session; the first tab changes an answer and submits, gets `409 session/already-submitted`, fetches `GET /sessions/:id` once and shows the recorded receipt with the already-submitted note, not an error screen | `e2e/specs/tier-2/e10-conflicting-resubmit-shows-recorded-receipt.spec.ts` | The conflict path stays recoverable and shows what was recorded (#72) | Submit idempotency: same digest replays, different digest conflicts |
| Landing on a questionnaire whose `closes_at` has passed, with empty storage, gets `409 questionnaire/closed`, renders the closed screen and creates no `session` row | `e2e/specs/tier-2/e11-closed-questionnaire-refuses-start.spec.ts` | Retirement is a hard cutoff at session start | Retirement: start and submit rejected past `closes_at` |
| A session filled before `closes_at` passes gets `409 questionnaire/closed` on submit, renders the closed screen, writes no rows, and keeps its stored answers | `e2e/specs/tier-2/e12-closed-mid-session-refuses-submit.spec.ts` | The cutoff also holds on the write path, and a refused submit never destroys local answers (#73) | Retirement: start and submit rejected past `closes_at` |
| `/q/<unknown uuid>` renders the not-found screen from `404 resource/not-found` with no page error or unexpected console error; a malformed id renders it without calling the API | `e2e/specs/tier-2/e13-unknown-questionnaire-not-found.spec.ts` | Not-found is a designed state, not an unhandled failure | Sessions: start, resume, version pinning |
| With the browser in `Pacific/Auckland` and `Pacific/Honolulu` at an instant where local and UTC dates differ, browser-local tomorrow is refused with `date/in-future` before any request and browser-local today is accepted and stored | `e2e/specs/tier-2/e14-date-constraint-across-timezones.spec.ts` | The client checks against browser-local today; the server checks against UTC today ± 1 day (#38) | Relative date constraints across a timezone boundary |
| An aborted submit keeps the stored answers and shows a focused Try again button; while the retry is in flight Submit is disabled and Try again is `aria-disabled`; the retry writes exactly one response set | `e2e/specs/tier-2/e15-network-failure-then-retry.spec.ts` | Answers survive until a real 2xx, and a retry cannot double-submit (#73) | Submit idempotency: same digest replays, different digest conflicts |
| A stored envelope with a bumped `formatVersion` pointing at another session, or unparseable text, is discarded: a new session starts with a blank form, and the submit body carries none of the bad envelope's answers | `e2e/specs/tier-2/e16-corrupt-envelope-discarded.spec.ts` | The envelope shape check degrades to a clean start rather than crashing or resuming the wrong session (#71) | Sessions: start, resume, version pinning |
| Choosing Other requires visible text before submit; the typed text round-trips to `other_text`; switching to a listed option drops the text from storage and the body, and the submit is accepted with `other_text` null | `e2e/specs/tier-2/e17-other-freeform-round-trip.spec.ts` | The two-part choice answer never sends `otherText` without the freeform option | Response validation: required, per-type value rules |
| A questionnaire with multiline text, single choice, multiple choice with min/max selections, an integer with bounds and unit, and a date with absolute bounds renders each control, fires each client rule before any request, and stores `72.0` as `number_value 72` with `number_unit kg` filled server-side | `e2e/specs/tier-2/e18-all-response-types.spec.ts` | The shared renderer and validator compose across all five types; the number travels as a decimal string and the unit comes from the pinned version (#42) | Response validation: required, per-type value rules |

**End-to-end — Tier 3, admin authoring**

| Case | File | Invariant defended | §3 row |
| --- | --- | --- | --- |
| A draft with a forward reference and an unsatisfiable rule lists both in the Publish checks panel with jump-to-item buttons that open and focus that item's Rules, and holds Publish back; a publish the server refuses answers `422 questionnaire/draft-invalid`, shows the not-published notice, focuses the panel, and leaves no published version | `e2e/specs/tier-3/e19-publish-validation-surface.spec.ts` | Publish-time validation surfaces as a summary panel with jump-to-item controls, and publish refuses on the same result the dry run shows (#54, #60) | Publish-time rule validation: forward refs, unsatisfiable conditions |
| Admin B reorders on an ETag made stale by admin A's save, gets `409 questionnaire/draft-stale`, sees the optimistic order roll back and a someone-else-changed-it notice, and the draft reloads to A's state | `e2e/specs/tier-3/e20-two-admins-one-draft.spec.ts` | Draft writes carry `If-Match` over `draft_revision`, and a conflicting optimistic write is undone, never merged (#43, [[10-frontend#5.2 The draft editor]]) | Questionnaire versioning: publish, immutability, one draft |
| A drag handle reached with Tab reorders a question with Space and the arrow keys, each step is announced in the live region, and the order persists through `If-Match` across a reload | `e2e/specs/tier-3/e21-keyboard-reorder.spec.ts` | Reordering is operable and announced without a pointer, which is why dnd-kit was chosen ([[10-frontend#5.2 The draft editor]]) | — accessibility |
| Editing a question from questionnaire A's draft editor writes version 2 and re-pins only A's item; B's draft and published version keep version 1, and the bank's Used by column shows both question versions in use | `e2e/specs/tier-3/e22-edit-question-repins-item.spec.ts` | Editing from the draft editor re-pins that one item and nothing else ([[10-frontend#5.3 The question editor, and re-pinning]]) | Question versioning: append-only, items pin a version at add time |
| An archived question is absent from the Add-question picker, a direct `PUT /draft` adding it is `422 questionnaire/draft-invalid` with `draft/question-archived`, and the published version that already placed it still renders for respondents | `e2e/specs/tier-3/e23-archived-question-not-placeable.spec.ts` | Archiving gates new placements only (#75) | Question versioning: append-only, items pin a version at add time |
| Open draft from a list that missed another tab's draft gets `409 questionnaire/draft-exists` and lands in that draft's editor with no error; a list already showing the draft goes there without a POST; one draft row exists either way | `e2e/specs/tier-3/e24-one-draft-at-a-time.spec.ts` | One open draft per questionnaire, and losing the open race is not an error (#67) | Questionnaire versioning: publish, immutability, one draft |
| The version preview offers no editing controls and renders the snapshot read-only; the next draft copies every item with its pins even when the bank has a newer version; no HTTP route writes a published version (`PUT` to the version URL is 404, a replayed draft ETag is 404 or `409 draft-stale`), and a direct SQL update is refused with `QP001` | `e2e/specs/tier-3/e25-published-version-immutable.spec.ts` | Published versions are immutable at the UI, the API and the database trigger ([[7-application-boundary]] §4.3) | Questionnaire versioning: publish, immutability, one draft |

## 8. Alternatives considered

### 8.1 Jest for the frontend

The tool already familiar here, and the RTL documentation's default. Rejected because it would mean a second transform pipeline and module-resolution config alongside Vite's, for a test API that is nearly identical to Vitest's. Vitest reuses `vite.config.ts`, so what the tests run through is what the app is built with — and the ESM/TS configuration that makes Jest tedious in a Vite repo simply does not arise.

### 8.2 Mocked repositories, or `pg-mem`, instead of a real Postgres

The fast option, and it would let the integration suite run with no Docker at all. Rejected because the invariants this project is graded on live in the database — the immutability trigger, the one-draft partial index, the append-only audit role, the missing `response` grant. A suite built on a mock passes with every one of those constraints dropped, so it would provide confidence precisely where there is none. `pg-mem` fails the same test one level down: triggers, roles and JSONB operator support are what it approximates.

### 8.3 A shared Postgres from docker-compose instead of Testcontainers

Genuinely attractive: the container already exists, startup cost is zero, and the inner loop is faster. Rejected as the *default* because "run the tests" becomes two commands with an ordering dependency, and CI needs a separately configured service that can drift from the compose definition. Kept as the `TEST_DATABASE_URL` path (§4), which is the same benefit without being the thing a newcomer or a pipeline has to know about.

### 8.4 `supertest`, or a listening server, instead of `inject()`

Rejected as strictly more machinery: a real port, a lifecycle to manage, and flake when ports collide under parallel workers. `inject()` exercises the identical Fastify lifecycle. A listening server is warranted only for what it uniquely tests — keep-alive, streaming, timeouts — none of which is in scope.

### 8.5 Cypress instead of Playwright

The better-known tool with a friendlier interactive runner. Rejected for the CI story this project actually needs: Playwright's parallelism, `webServer`/`--wait` integration, and above all the trace viewer, which makes a failure on a runner debuggable without reproducing it locally. Cypress's in-browser execution model also makes the multi-tab and multi-session interactions in specs 2 and 3 awkward.

### 8.6 A coverage percentage gate

Rejected. The graded behavior is concentrated in a few intricate modules, and a repo-wide threshold is most easily met by testing the surface area that does not matter, while a per-module threshold is the §3 table with worse resolution. Coverage is collected and read; the gate is the §3 table.

### 8.7 Contract tests between definition and execution

Considered because the two halves are deliberately separable ([[7-application-boundary#8. Deployment topology]]). Rejected while they share one process and one shared types package: the compiler already fails the build on a contract break, which is what a Pact-style test would report later and less precisely. Worth revisiting at the point the split becomes two deployables, since that is when the type check stops spanning both sides.

## 9. Open questions

1. **Property-based testing for the rule engine.** `fast-check` over generated questionnaires and answer sets could assert properties the example-based cases cannot — evaluation never revisits an earlier index, the reachable set is stable under reordering of independent items, a predicate over unshown questions never makes an item visible. The engine's combinatorial input space is the ideal target; whether it earns its keep inside an interview timeline is undecided.
2. **Whether end-to-end runs on every PR** or only on the main branch once the suite grows past roughly a minute. Fine as-is at three specs.
3. **Load testing.** The claims in [[3-scaling]] about delivery under concurrency are currently arguments, not measurements. `k6` or `autocannon` against the definition-delivery endpoint would substantiate or embarrass them. Not designed.
4. **Accessibility beyond the committed minimum.** Narrowed rather than deferred: an `@axe-core/playwright` check on the respondent form and the draft editor **is** in scope, alongside the role- and label-based queries in §2.4, on the grounds that the domain argues for it even where the rubric does not ([[10-frontend#7. Accessibility]]). What remains open is everything past that line — a full WCAG 2.2 AA audit, a real screen-reader matrix across NVDA, JAWS and VoiceOver, and reduced-motion and high-contrast handling.
5. **Mutation testing** (Stryker) as the honest check on whether the rule-engine suite is actually good rather than merely green. The right tool for the question and almost certainly out of scope; noted so the question is on the record.
