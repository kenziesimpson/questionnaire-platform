# Dynamic Questionnaire Platform — Design Doc

> Status: **scaffold** — sections are filled in as decisions land. Prompts in *italics* are the questions each section should answer; delete them once answered.
> Related: [[1-ideation]], `.claude/skills/questionnaire-assignment/SKILL.md` (outside the vault index), assignment brief (Claude Project).

## 1. Overview

The Dynamic Questionnaire Platform lets an admin author reusable, versioned questions into versioned questionnaires with conditional branching over prior answers, then publish and retire them, and lets a respondent complete a published questionnaire through a form that reveals only the questions their own answers make applicable. The two roles work against different halves of the same system: admins operate an authoring UI over drafts and published version history — creating and reusing questions, wiring branching predicates, publishing — while respondents work against exactly one pinned published version, answering, branching, and resuming a session they left incomplete. The end-to-end workflow this prototype demonstrates is the one the brief grades directly: an admin publishes a medical-condition questionnaire whose yes/no answer determines whether two follow-up questions appear, a respondent completes it down each branch, the admin then publishes a second version that changes one question without disturbing what version one's already-collected responses meant, and both versions stay independently interpretable afterward.

## 2. Goals

- **Prove the full authoring-to-response loop end to end.** An admin creates, edits, publishes and retires questionnaires built from reusable, versioned questions with conditional branching; a respondent completes a published version, branches correctly, and resumes an interrupted session. This is the brief's required-capabilities checklist and the "working behavior" review dimension in one loop — running, not merely described.
- **Make immutability a database guarantee, not a house rule.** A published questionnaire version, and the question versions it pins, never change once published, and a collected response stays interpretable against the version it was gathered under — enforced by a trigger and a test that bypasses the API ([[#6. Versioning & Immutability]]) rather than left to application discipline.
- **Give the definition/execution boundary a mechanical answer.** `/api/definition/*` and `/api/run/*` are separate modules with no cross-imports, separate database roles, and exactly one immutable artifact crossing the line ([[#9. API / Service Boundary]]) — the brief's named "clear service/API boundary" capability, checkable without trusting a naming convention.
- **Ship the mandatory branching demo with tests, not a walkthrough.** The yes/no → conditional-follow-up scenario, and the version-2 publish that must not disturb version-1 responses, are both covered by automated tests ([[#15. Testing]]) — the brief asks specifically for versioning and conditional navigation to be tested "above all."
- **Model the domain so the hard cases are unrepresentable, not merely validated.** Branch cycles, forward references, a draft leaking into execution, and an invalid answer shape are all closed by the format and the schema ([[#7. Branching Rules]], [[#12. Database]]) rather than caught at runtime — what the brief's "domain design" dimension is judged on.
- **Record every material decision with what it cost.** The Decisions Log ([[#17. Decisions Log]]) carries each choice against its rejected alternatives; "decision quality" is graded against that record, not against the finished architecture alone.
- **State what does not ship, and why, with a production path for each.** Auth, multi-tenancy, HIPAA, localization and the checkpoint endpoint are named in [[#4. Out of Scope]] and [[#19. Future Work]] instead of left implicit — "production thinking" is graded on the plan for what is missing as much as on what is built.
- **Keep the design doc readable front to back, in one sitting.** Depth lives in the numbered docs it links to; this document stays the index a reviewer actually finishes, which is what the "communication" dimension asks for.

## 3. Constraints

*Hard limits we are designing within: time, single developer, must be runnable with one command, published versions immutable, etc.*

- **Nothing is deleted; things are hidden.** A general principle rather than a per-entity rule: questionnaires retire via `closes_at` ([[#8. Sessions & Responses]] §8.1), questions are archived rather than removed, question versions are append-only, and published snapshots and collected responses are immutable. Most of the design had already converged on this independently — stating it explicitly is what makes that coherent rather than coincidental, and it is the rule to apply when a new entity's lifecycle comes up. The risk being avoided is data loss, and in this domain the data is someone's medical history. See Decisions Log #15.
  - **Known exception: erasure on request.** A production system under GDPR or HIPAA must be able to remove a specific respondent's data. That is a separate, explicitly invoked, audited capability — never something normal operation does — and the distinction is what keeps "nothing is deleted" true as an operational statement. Out of scope for the prototype; see [[#19. Future Work]].
  - Archival ([[3-scaling#3. Problem: response ingest vs. reads]] §3.6) moves old partitions to cold storage. That is movement, not deletion, and stays inside the principle.
- **Prefer the simple, out-of-the-box thing.** Where two designs deliver materially the same guarantee, take the one with less bespoke machinery: stock tooling over hand-rolled, generated migrations over custom SQL, application validation over a database object built only to hold it. This is one developer against a deadline, and every custom object is something a reviewer has to read and a future agent has to keep in sync. It does **not** override the invariants above — where the data layer is the *stated* enforcement point (Decisions Log #17, #24), the custom object earns its place and the rule becomes that it is written down and tested instead. The distinction is between machinery that carries a guarantee we are claiming, and machinery that closes a shape nothing can produce. See Decisions Log #33; first applied in #34.

## 4. Out of Scope

| Deferred capability | Why deferred | Production approach |
| --- | --- | --- |
| Authentication / authorization | Not required to demonstrate the core workflow; would consume time better spent on versioning and branching. | Define the access *model* and data barriers now (admin vs. respondent, session ownership) so auth can be slotted in. Assume an upstream identity provider (OIDC) and enforce roles in a Fastify hook. |

## 5. Questionnaire Format

> **Detail: [[5-questionnaire-format]].** Summary only here.

A questionnaire version is a **flat, ordered list of items**. Each item places one question version at an index and carries the questionnaire-specific concerns — `required` and an optional `visibleWhen` predicate. Reusable question content carries neither: where a question sits, whether it is required there, and what makes it appear are properties of the placement, not of the question.

There are no edges between questions. Order is the list index, and the next question is the first unanswered item whose predicate is true, so convergence after a branch is not a property to prove — it is the only thing a list can do.

Five response types — `text`, `single_choice`, `multiple_choice`, `number`, `date`. There is no `yes_no` type: a yes/no question is a `single_choice` with two options, created by an editor template seeding the reserved ids `yes` / `no` with editable labels, so the same question can be phrased True / False without becoming a different kind of thing. Two identity guarantees carry the versioning story: **option ids are stable across question versions**, and **number answers are stored with their unit**. Both exist so that revising a question cannot retroactively change what an earlier response meant.

A published version serializes to a single JSONB document carrying its own `formatVersion` ([[#12. Database]] §12.1).

See Decisions Log #6, #11, #12, #36 (which supersedes #10).

## 6. Versioning & Immutability

> **Detail: [[5-questionnaire-format#6. Versioning mechanics]].**

**Questionnaires.** At most one draft at a time, enforced by a partial unique index rather than application logic. **Publishing promotes the draft row in place** to version *N*; the next draft is an explicit copy of the latest published version. A published version is never edited.

**Questions.** Append-only, with no draft state on the bank: every save writes a new immutable `question_version` row, so saving *is* publishing. A stable `questionId` carries identity across revisions, and a questionnaire item pins a `questionVersion` at the moment the question is added and keeps it, so a draft never shifts under its author. Questions are archived, never deleted ([[#3. Constraints]]).

**Responses** store `questionId` — what you aggregate on — alongside `questionVersion` (what you render with), the questionnaire version, and option ids. The version is derivable from the snapshot; storing it anyway keeps a response interpretable without loading the definition it was collected under. The line stops at the version pointer: prompt text is not copied onto responses.

Immutability is enforced in three layers: a database trigger rejecting `UPDATE` on published rows — that is the guarantee — a `409` from the authoring API for a usable error, and a test that drives the update straight at the database so the guarantee cannot silently regress behind a service-layer refactor.

The published snapshot carries a `formatVersion` and is upgraded **in memory at read time**; stored bytes are never rewritten. Immutability here means the bytes, not merely the meaning — the stored document is the record of what a respondent was actually shown.

See Decisions Log #7, #8, #13, #14.

## 7. Branching Rules

> **Detail: [[5-questionnaire-format#4. Branching rules]] and [[5-questionnaire-format#5. Publish-time validation]].**

Each item carries an optional `visibleWhen` predicate: a **single level** of `all` / `any` over conditions **typed per response type**, so an invalid comparison such as a date against a number is unrepresentable in the shared package rather than a runtime error class. A condition names an **`itemId`** — a placement, not a reusable question — and may reference only items at a lower index, so "is it earlier?" always has exactly one answer. A question may be placed at most once per version, which keeps `question_id` a sound aggregation key (Decisions Log #41).

Next question is the first unanswered item whose predicate is true; completion is running off the end of the list. A condition on a question that was not shown evaluates to `false` for every operator, negative ones included — a condition means *the referenced item is shown and its answer exists and satisfies the operator*. Text conditions are `{ op: "answered", value: boolean }`; `value: false` is the one case that holds without an answer, and only while the item is shown.

**Cycles and deadlock are not detected, they are unrepresentable.** Forward-only references make a cycle unconstructible, and falling off the end of the list is always available as a terminal state. Publish-time validation therefore covers forward references, satisfiability and referential integrity. Satisfiability is checked exactly, by domain intersection — affordable precisely because composition is flat.

One evaluator in the shared package, used by both the client (rendering) and the server (submit-time authority).

See Decisions Log #6, #9.

## 8. Sessions & Responses

> **Detail: [[7-application-boundary#5. Execution API]].**

**Lifecycle** is `in_progress → submitted` and nothing else. Abandonment is not a state; it is the absence of a submit, inferred from the session record for analytics ([[6-observability#4. Domain events]]) rather than written by a process that has to decide when to give up. Resume works because the session id is durable and the session pins its version at creation: the browser holds the partial answers, the server holds the pin.

**There is no checkpoint endpoint.** A debounced `PUT /sessions/:id/progress` was part of the design until late and is deliberately out. With respondents anonymous, the session id is a bearer capability held in the same browser storage as the partial answers, so every scenario that loses the answers loses the id that would fetch them back — the server-side copy is unreachable in precisely the cases it exists for. Genuine cross-device resume needs an identity to key on, not a write path. The operational half of its value — *where* a session stopped, not merely that it did — is already carried by client-emitted domain events ([[6-observability#4. Domain events]]). Deferred rather than rejected, and additive when it returns: [[9-database-schema#12. Open questions]] holds the table shape. See [[7-application-boundary#9.7 A debounced checkpoint endpoint]] and Decisions Log #25.

**Republish while sessions are in flight is a non-event.** A session pins its version when it starts, every later read returns that version, and the execution API has no route that resolves "the current version of questionnaire X" ([[7-application-boundary#5.2 There is no unpinned definition read]]). A new publish is therefore invisible to every session already running and visible to every session started afterwards, with no coordination, no polling and no migration of in-flight state.

**Submit is the single write and the server is the authority.** It re-evaluates the reachable path with the shared rule engine against the pinned definition, and accepts only if every visible required item is answered, every submitted answer belongs to an item visible on the computed path, every value satisfies its question version's constraints, and `closes_at` has not passed. All-or-nothing in one transaction — partial acceptance would leave a session in a state the model does not have.

**Idempotency uses the session as the key**, not a separate idempotency-key table: a duplicate submit is always a retry of the same session. Submit locks the session row, and if it is already submitted a stored digest over the canonicalized answers decides between replaying the original receipt and returning `409`. A network retry is safe; a genuine second submission of *different* answers is an error rather than a silent overwrite of someone's medical history. This also catches the two-tabs case that a client-supplied key would miss.

See Decisions Log #19.

### 8.1 Questionnaire lifecycle and retirement

Retirement is a property of the **questionnaire**, not of a version. Retiring never un-publishes a version: every published version stays readable so historical responses can still be rendered against the definition they were collected under.

The two lifecycles the product needs — questionnaires that run indefinitely, and questionnaires that close on a date — collapse into **one nullable `closes_at` timestamp**:

| Lifecycle | Representation |
| --- | --- |
| Ongoing | `closes_at IS NULL` |
| Scheduled close | `closes_at` set to a future date |
| Retired now | `closes_at` set to `now()` |

One field and one comparison, rather than a status enum plus a date that can drift out of agreement with each other.

**Behaviour past `closes_at`:** starting a session and submitting one are both rejected, and the respondent app renders a "responses closed" page rather than a raw error. This is a **hard cutoff** — a session started before the close but submitted after it is rejected too.

That is the strict choice and it has a real cost: a respondent can lose completed work through no fault of their own. The alternative, a **soft cutoff** admitting any session that started before `closes_at`, is kinder but leaves the close date open-ended for an unbounded window. The likely resolution is a per-questionnaire `cutoffMode: 'hard' | 'soft'` defaulting to hard. Deferred — [[#18. Open Questions]] §3 and [[3-scaling#8. Open questions]].

## 9. API / Service Boundary

> **Detail: [[7-application-boundary]].**

The brief grades "a clear application or service boundary ... that separates questionnaire definition from questionnaire execution", and the easy way to fail it is to ship a URL prefix — two route groups over one service layer and one set of models, where the separation is a naming convention a reviewer has to take on trust.

**The boundary is an artifact, not a route prefix.** Exactly one object crosses it, the immutable `PublishedDefinition` snapshot ([[5-questionnaire-format#3. Serialization]]), and the dependency runs one way: the definition side produces snapshots, the execution side consumes them and nothing else. Execution never reads a draft, never resolves a `questionId` against the question bank, never joins to an authoring table. That is affordable only because the snapshot is already self-contained by construction ([[#12. Database]] §12.1) — the storage decision was made for read performance, and a clean cut between the halves is the property being cashed in here.

**Three representations, each on one side of the line.** `QuestionnaireDraft` (definition only — normalized, mutable, may be invalid), **`PublishedDefinition`** (crosses; immutable, self-contained), `Session` + `Response` (execution only, pinned to a version). Execution code accepts only the middle one.

**Enforced in three places, none of which is the URL.** Encapsulated Fastify plugins with no cross-imports, an ESLint zone rule reusing the pattern already chosen for the telemetry boundary; wire types in `@qp/shared` that give execution no way to *name* a draft; and two database roles for these two halves specifically, where `qp_execution` has no grant on any authoring table and `qp_definition` has **no grant on `response`** — the authoring surface is not a back door into answer data. The barrier is therefore something Postgres enforces rather than something the service layer promises, and "are the halves actually separate?" has a mechanical answer instead of an architectural claim. (A third role, `qp_reporting`, exists alongside these two for an unrelated admin-only surface — [[7-application-boundary#3.2 Database grants]], Decisions Log #89 — and does not change this boundary.)

**Two route groups.** `/api/definition/*` owns the question bank, drafts, publish, retire, version history and snapshot inspection. `/api/run/*` owns sessions and submission, and is deliberately three routes — the client holds the whole definition and evaluates branching locally, so there is no next-question round trip to design. **The execution API has no unpinned definition read:** fetching a definition is inseparable from starting a session, which is what makes version drift mid-session unrepresentable rather than handled ([[#8. Sessions & Responses]]). Admin preview and version history live on the definition API instead, where the audience, the addressing and the access model are all different.

**Conventions.** RFC 9457 `application/problem+json` on every non-2xx, with `type` slugs from a closed union in `@qp/shared` so they are exhaustive on the client and cannot be invented at a call site. `400` is schema failure only — so a `400` is always a client bug and never a user mistake, which makes it a usable alerting signal; `409` means the request conflicts with current state; `422` means well-formed but domain-invalid. Validation is one TypeBox schema per route with the TypeScript type inferred from it, so a route's declared contract and its handler cannot disagree, and the frontend imports the same types. **Error bodies never echo a submitted answer** — a `422` names the item and the rule it broke, never the value — extending the telemetry redaction rule ([[#14. Observability]]) to the one other place it is easy to lose.

**Access model** (auth itself is out of scope, [[#4. Out of Scope]]): authors authenticate against `/api/definition/*` through a single plugin-wide hook, so a new definition route is protected by default and forgetting is not one of the available mistakes; respondents are anonymous and the session id is a bearer capability, which makes it a cryptographically random id, kept out of `Referer` headers and query strings and never rendered to anyone else.

**Topology:** two encapsulated plugins in one process now, two services later — chiefly for security, since the authoring API then need not be routable from the public internet at all, and a vulnerability reached through the unauthenticated respondent surface lands in a process that cannot touch authoring tables. Because the boundary is enforced by module graph, types and grants rather than by a network hop, that split is a deployment change rather than a rewrite; [[7-application-boundary#8.3 What we do now to keep the split cheap]] lists what the prototype does to keep it that way.

See Decisions Log #17, #18, #19, #20, #21.

## 10. Frontend

> **Detail: [[10-frontend]].** Summary only here.

**Decision:** React + TypeScript via Vite, as **two applications** — `apps/respondent` and `apps/admin` — served by one nginx container, with a shared `packages/ui` holding the component primitives and the questionnaire renderer. See Decisions Log #1, #27.

Two apps rather than one, because the eventual split into separately deployed frontends ([[3-scaling#6. Future improvement: split the frontends]]) is then a deployment change and the boundary between them is package resolution rather than a lint rule. The respondent bundle carries no admin code, which is the frontend echo of the argument for splitting the services ([[#19. Future Work]]).

**Respondent experience.** One URL, `/q/:questionnaireId`, and a state machine behind it — starting, form, submitted, closed, not-found. **All currently visible items render on one page**, and answering re-evaluates every predicate so items appear and disappear in place. That makes answer pruning something the respondent watches happen rather than something that occurs off-screen, which matters because submit rejects answers to items not on the recomputed path. Partial answers live in `localStorage` including answers to hidden items; the visible set is filtered once, at submit, using the same engine call the server runs. On landing the app looks for a stored session and resumes it before considering a new one. A successful submit clears the stored answers but keeps the session id, so reopening the link shows the receipt again rather than a blank form. See Decisions Log #28, #29, #70–#73.

**Admin experience.** Five screens — questionnaire list, draft editor, question bank, version history, preview. The question bank keeps its own screen because reuse is a graded capability and a screen showing one question used by three questionnaires demonstrates it; questions can also be created and edited from inside the draft editor, where **editing re-pins that item** to the new version so an edit cannot silently do nothing. The predicate editor is a flat list of condition rows — the honest UI for a single level of `all` / `any` — and it offers only earlier questions and only operators valid for the referenced question's type, so forward references and mistyped comparisons are unrepresentable rather than rejected. The question editor does the same for the cross-field constraint rules, and a question's response type is locked after its first save, on the server as well as in the dialog. See Decisions Log #30, #58, #61.

**Authoring concurrency.** Add-item requests carry the `questionVersion` the client displayed rather than resolving "current" server-side, so an author pins what they read. Two authors editing one question concurrently is accepted and documented rather than guarded: append-only loses no version, and because pinning is always explicit, a lost authoring edit cannot reach a published snapshot or change what a response means. See Decisions Log #31.

**Accessibility** is treated like answer redaction — designed in rather than audited afterwards, because the domain is medical. Radix primitives carry dialog focus management and control semantics, dnd-kit supplies a keyboard sensor and announcements for reordering, and the one gap the single-page model creates — dynamically revealed questions being silent to screen readers — is closed with an `aria-live` region. [[10-frontend#7. Accessibility]] states what is committed and what is explicitly not.

**Libraries** follow one rule: the respondent app stays dependency-light and imperative, the admin app takes tooling where it has cached server state or conventional editing. TanStack Router (code-based) and TanStack Query in admin; plain `fetch` and TanStack Form in respondent; dnd-kit for reordering; Tailwind v4 with shadcn/Radix primitives in both. See Decisions Log #32.

Deployment shape: both apps build into one tree — respondent at the root, admin under `/admin/` — served by one nginx container, built from `deploy/frontend/`, that reverse-proxies `/api` to the backend as a stand-in for a production ingress (see [[#13. Deployment]]). Assets can move to a CDN later without code changes.

## 11. Backend

**Decision:** Node LTS + Fastify + TypeScript. See Decisions Log #2, #3.

- Rationale: lightweight, schema-based request validation built in (JSON Schema / TypeBox), first-class plugin ecosystem including `@fastify/otel` for observability later, and a mature Node-compat surface so plugins and instrumentation "just work".
- API types shared with the frontend through a common workspace package.
- **Module layout:** two encapsulated Fastify plugins — `modules/definition` (bank, drafts, publish, retire, history) and `modules/execution` (sessions, submit) — each with its own routes, schemas and repository layer, no cross-imports, and its own connection pool bound to its own database role. `@qp/shared` holds the wire types and the single rule-engine implementation both sides use. One process today, two services when it earns it — see [[#9. API / Service Boundary]] and [[7-application-boundary#3. Enforcing the boundary below the type system]].
- **Transactions and concurrency:** publish is one transaction and the only multi-table write. Concurrent admin edits to a draft are resolved with an `If-Match` ETag over the draft, returning `409` rather than clobbering a second tab. Concurrent respondents touch disjoint session rows, and submit takes `SELECT ... FOR UPDATE` on its own session so a duplicate submit is serialized rather than raced ([[7-application-boundary#5.4 Submit: authority, validation, idempotency]]).

## 12. Database

> **Detail: [[9-database-schema]].** Summary only here.

**Decision:** PostgreSQL, accessed via Drizzle ORM over the `pg` (node-postgres) driver, with `drizzle-kit` generating SQL migration files. See Decisions Log #4.

- Rationale: Postgres gives transactions for publish operations, JSONB for storing immutable published definitions, and native partitioning for response-history growth. Drizzle keeps the schema in TypeScript, emits readable/committed SQL migrations, and stays close enough to SQL for partitioning, JSONB queries and `SELECT ... FOR UPDATE`.
- Connection pooling: `pg` pool in-process for the prototype; PgBouncer (or a managed pooler) in transaction mode is the first scaling step, and the trigger is the fourth backend process rather than a replica count (#78).
- Driver is swappable (e.g. Drizzle's `bun-sql` adapter) if the runtime ever moves to Bun.
- Schema: three schemas — `definition`, `execution`, `audit` — so the grant barrier below is structural rather than a per-table list to maintain. Entities, constraints, triggers and the ERD are in [[9-database-schema]]; §12.2 summarises the shape.
- Read/write characteristics: the two halves have opposite profiles. Definition tables are small (dozens of rows), written rarely and transactionally — a lost publish is a correctness failure, not a throughput one — and read almost entirely out of the in-process snapshot cache, so steady-state definition reads approach zero database work per session. `response` is large, append-only and written in one burst per completed session, with analytics reads that tolerate replication lag. `session` is the only contended row and it is contended only with itself.
- Indexing strategy: every index serves a named query or a named invariant, listed in [[9-database-schema#7. Indexing strategy]]. Notably absent: no GIN index over `snapshot` — access is whole-document and `version_question_index` covers the one query that would want it.
- Audit trail: an `audit` schema in the same instance, owned by a dedicated `audit_owner` role and written only through a `SECURITY DEFINER` function that appends. `qp_definition` holds no privilege on the table at all — not even `SELECT` — so append-only is a database guarantee rather than a convention, and the wrapper function is the same single repository entry point the outbox would later swap in behind. The audit write joins the publish transaction. Extracting it to a separate database or its own service later goes via a transactional outbox — kept cheap by design, not built now. See [[6-observability#5.1 Isolation — separate schema with a restricted role]].
- Application roles: `qp_definition` and `qp_execution`, both distinct from the migration role that owns the schema. `qp_execution` has no grant on any authoring table; `qp_definition` has no grant on `response`. The definition/execution boundary ([[#9. API / Service Boundary]]) is therefore enforced by Postgres grants and not only by module structure. Detail: [[7-application-boundary#3.2 Database grants]].

### 12.1 Authoring is normalized; published is a snapshot

Two representations of the same questionnaire, with publish as the seam between them. See Decisions Log #7.

**Authoring side — normalized rows.** Question bank, question versions, questionnaire drafts and draft items are ordinary relational tables. This is where foreign keys, partial edits and the reuse query ("which questionnaires use this question?") need to work naturally.

**Published side — one JSONB document per version.** Publishing runs in a single transaction: read the normalized draft, run the [[5-questionnaire-format#5. Publish-time validation]] validations, serialize to JSONB, flip the row to `published`. The result is read whole and never partially updated.

Why the split:

- The client fetches the entire definition once per session ([[3-scaling#2. Load model (what actually hits the backend)]]), so the read pattern is "give me all of it" — exactly what one row satisfies and what a four-way join plus row assembly does badly, on every session start.
- A `(questionnaire, version)` document is immutable, so it can be served with `ETag` and `Cache-Control: immutable` and held compiled in an in-process cache that never needs invalidating ([[3-scaling#4. Problem: hot definition reads]]).
- Immutability is enforced on one row in one table instead of being spread across item, option and rule tables.

**The duplication is the point, not an accident.** Question content exists both in the bank row and inside every snapshot that used it. That redundancy is what immutability *means* here: a published version cannot be disturbed by a later edit to the question it was built from.

**Reverse lookups get a derived side-table.** "Which published versions contain question X?" is the query a snapshot makes awkward. Rather than lean on a GIN index over the document, publish also writes rows into a thin `version_question_index` mapping version to question version ids. The snapshot stays authoritative; the side-table is a derived index that can be rebuilt from the snapshots at any time.

**On JSONB specifically.** `jsonb` parses on write into a decomposed binary form — key order and whitespace are not preserved, duplicate keys collapse, writes are slightly slower, reads and operators much faster. It is a queryable document type (`->`, `->>`, `@>`, `?`, GIN indexes), not merely a blob type; we use it in a blob-like way because our access pattern is whole-document. Documents above roughly 2 KB are TOASTed out-of-line, and the binary layout means reading one field from a TOASTed document generally de-TOASTs all of it — irrelevant here for the same reason, and a further argument for the side-table over a GIN index.

### 12.2 Schema shape

> Full DDL, triggers, grants and alternatives in [[9-database-schema]].

**`definition`** holds the question bank (`question`, `question_version`, `question_version_option`),
questionnaires and their versions (`questionnaire`, `questionnaire_version`), the draft items
(`questionnaire_item`) and the derived `version_question_index`. **`execution`** holds `session` and
`response`. **`audit`** holds one append-only table nothing can reach directly.

Four invariants live in the data layer rather than in service code, which is what makes them survive a
refactor:

- **A draft is structurally unreferenceable.** `version` is NULL exactly when a row is a draft, so a
  unique constraint on `(questionnaire_id, id, version)` lets any composite foreign key from `NOT NULL`
  columns match only published rows. A session cannot pin a draft and a questionnaire cannot point at
  another questionnaire's version — both are foreign key violations, not application checks.
- **Published rows reject `UPDATE` and `DELETE`, and cannot be inserted.** §6.4 specifies the `UPDATE`
  trigger; `DELETE` needs the same guard for the same reason, and an `INSERT` of a row already `published`
  is refused, so a published version exists only by promoting a draft. Draft items are guarded against
  their parent's status with a locking read, because the naive version of that trigger races a concurrent
  publish and lets the snapshot and the item rows disagree.
- **An invalid answer shape cannot be stored.** `response` uses typed columns per response type — option
  ids as a GIN-indexed array, `numeric` plus its unit, a bare `date` — under one check constraint, so
  "a single-choice answer with no choice" is unrepresentable rather than merely rejected upstream.
- **Collected responses are immutable and the audit log is append-only, both by grant.**
  `qp_execution` has `SELECT, INSERT` on `response` and nothing else, and a response's version must be
  its session's pinned version. The only `DELETE` any application role holds is `qp_definition` on draft
  items. `qp_execution` reads questionnaire versions through a published-only view, never the base table.
  This is [[#3. Constraints]] enforced where the brief asks for it rather than asserted.

`response` is range-partitioned monthly on `created_at`, which is set explicitly to the session's
`submitted_at` rather than defaulted — so a session's rows are provably co-located and the idempotent
replay read prunes to one partition. There is no `DEFAULT` partition: it would block
`DETACH ... CONCURRENTLY` outright and turn archival into an `ACCESS EXCLUSIVE` operation on the hot
table.

Concurrency control is three row locks, one each on the questionnaire row (publish vs. create-draft),
the question row (two concurrent edits racing on `version = max + 1`) and the session row (submit).
The middle case of the three is the only one that corrupts state rather than erroring, and it silently
loses a published version — [[9-database-schema#5. Concurrency control]].

See Decisions Log #23, #24.

## 13. Deployment

**Decision:** Each component deployed as its own container — frontend, backend, database — orchestrated with Docker Compose for local development and the prototype demo. Long-term target is Kubernetes with the same three-way split. See Decisions Log #5.

### 13.1 Local / prototype (Docker Compose)

| Service | Image / role | Notes |
| --- | --- | --- |
| `frontend` | nginx serving both Vite production builds, built from `deploy/frontend/Dockerfile` | Respondent at `/` and admin at `/admin/`, each with its own SPA fallback ([[10-frontend#2.2 URL structure and the nginx front]]). Reverse-proxies `/api/*` to `backend` so the browser stays single-origin (no CORS, no build-time API URL). |
| `backend` | Node LTS + Fastify | Stateless; config entirely from env vars. `depends_on` the `migrate` service completing successfully. |
| `migrate` | One-shot: `drizzle-kit migrate` + demo-questionnaire seed | Runs after `roles` exits 0. |
| `roles` | One-shot: `db/init/01-roles.sh` | Runs after `db` is healthy, exits 0. The only path that creates or updates roles — see below. |
| `db` | `postgres:16` | `pg_isready` healthcheck; named volume for persistence. |

- One command for a reviewer: `docker compose up` builds and runs the production-shaped stack, seeded with the medical-condition demo questionnaire.
- Configuration: environment variables only, with a committed `.env.example`. No secrets in images.
- **Four connection strings, not one** (three as of the original design; `DATABASE_URL_REPORTING` joined in Wave 3a, Decisions Log #89). `DATABASE_URL_OWNER` (the `migrate` service), `DATABASE_URL_DEFINITION`, `DATABASE_URL_EXECUTION` and `DATABASE_URL_REPORTING` (the backend's three pools, one per role; `modules/reporting` holds only its own `qp_reporting` pool and never borrows another role's — [[7-application-boundary#3.2 Database grants]]). Three pools at `pg`'s default of ten make 30 connections per backend process, which is what moves the pooler trigger to the fourth process (#78, [[3-scaling]]). The unsuffixed `DATABASE_URL` is deliberately not defined, so a stray `process.env.DATABASE_URL` fails loudly instead of silently selecting a role; `TEST_DATABASE_URL` keeps the unsuffixed form and is owner-level ([[8-testing#4. Postgres for integration tests — Testcontainers]]). Roles are created by `db/init/01-roles.sh`, never by a migration, and only ever by the `roles` service — the `db` service does not mount `db/init` — [[9-database-schema#11.3 Roles are not schema, and must not be in a committed migration]].
- **Postgres binds to loopback.** The `db` service publishes `127.0.0.1:${POSTGRES_PORT}:5432` rather than all interfaces, so `psql` from the host still works while the database holding medical answers is not reachable from the network.
- **Compose defaults let the stack boot without a `.env`,** which is a deliberate trade for "one command to run" and is the reason the committed defaults are obviously non-secret. Stated in the README rather than left as an accident; a real deployment supplies all six credentials from a secret store (`QP_REPORTING_PASSWORD` joined the other five in Wave 3a).
- Redis (from ideation notes) intentionally not included until a concrete need (session cache, rate limiting) appears — see Future Work.

**Reverse proxy — a stand-in for the production edge.** The nginx in the frontend container is filling a role that a production system fills elsewhere: in Kubernetes the ingress controller (nginx/Envoy/Traefik) routes `/api` → backend and `/` → frontend, and on a cloud platform the load balancer or CDN does the same path-based routing. The *principle* — single origin for the browser, path-based routing in front of the app — is permanent; only the implementation moves. Static assets likewise move to a CDN/object storage in production, leaving the ingress to route only. Keeping the proxy avoids CORS config, `SameSite` cookie issues and a build-time API URL baked into the bundle.

**Migrations have a dedicated owner.** Something must run `drizzle-kit migrate` and the seed before the API serves traffic. Options considered: (a) backend runs migrations at startup — simple, but with >1 replica both race to migrate, and schema changes become coupled to app restarts; (b) run manually — fails "one command" and repeatability; (c) a dedicated one-shot process. We use (c): the `migrate` service gates the backend via `depends_on: condition: service_completed_successfully`, giving a deterministic boot order (db healthy → schema + seed → API) that works with any number of backend replicas. In Kubernetes this becomes a Job that must succeed before the new backend rollout, which is also how a release protects published questionnaires (see [[#16. Scale & Growth]]).

**Dev loop / hot reload.** `docker-compose.override.yml` is auto-merged by `docker compose up` and is committed but local-only in effect. It changes two things per app service: the command (`frontend` → two Vite dev servers, respondent on 5173 and admin on 5174, `backend` → `tsx watch`) and bind mounts of the source directories. Vite pushes changes to the browser via HMR; `tsx` restarts the API. The base file is untouched, so a production build or CI run (`docker compose -f docker-compose.yml up`) gets real multi-stage images with no dev tooling. *Known issue, anticipated up front:* file-change events through bind mounts on macOS can be sluggish or missed, so the Vite config enables its built-in `server.watch.usePolling` from the start rather than waiting to hit it. `tsx watch` has no polling flag (it uses its own native watcher, not chokidar); it relies on Docker Desktop's VirtioFS event propagation, which is generally reliable — if a backend edit is ever missed, restarting the `backend` container is the fallback.

### 13.2 Long-term (Kubernetes)

Same three-way split as [[#13.1 Local / prototype (Docker Compose)]], on a different substrate — Decisions Log #5 already commits to Kubernetes, so what follows is what changes, not a new decision.

- **`frontend` and `backend` become Deployments + Services**, one-for-one with the compose services; `db` moves to a managed Postgres instance rather than an in-cluster StatefulSet, buying durability, backups and failover instead of reimplementing them.
- **nginx's two jobs split apart.** Routing becomes the ingress controller's job — `/api` → backend, `/` → frontend, the path-based rule [[#13.1 Local / prototype (Docker Compose)]] already states as the permanent principle — and serving the built assets moves to a CDN in front of object storage, leaving the ingress to route only, exactly as that section anticipates.
- **`migrate` becomes a Job that must succeed before the new backend rolls out** — the same deterministic boot order the compose service already gives, enforced by the scheduler instead of `depends_on`, and the mechanism that keeps a release from serving traffic against a schema it doesn't support ([[#16. Scale & Growth]]).
- **Backend scales via HPA** instead of a fixed replica count; config moves from compose env vars to ConfigMaps and Secrets, still the same four connection strings ([[#13.1 Local / prototype (Docker Compose)]]); `/health/live` and `/health/ready` ([[6-observability#8. SLOs and alerting]]) back the liveness and readiness probes, readiness in particular what stops a pod taking traffic while any of its three database pools cannot answer `SELECT 1`. Migrations are gated by the migrate Job, not by the probe.
- **Image build, registry and rollout strategy (rolling vs. blue/green) are the one piece left open.** Whichever is chosen has to preserve the guarantee the migrate Job already gives — no backend version live against a schema, or a published-definition shape, it predates.

## 14. Observability

**Decision:** OpenTelemetry end to end, with respondent answer values structurally excluded from every signal. Reasoning, field lists and enforcement design are in [[6-observability]]; its §13 holds the telemetry decisions O1 to O21. Build status by pull request is the table in [[4-implementation-plan#Wave 3b — observability and pipeline]]. See Decisions Log #16 and #92 to #97.

**Shipped** (T0a to T0c, the hardening pass and FE0):

- **One telemetry package.** `packages/telemetry` is the only importer of `pino` and `@opentelemetry/*`, enforced by ESLint. Logs are structured JSON on stdout, one logger per module, and a line written inside a span carries its `trace_id` and `span_id`.
- **Answers are kept out by construction.** Answer values are `Sensitive<T>`. Log and span context is a closed registry of fields whose types cannot carry free text, and a scrub runs at the call, at log output and at export; span names and logger modules are closed lists. A sentinel leak test plants a value and asserts it reaches no log, span or metric; it runs as its own CI job, "Response telemetry leak test" (`npm run test:leak-test`), and every PR that adds a path touching answers extends it. The `telemetry-safety` skill carries the rule for agents. Detail: [[6-observability#3. Respondent answers must never enter telemetry]].
- **Domain events** are one call, `emitDomainEvent`, writing a log line and a counter, so the two cannot drift. Only the leak test calls it so far: the backend logs requests, problems, health and shutdown, and opens no manual span and emits no domain event yet.
- **Errors and health.** Every non-2xx is an RFC 9457 problem, a 500 carries its trace id as `detail`, and `/health/live` and `/health/ready` back the probes ([[#13.2 Long-term (Kubernetes)]]).
- **Export is off by default.** With `OTEL_EXPORTER_OTLP_ENDPOINT` unset the app records spans and writes logs but exports nothing. Set, it sends traces and metrics over OTLP/HTTP through the scrub. There is no logs signal; logs leave through stdout.
- **A browser SDK**, `@qp/telemetry/browser`: a bounded queue, error capture that records a class name and stack frames and never a message, and a hand-built `traceparent`. Neither app uses it yet, and the backend has no ingest for it.

**Planned**, each owned by a numbered pull request:

- **Backend spans, events and metrics** for execution, definition and reporting (B1, B3, B4), the `/api/telemetry` ingest (B2), and the `view_response` audit row on a raw-answer read (B4, narrowed by A1).
- **Both apps** report through the browser SDK and propagate trace context (FE0b, FE1, FE2). Client-side telemetry is not optional: the SPA evaluates visibility in the browser, so server traces are blind to the path a respondent took.
- **Database telemetry** ([[6-observability#14. Database telemetry]]): D1 and D2.
- **The Collector, the opt-in `observability` Compose profile, and six alerts** (P1, P2). One command stays one command: the profile is opt-in.
- **Verification** of the whole chain, and the reconciliation of [[6-observability]] against what shipped (V1).

The audit trail is not an application log: who published or retired which version lives in an `audit` schema that no application role can reach except through one append-only function, so immutability is enforced by Postgres and the audit write shares the publish transaction. See [[#12. Database]] and [[6-observability#5. Audit trail]].

*Deferred, with direction recorded: SLOs, error budgets and backup verification ([[6-observability#8. SLOs and alerting]]); correctness and invariant monitoring for failures that return 200 and move no signal ([[6-observability#9. Correctness and invariant monitoring]]); deploy markers and a synthetic leak test ([[6-observability#12. Change correlation and synthetics]]).*

## 15. Testing

**Decision:** four layers, weighted toward the fast ones, with a real Postgres wherever the invariant being tested is a database object. Layer-by-layer reasoning, the database strategy and the pipeline shape in [[8-testing]]. See Decisions Log #22.

- **The suite exists to make the design's claims falsifiable**, not to reach a coverage number. Three invariants are load-bearing and every layer defends one: a published version is immutable, a response's meaning is pinned to the version it was collected under, and the server is the authority on the reachable path. [[8-testing#3. Required coverage — the graded list]] maps each graded capability to the layer that proves it.
- **Most of the suite is pure domain unit tests on the shared package** — rule evaluation, publish-time validation, snapshot `formatVersion` upgrade, answer canonicalization. Because client and server run the *same* evaluator ([[#11. Backend]], [[#10. Frontend]]), one suite covers both; two evaluators would need two suites and could still drift. Table-driven, since the input space is a product of small sets rather than a list of scenarios.
- **Backend tests use Fastify's `app.inject()`** — a synthetic request through the full lifecycle (hooks, validation, handler, serializer, error handler) in-process, with no port and no lifecycle to manage. The only prerequisite is splitting the entrypoint into a `buildApp()` factory and a thin `server.ts`. Registering *one* plugin on a bare instance and driving it to completion also turns the definition/execution separation into a mechanical check rather than an architectural claim ([[7-application-boundary#3.1 Module encapsulation]]).
- **Integration tests run against a real Postgres from Testcontainers, never a mock.** The invariants under test *are* database objects — the immutability trigger, the one-draft partial index, the append-only audit role, `qp_definition`'s missing grant on `response` — and a mocked repository passes with all four absent. One container per run; migrations applied once to a template database; each Vitest worker creates its own database from that template, so parallel workers cannot truncate each other's rows. `TEST_DATABASE_URL` overrides the container, which is what keeps the suite portable to a pipeline that can supply Postgres but not a Docker socket.
- **Immutability is tested in all three places it is enforced**, including one test that bypasses the API and writes to the database directly — the only place the suite deliberately goes around the application, because a trigger that is never reached is not evidence of anything.
- **Frontend: Vitest + React Testing Library on jsdom.** Vitest rather than Jest so there is one transform and resolution config in the repo instead of two; the test API is Jest's. Component tests import the shared engine rather than restating what branching should do.
- **End to end: three Playwright specs against the composed stack** — the mandatory branching demo, resume, and the v2 publish boundary — run through nginx against the production images, so what is tested is the artifact a reviewer runs. Preconditions are seeded through the definition API rather than clicked through the admin UI, except in the spec that is about authoring. The mandatory demo is covered at two layers deliberately: exhaustively in the rule-engine cases, and once end to end.
- **One command locally; parallel jobs in CI.** A root Vitest config with `projects` makes `npm test` run all three Vitest suites; `npm run test:e2e` stays separate. CI (`.github/workflows/ci.yml`) runs `lint`, `typecheck`, `build`, `unit-tests` (four shards), a "Response telemetry leak test" job and the end-to-end job side by side, after a `changes` job that skips them all when only Markdown changed. The end-to-end job builds images and brings up compose, so a typo does not wait on a Docker build ([[8-testing#5.2 Pipeline shape]]). Because the specs need only a `baseURL`, the same three run after a deployment as a smoke test — which is the answer to how this works under Kubernetes without a second suite.
- *Deferred, recorded in [[8-testing#9. Open questions]]: property-based testing of the rule engine, load testing to substantiate [[3-scaling]], accessibility assertions, mutation testing, and whether end-to-end runs on every PR once it outgrows three specs.*

## 16. Scale & Growth

> **Detail: [[3-scaling]].** Condensed to the five areas the brief calls out; ordered levers and triggers live in the detail doc.

| Area | Approach |
| --- | --- |
| Read traffic | Client fetches the whole published definition once per session and evaluates branching locally with the shared engine ([[#7. Branching Rules]]), so concurrency load is almost entirely a cacheable fetch, not server computation. `Cache-Control: public, max-age=<long>, immutable` plus an `ETag` per `(questionnaire, version)` make that fetch cacheable at every layer because a published version never changes; each backend replica also holds a compiled in-process cache keyed the same way, with the mutable `questionnaire → current version` pointer on a short TTL. A CDN in front of the respondent app and a shared Redis L2 cache are the next two levers, triggered by replica count and cache cold-starts respectively — not built until then. Detail: [[3-scaling#4. Problem: hot definition reads]]. |
| Data growth | `response` is append-only and range-partitioned monthly on `created_at`, set explicitly to the session's `submitted_at` so a session's rows are provably co-located ([[#12. Database]] §12.2); there is no `DEFAULT` partition, so a missed rollover fails loudly instead of blocking `DETACH ... CONCURRENTLY` later. Every index ties to a named query or invariant ([[9-database-schema#7. Indexing strategy]]) — notably no GIN index over the JSONB snapshot, since access is whole-document and the derived `version_question_index` already serves the one reverse lookup that would want it. Archival detaches older partitions to cold/columnar storage on a per-questionnaire retention policy once volume warrants it — movement, not deletion ([[#3. Constraints]]). Detail: [[3-scaling#3. Problem: response ingest vs. reads]]. |
| Reliability | Submit is idempotent on the session via a stored digest over the canonicalized answers (Decisions Log #19, #37): a network retry replays the original receipt, a genuine second submission of different answers gets `409`, and acceptance is all-or-nothing in one transaction, so there is no partially-saved session to recover. Postgres MVCC means writes don't block reads; the real risk under load is resource contention, so respondent reads and admin/analytics reads are isolated by path, with a read replica taking cross-session queries off the primary once volume warrants it. The failure mode the design is built around is a lost acknowledged submission — why the ingest queue that appears once write volume saturates the primary must be durable (Redis Streams with AOF and ack-after-commit, at minimum) rather than fire-and-forget. Detail: [[3-scaling#3. Problem: response ingest vs. reads]]. |
| Change control | Published questionnaire versions and question versions are immutable and append-only ([[#6. Versioning & Immutability]]), so a schema migration only ever adds — it never has to reconcile a definition that changed under it, and a response's meaning stays pinned to the version it was collected against. `drizzle-kit` generates committed, reviewable SQL migrations; roles are created by `db/init/01-roles.sh`, never inside a migration, keeping a schema change and an access-control change two different reviews ([[9-database-schema#11.3 Roles are not schema, and must not be in a committed migration]]). In Kubernetes a migrate Job must succeed before the new backend rolls out ([[#13.2 Long-term (Kubernetes)]]) — the same guarantee compose already gives: a release never serves traffic against a schema, or a `formatVersion`, it predates. |
| Operations | First-priority signals: RED per endpoint, Node saturation (event loop lag, `pg` pool waits), and domain events for session drop-off. Respondent answers are structurally excluded from telemetry. Audit trail in the database, not the log pipeline. SLOs, alerting and backup verification deliberately deferred. Detail in [[6-observability]]. |

## 17. Decisions Log

*Each decision with alternatives considered and why they were rejected. Append as decisions are made.*

| # | Decision | Alternatives considered | Rationale | Date |
| --- | --- | --- | --- | --- |
| 1 | Frontend: React + TypeScript via Vite, as an SPA with a separate backend | Next.js (unified FE/BE) | Next's strengths (SSR, SEO, auth ecosystem) don't apply to an admin tool / respondent flow with auth out of scope, while its App Router complexity does. The brief grades a clear definition-vs-execution service boundary and separation of concerns; a separate backend makes that boundary architecturally obvious instead of something a reviewer must trust. Vite is the current standard for React SPAs. | 2026-09-12 |
| 2 | Backend framework: Fastify | NestJS; FastAPI (Python) | Lightweight, built-in schema validation, `@fastify/otel` plugin for observability later. Nest adds boilerplate/DI ramp; FastAPI would mean two languages and generated rather than shared API types. | 2026-09-12 |
| 3 | Runtime: Node LTS (Bun optional for tooling only) | Bun as server runtime | Workload is I/O bound so runtime speed is not the bottleneck; scaling levers are pooling, indexing, caching, replicas. Bun's Node-compat layer still carries risk with Fastify plugins, pino transports and OTel instrumentation, and "straightforward to run" is graded. | 2026-09-12 |
| 4 | Data access: Drizzle ORM + `pg` driver + drizzle-kit migrations on PostgreSQL | Prisma; raw driver only (`pg` / `Bun.sql`) | Repeatable migrations and a reviewable schema are deliverables, so a raw driver is insufficient. Drizzle is SQL-close (partitioning, JSONB, row locks are natural), emits committed SQL migrations, and is driver-swappable. Prisma is heavier and abstracts more of the SQL. | 2026-09-12 |
| 5 | Deployment: separate frontend (nginx), backend and Postgres containers via Docker Compose; dedicated one-shot migrate/seed service; dev-only compose override with polling file watch; Kubernetes with the same split long-term | Single container serving SPA from backend; migrations at backend startup; manual migrations; Redis sidecar from day one | Three-way split mirrors the graded separation of concerns and is the same topology k8s will use. nginx proxy stands in for the production ingress and keeps the app single-origin. Dedicated migrate service gives deterministic boot order and survives multiple replicas. Polling watch chosen up front because bind-mount events on macOS are unreliable. Redis deferred until a concrete need. | 2026-09-12 |
| 6 | Questionnaire structure: a flat ordered list of items, each with an optional visibility predicate over earlier answers | Pointer/graph of questions with merge edges; adjacency-constrained optional segments (original ideation) | A graph needs an explicit merge edge per branch plus a proof that all paths converge, and makes cycles constructible. Adjacency buys convergence structurally but ties each rule to exactly one trigger question, which the brief's "one or more previous responses" rules out. A list makes order the index, convergence automatic, insertion a splice, and cycles and non-termination unrepresentable. Cost: no arbitrary jumps or loops, which the brief does not require. | 2026-09-12 |
| 7 | Published definitions stored as one JSONB snapshot per version; the authoring side stays normalized | Fully normalized published versions; JSONB for drafts too | The execution read pattern is "the whole definition, once per session", which one immutable row serves and a four-way join serves badly. Immutability lands on one row in one table instead of four. Normalized drafts keep foreign keys, partial edits and the question-reuse query. A derived `version_question_index` restores reverse lookups without a GIN index over the document. | 2026-09-12 |
| 8 | Publishing promotes the draft row in place to version N | Copy the draft into a new published row and retain the draft | Promotion means no moment exists where two rows describe the same pending version, and "at most one draft" becomes a partial unique index rather than application logic. The next draft is an explicit copy of the latest published version, which is the action an author actually wants. | 2026-09-12 |
| 9 | Branch conditions are a per-response-type discriminated union, grouped by a single level of `all` / `any` | Generic `{ questionId, op, value }`; arbitrary nested boolean expression tree | Typing conditions per response type makes invalid comparisons (date against number) unrepresentable in the shared package rather than a runtime error class to detect, message and test in an engine that runs on both client and server. One `all` / `any` level satisfies "one or more previous responses" without an unbounded tree needing recursive editing, validation and explanation. | 2026-09-12 |
| 10 | ~~`yes_no` is sugar over `single_choice` with reserved option ids `yes` / `no`~~ — **superseded by #36**, which removes the type entirely | A distinct boolean response type | One storage shape and one operator set for every choice question; reserved ids keep yes/no rules and cross-questionnaire analytics uniform. A separate boolean type would duplicate the choice machinery for the two-option case. | 2026-09-12 |
| 11 | Required-ness and branch predicates live on the questionnaire item, not on the reusable question | Both on the question version | A question is reusable; whether it is required, and what makes it appear, are properties of where it sits. Putting them on the question would force a duplicate question record whenever the same prompt is required in one questionnaire and optional in another, defeating the reuse the brief asks for. | 2026-09-12 |
| 12 | Number answers stored as `{ value, unit }`; option ids stable across question versions | Bare numeric answers with the unit resolved from the definition; storing option labels on responses | Both express the same invariant: a response must stay interpretable when its question is revised. A unit change (cm to in) or an option relabel in v2 cannot retroactively change what a v1 answer meant, and the stored response is readable without joining back to the definition. | 2026-09-12 |
| 13 | Questions are append-only with no draft state on the bank; questionnaire items pin a question version at add time | Independently versioned bank with its own draft/publish lifecycle; questions as mutable templates versioned only at the questionnaire level | Append-only gives real question versioning while keeping the bank's lifecycle to "insert a row" rather than a second draft-to-publish state machine, and it composes with the snapshot instead of duplicating it. Pinning at add keeps a draft stable between sessions and makes upgrading a visible act. Costs: an explicit save action (one save, one version) and no upgrade action yet. The independently versioned bank stays available later as an additive change. | 2026-09-13 |
| 14 | Responses store `questionId`, `questionVersion`, the questionnaire version and option ids | Store only `questionId` and derive the version from the snapshot; also denormalize prompt and label text onto the response | `questionId` is what aggregation across versions needs; `questionVersion` is derivable from the snapshot but stored anyway so a response is interpretable without loading the definition. The line stops at the version pointer — a unit changes what a value means numerically, prompt wording does not, and copying it onto every row buys nothing the snapshot does not already hold exactly. | 2026-09-13 |
| 15 | Nothing is deleted; things are hidden — retire, archive, append-only, immutable | Hard deletes with cascade; soft-delete flags on selected tables only | The data is medical history, so the risk of loss outweighs the tidiness of deletion, and most of the design had already converged here independently. Making it a stated principle means new entities inherit it by default rather than each lifecycle being re-argued. Erasure on request (GDPR/HIPAA) is the known exception and is a separate audited capability, not normal operation. | 2026-09-13 |
| 16 | Observability: OpenTelemetry end to end; respondent answer values structurally excluded from telemetry (`Sensitive<T>` wrapper + single telemetry boundary + exporter allowlist + sentinel test gated in CI); domain events as a first-class stream; audit trail in an `audit` schema behind an `INSERT`/`SELECT`-only role rather than in the log pipeline; client-side telemetry batched through a backend endpoint | APM auto-instrumentation alone; deny-list / regex redaction of logs; audit entries as `info` logs; a separate audit database fed by a transactional outbox; browser telemetry shipped straight to a Collector | Auto-instrumentation answers "why did this request fail" but not "why did this respondent give up", which is the question this domain actually has — and it cannot see decisions the client makes locally. Deny-list redaction fails open on every field added later, so a wrapper type makes a leak structurally impossible instead; the demo questionnaire collects medical conditions. An audit trail needs retention and integrity independent of logging-cost decisions; a restricted role makes append-only a database guarantee while keeping the audit write inside the publish transaction, where a separate database would place it outside and allow a publish with no audit record. The outbox that resolves that is the documented path to a standalone audit service, deferred rather than rejected. A public collector would be unauthenticated and could not apply the same attribute allowlist. | 2026-09-12 |
| 17 | The definition/execution boundary is one immutable artifact crossing a one-way dependency, enforced by module zones, shared wire types and separate database roles *(amended by #48: execution reads versions through a published-only view)* | Two route groups over a shared service layer; execution assembling definitions from the normalized authoring tables | A URL prefix is a naming convention a reviewer has to take on trust, and nothing would stop an execution handler reading a draft next month; the brief grades the boundary explicitly, so one that *cannot* be violated beats one that merely has not been. The self-contained snapshot (#7) makes the cut free — execution needs no authoring table to render anything. Grants are the only layer that survives a refactor or a careless import, and denying `qp_definition` any read on `response` stops the authoring surface being a back door into medical answers. | 2026-09-13 |
| 18 | The execution API has no unpinned definition read; version history and snapshot inspection are definition-side endpoints | `GET /api/run/questionnaires/:id/current` with session creation separate; one shared definition endpoint serving both audiences; admins reading history straight from the database | An unpinned read reintroduces the window where the client renders version *N* while the session pins *N+1* — exactly the in-flight-republish case the brief asks us to defend — and closing it needs reconciliation machinery that exists only because the endpoint does. Deleting the route makes the drift unrepresentable instead. CDN cacheability is a weak counterargument for a few-KB document already held in an in-process cache. The admin history view is a real need, but its audience, addressing and access model all differ, so it belongs on the authenticated side; direct database reads would concede the grant barrier and are not available to a browser app in any case. | 2026-09-13 |
| 19 | Submit is idempotent on the session itself via a stored digest of the canonicalized answers; validation is all-or-nothing in one transaction | A client-supplied `Idempotency-Key` header with its own table; last-write-wins; partial acceptance of the valid answers | The session already *is* the unit of a submission, so a separate key adds a table and a lifecycle for no additional guarantee — and the session-keyed form additionally catches two open tabs, which a client-generated key would not. Comparing a digest distinguishes a safe network retry from a genuine second submission of different answers, which must be an error rather than a silent overwrite of medical history. Partial acceptance would leave a session in a state the model does not have. | 2026-09-13 |
| 20 | RFC 9457 `problem+json` on every non-2xx with a closed union of `type` slugs; `400` schema-only, `409` state conflict, `422` domain-invalid; error bodies never echo submitted answers | A bespoke `{ error, message }` envelope; `400` for anything client-caused; returning the offending value in validation messages | A standard already specifies how to extend itself, so validation and submit failures add extension members rather than a second error shape. Reserving `400` for schema failures makes it a signal that always means client bug and never user mistake, which is alertable; splitting `409` from `422` keeps two genuinely different situations from collapsing into one code. Echoing the value back is the natural phrasing of a validation message and the easiest place to lose the redaction rule #16 enforces everywhere else — the client holds the answer and can render the message locally. | 2026-09-13 |
| 21 | Two encapsulated Fastify plugins in one process now, with the split into two services kept as a deployment change | Two services from day one; a single plugin with route-prefix separation only | One process keeps "one command to run" — explicitly graded — and one log stream to debug, while module, type and grant enforcement means nothing about the boundary depends on a network hop. The split is deferred rather than designed away because it is mostly a security win: the authoring API need not be internet-routable at all, and a vulnerability reached through the anonymous respondent surface would land in a process whose role cannot touch authoring tables. Keeping it cheap has a price paid now — no cross-imports, no shared in-process state, separate pools on separate roles from the start. | 2026-09-13 |
| 22 | Testing: four layers — shared-package domain units, Fastify `inject()` integration against a Testcontainers Postgres, Vitest + RTL components, three Playwright specs against the composed stack; one command via Vitest `projects`; no coverage gate | Jest on the frontend; mocked repositories or `pg-mem` instead of a real database; a compose-provided test database as the default; `supertest` or a listening server instead of `inject()`; Cypress; a coverage percentage threshold; Pact-style contract tests across the boundary | The invariants this project is graded on are database objects — the immutability trigger, the one-draft index, the audit role, the withheld `response` grant — so a mock or `pg-mem` would pass with all of them dropped and give confidence exactly where there is none. Testcontainers keeps `npm test` a single command that behaves the same on a laptop and a runner, with `TEST_DATABASE_URL` as the escape hatch for pipelines that can supply Postgres but not a Docker socket. `inject()` exercises the identical Fastify lifecycle with no port to collide under parallel workers, and registering one plugin alone turns the definition/execution boundary into a runtime check. Vitest reuses `vite.config.ts`, so the tests run through what the app is built with, for a Jest-shaped API. Playwright is chosen over Cypress for the trace viewer, which makes a failure on a runner debuggable without reproducing it. A coverage threshold is rejected because the graded behavior sits in a few intricate modules and a repo-wide number is most easily met by testing what does not matter. | 2026-09-13 |
| 23 | Schema: three Postgres schemas; normalized authoring plus JSONB snapshot; typed per-type columns on `response`; `response` range-partitioned monthly on `created_at` with no default partition; immutability and the published-only pin carried by triggers and composite foreign keys | One `value jsonb` column on `response`; a `question_option` registry table to make option-id stability a foreign key; deleting item rows at publish; partitioning `session` too; a `DEFAULT` partition | Typed columns make "how many chose option X" an index scan and make an invalid answer shape unrepresentable, at the cost of a migration per new response type. The option registry was built and removed: it cannot distinguish a relabel from a new id, so it reads as enforcement without being it. Keeping item rows avoids a snapshot-deserialize path whose bugs would produce a wrong *next version* rather than stale admin data. A default partition blocks `DETACH ... CONCURRENTLY` and turns a missed rollover into a blocked fix instead of a loud failure | 2026-09-13 |
| 24 | Response immutability and audit append-only are enforced by Postgres grants, not by service code: `qp_execution` gets `SELECT, INSERT` on `response` and no `DELETE` exists anywhere *(amended by #45: `qp_definition` may delete draft items)*; `audit.event` is reachable only through a `SECURITY DEFINER` function owned by `audit_owner` | Narrowed `INSERT`/`SELECT` grants on the application role for audit; enforcing response immutability in the repository layer; a second connection as a genuinely separate audit role | [[#3. Constraints]] says responses are immutable and the brief asks for invariants in the data layer, but the mechanism had only ever been described for published definitions — a grant closes it for the same cost. `SECURITY DEFINER` is strictly stronger than narrowed grants for the same effort (the app role ends up with no privilege on the audit table at all) and keeps the write inside the publish transaction, which a second connection cannot do | 2026-09-13 |
| 25 | No checkpoint endpoint: partial answers stay in the browser and the execution API is three routes, with no server write between session start and submit | Shipping `PUT /sessions/:id/progress` on a debounce; a progress column on `session` rather than a separate table | With auth out of scope the session id is a bearer capability stored in the same browser storage as the partial answers, so a cleared profile, an incognito close or Safari's ~7-day eviction take the id and the answers together — the server-side copy is unreachable in exactly the cases it exists for, and real cross-device resume needs an identity to look a session up by rather than a write path. What is left is operational visibility into where a session stopped, and the client-emitted domain events from #16 already answer that without a second place raw answers live, each with its own grant, redaction surface and retention question, on the hot side of the system. Deferred rather than rejected: [[9-database-schema#12. Open questions]] records the table shape, so it comes back as one table, one grant and one route with nothing to migrate | 2026-09-13 |
| 26 | The seeded demo's version 2 relabels exactly one option — `opt_hyperten`, "Hypertension" → "High blood pressure (hypertension)" — keeping the option id, and changes nothing else | Rewording a question prompt; adding further condition options; adding a new branching predicate or a new conditional item as the v2 change | The brief asks for a v2 that changes one question *without changing the meaning of responses already collected*, so the change has to put a v1 response at real risk and then demonstrate the risk is not real. A relabel does exactly that: the response stored `opt_hyperten` rather than the text (#12), so v1 and v2 answers still aggregate on one id while each renders through its own pinned `questionVersion` — and a design that copied labels onto responses or reissued ids on edit would fail visibly instead of silently. Rewording a prompt exercises the same mechanism with a softer claim, since most rewordings worth making do shift the question. Adding options or a new conditional item is monotone: nothing collected under v1 is at risk, so a passing test proves nothing was ever in danger. A predicate also lives on the item, not the question, so changing one would not touch the append-only question-version mechanism the requirement points at; it is kept instead as an integration fixture proving submit re-evaluates against the *pinned* definition | 2026-09-13 |
| 27 | Frontend is two Vite applications — `apps/respondent` and `apps/admin` — served by one nginx container, with a shared `packages/ui` for primitives and the questionnaire renderer | One app with two HTML entry points; one app and one bundle with `/admin` routes; two nginx containers | The split into separately deployed frontends is already planned, so making them two applications now turns that into a deployment change, and the boundary becomes package resolution — `apps/respondent` cannot import `apps/admin` because it does not depend on it — rather than an ESLint zone rule that can be misconfigured. The respondent bundle carrying no admin code is the frontend form of the security argument for splitting the services (#21). One container keeps "one command to run" intact, at the cost of a second build and a second dev server. A single bundle with route separation was rejected on the same grounds as a route-prefix API boundary: the separation would be a naming convention | 2026-09-13 |
| 28 | Respondent renders all currently visible items on one page, re-evaluating every predicate on each answer, rather than one item per screen | A wizard driven by the engine's "first unanswered item whose predicate is true", with a review step before submit | Submit rejects answers to items not on the recomputed path, so closed branches must be pruned — on one page the respondent watches those questions disappear, while a wizard performs the same pruning two screens behind them, silently. It also removes state the model would otherwise need: no cursor to persist, no review step, no back-navigation semantics. The cost is accepted and recorded: "dynamically determines the next applicable question" is satisfied by filtering the item list rather than navigating it, and one page scales badly past demo size. The renderer's props do not encode the choice, so a wizard stays a change inside the renderer | 2026-09-13 |
| 29 | Partial answers persist to `localStorage` including answers to hidden items, filtered to the visible set once at submit; no resume-link route; the app checks storage for an existing session before creating one | Deleting answers the moment a branch closes; IndexedDB; caching the definition locally; a `/s/:sessionId` capability URL for cross-device resume | Pruning eagerly requires holding "storage contains only visible answers" across every mutation, and any path that misses it submits a stale answer and earns a `422`; filtering once at submit is a single chokepoint using the same engine call the server runs, and it lets a respondent toggle back without losing what they typed. IndexedDB buys nothing at a few KB, and Safari's eviction window applies to both. The definition is not cached because the resume read already returns it and a second copy could only disagree. The resume link is dropped because with the checkpoint endpoint deferred (#25) it opens a valid session containing no answers | 2026-09-13 |
| 30 | Five admin screens with the question bank as its own screen; questions can also be created and edited inside the draft editor, and **editing there re-pins that item** to the new question version | Folding question creation into the draft editor's picker only; leaving the item pinned and prompting "latest is v4 — update?"; building the general upgrade-to-latest action | Reusable questions are graded, and a screen that shows one question used by three questionnaires demonstrates reuse in a way a picker cannot. Inline editing then collides with pinning: without re-pinning, an author's edit creates version *N+1* while the item stays on *N* and the edit appears to do nothing — the worst kind of bug, because the model is behaving as designed. Re-pinning on an unambiguous in-context gesture fixes it without building the deferred general upgrade action; a read-only staleness marker covers the remaining path, editing from the bank screen | 2026-09-13 |
| 31 | Add-item requests carry the `questionVersion` the client displayed; concurrent edits to one question are accepted and documented rather than guarded; adding an archived question is rejected *(narrowed by #75: an archived question already placed in the draft is not rejected)* | Resolving the current question version server-side at insert; an `If-Match` on question saves mirroring the draft ETag; allowing archived questions to be added | Carrying the version makes "items pin at add time" literally true and is the authoring echo of the execution side's no-unpinned-read rule (#18) — an author pins what they read. Concurrent question edits are then safe to accept on blast radius rather than frequency: append-only loses no version, and because pinning is always explicit and nothing auto-upgrades, a lost authoring edit cannot reach a published snapshot or change what a collected response means. It stays a collision over bank content with both versions in history. A version picker in the draft editor is the change that would make the divergence visible, and is future work | 2026-09-13 |
| 32 | Frontend libraries split by app: TanStack Router (code-based) and TanStack Query in admin, plain `fetch` and TanStack Form in respondent, dnd-kit for reordering, Tailwind v4 with shadcn/Radix primitives in both *(amended, respondent half: TanStack Form is used per field — one `<form.Field>` per visible item, each validated against the shared `validateAnswer`, plus one form-level validator for the visibility-dependent `answer/required` check, with each field's own touched state (set on blur) deciding when its error shows — [[11-structural-refactor#3. The PRs]], PR 8b)* | React Router v7; file-based routing; a form library in admin; no form library anywhere; `react-beautiful-dnd` or Pragmatic drag-and-drop; Radix directly with no shared component set | One rule decides the split: the respondent app stays dependency-light and imperative — one URL, three uncached calls — while the admin app takes tooling where it has genuinely cached server state and conventional editing. A form library is declined in admin because its two hard forms are shaped at runtime, by the selected response type and by the referenced question's type, so it would spend its budget on conditional field registration. dnd-kit is chosen for a first-class keyboard sensor and screen-reader announcements, which is what makes drag reordering usable without a pointer; `react-beautiful-dnd` is deprecated and its successor leaves keyboard support to assemble. A shared component set beat Radix-direct once the bundle argument was measured at 3–4 kB gzipped against real consistency across two apps built in parallel. **Amended (PR 8b):** validating and surfacing errors per field, gated on that field's own touched state, is what the accessibility work in [[10-frontend#7. Accessibility]] needs `aria-invalid`/`aria-describedby` to track honestly — a single whole-form validator can say the submission is invalid, but not which item a respondent has actually visited, so it could only ever show everything at once or nothing at all | 2026-09-13 |
| 33 | Guiding principle: prefer the simple, out-of-the-box option wherever two designs deliver materially the same guarantee — stock tooling, generated migrations, application validation — reserving bespoke machinery for invariants whose stated enforcement point *is* the data layer | Arguing each case on its own merits with no stated default; a blanket "enforce every invariant in the database" rule | The design has accumulated an unusual amount of database machinery — triggers, composite foreign keys, a `SECURITY DEFINER` function, four roles, range partitioning — and each piece is justified individually, which is exactly what makes the pattern hard to stop: every shape that *could* be closed starts to look like it should be. Stating the default inverts the burden of proof, so a new custom object has to argue against a reviewer's reading time and the next agent's ability to keep it in sync rather than being the obvious next move. It costs nothing where enforcement is the point — #17 and #24 are untouched, and those remain the cases where the database is the only layer that survives a refactor — and it settles the cases where it is not, which otherwise get re-argued one at a time. | 2026-09-13 |
| 34 | Duplicate ids within a `multiple_choice` answer's `option_ids` are rejected by the submit validator, not by a database constraint | An `IMMUTABLE` helper function wrapping the de-duplication subquery, called from the `response_shape` check; both together | A `CHECK` cannot contain a subquery and Postgres has no core array-distinct, so the database form costs a custom migration and a permanent schema object existing to hold one subquery — the case #33 is about. The validator meanwhile already walks `option_ids` to check every id against the pinned question version's options, so uniqueness is one line beside a walk that must happen regardless, and it produces a `422` naming the item rather than a constraint violation the client cannot act on. The cost is stated rather than glossed: this is the one representable-but-invalid answer shape the database does not close, and it is precisely the shape that would double-count under `GROUP BY unnest(option_ids)`. It is also unreachable from a checkbox UI, which sends a set. The helper stays available as one additive migration that changes no application code — [[9-database-schema#13.8 An `IMMUTABLE` helper closing duplicate `option_ids`]]. | 2026-09-13 |
| 35 | `question.key` is **not** carried into the published snapshot; `questionId` in the snapshot is the bank uuid, and the worked example in [[5-questionnaire-format#3. Serialization]] is corrected to show uuids. The seed hardcodes its ids so the documented uuids are real | Carrying `key` alongside the uuid as an optional debugging field; carrying it as the authoritative `questionId` and demoting the uuid; leaving the example's readable slugs and accepting the mismatch | `key` lives on `definition.question`, which takes `UPDATE` (that is how `archived_at` is set), so it is mutable — copying it into an immutable evidentiary snapshot is the move #14 already rejected for prompt text on responses, and a later rename would leave every existing snapshot silently disagreeing with the bank. It is also nullable, so it could only ever be an optional field nothing may depend on, sitting beside the identifier that `visibleWhen` and every stored response actually key on — a decorative id next to a real one is an invitation to read the wrong one. The debuggability argument it was weighed for is already answered inside the document: every item carries its `prompt` inline, so a snapshot is self-describing without the slug, and the slug's genuine use — a stable handle for seeds and tests — is served by the column existing in the bank. Hardcoding the seed's ids is what converts the cost of this decision (a less readable example) into something verifiable instead. Follows #33 | 2026-09-13 |
| 36 | **`yes_no` is removed as a response type.** Five types remain; a yes/no question is a `single_choice` with two options, created by an editor template that seeds reserved ids `yes` / `no` with editable labels. The condition union has one `single_choice` member, the `display` hint is deleted, and `response_shape` loses its `yes_no` branch. Supersedes #10 | Keeping `yes_no` as a distinct type and giving conditions a matching `yes_no` tag (so the discriminant is the identity function); keeping the type but collapsing only the condition tag, with a documented type→tag mapping | #10 already called `yes_no` sugar, but the schema then half-implemented it — a distinct value in the type check, its own `response_shape` branch, a `display` render hint — while the operator table collapsed it, so the worked example declared a `yes_no` question and referenced it with a `single_choice` condition, which §5.4's own rule rejects. Something had to give. Removing the type rather than promoting it makes `condition.type === question.type` exact with no mapping to document or misremember, deletes a field with no column behind it, and drops a branch from the response constraint — three things gone rather than one added, which is the direction #33 points. It also returns a decision to the author: reserved ids with fixed Yes/No labels prevented phrasing the same question True / False or Agree / Disagree, for a widget that is a two-option radio list either way. The cost is stated rather than hidden: cross-questionnaire aggregation on `yes` / `no` becomes an editor convention rather than a database domain check, which is the same tier as option-id stability and defended the same way — upheld by the editor, proven by a test. The brief lists response types "such as ... yes or no", and the capability is present and visible in the editor; only the storage type is gone | 2026-09-13 |
| 37 | The submit digest is `SHA-256(JSON.stringify(...))` over the **validated response rows** — answers sorted by `itemId`, `optionIds` sorted, numbers as canonical exact decimal strings (trailing fractional zeros and point stripped, `-0` → `0`, applied by the validator before the row is built, so `72.5` and `72.50` are one answer), unanswered optional items absent — canonicalized in `@qp/shared`, and constrained to stay a pure function of the persisted rows | Hashing the raw request body; RFC 8785 JCS or another canonical-JSON library; a delimiter-joined byte string; including the session id or questionnaire version in the digest | Hashing bytes makes a re-serialized retry a spurious `409` on a submission that already succeeded, which is the failure the mechanism exists to prevent. A canonical-JSON standard solves a problem we do not have — it canonicalizes JSON *received from elsewhere*, whereas this object is built by our own code from our own rows, so key order is already fixed by the source; JCS additionally specifies numbers in terms of IEEE doubles, which would round the `numeric` column §6.1 chose so an evidentiary record does not. A delimiter-joined string collides the first time a text answer contains the separator, and `JSON.stringify` supplies correct escaping for free. Keeping the digest a pure function of stored rows is what makes the canonicalization reversible as a backfill instead of permanent, so the session id is left out as both redundant (comparison is always within one session) and disqualifying. The failure asymmetry justifies the small spec: drift yields a false `409` where nothing is lost, while a false `200` needs the form to lose information, not merely order it differently. Decimal canonicalization lives at the row, not in the digest, because `numeric` preserves input scale: canonicalizing only inside the digest would make it disagree with a digest recomputed from the stored column; and it loses no information, since trailing zeros do not change the value | 2026-09-13 |
| 38 | Relative date constraints (`not_future`, `not_past`) are evaluated server-side against **UTC today with one day of tolerance**, and client-side against the browser's local date; the shared evaluator takes `today` as a parameter rather than reading a clock | Strict comparison against the server's UTC date; the client submitting its UTC offset so the server can validate exactly; dropping relative constraints | Strict UTC is not a conservative choice, it is a daily outage: a respondent at UTC+13 cannot enter today's date for a `not_future` question for roughly thirteen hours a day, and the seeded demo puts exactly that constraint on a diagnosis date. Sending the offset is the correct fix and was not taken yet because it adds a wire field, a trust question and a storage question to make a soft constraint exact by one day — recorded in Open Questions §14 rather than rejected. The tolerance is defensible on failure asymmetry, the same reasoning that sized #37: the strict form blocks a correct answer and strands the respondent, while the tolerant form accepts a date at most one day beyond true on a field where the respondent is recalling something from years ago. Splitting the two clocks keeps the slack invisible to the user, since the browser validates against the respondent's own calendar and the server is only rejecting nonsense. Parameterising `today` makes every timezone case a unit test instead of a mocked clock | 2026-09-13 |
| 39 | Five database identities with three connection strings: a bootstrap superuser used only at cluster init, `qp_owner` as a plain role owning the database and every object, `qp_definition` and `qp_execution` as the backend's two pools (a third, `qp_reporting`, joined in Wave 3a — six identities, four connection strings, #89), and `audit_owner` as `NOLOGIN`. Roles are created by `db/init/01-roles.sh`, run by compose's one-shot `roles` service and shared verbatim with the Testcontainers `globalSetup`. No unsuffixed `DATABASE_URL`; Postgres binds to loopback | Making `qp_owner` the `POSTGRES_USER` (and therefore a superuser); creating roles in a drizzle migration; giving `audit_owner` a login; keeping a bare `DATABASE_URL` alongside the suffixed names; a separate role-setup path for tests | `initdb` creates `POSTGRES_USER` as a cluster superuser unconditionally, so folding `qp_owner` into it makes the identity that runs on every deploy a superuser — adding `COPY ... FROM PROGRAM`, `pg_authid` and the rest of the cluster to the blast radius of a routinely-executing container, and inflating §10's owner-level caveat into a cluster-wide one. The two identities have different lifetimes: one mints roles once, the other runs continuously. Separating them is the conventional arrangement rather than extra machinery, so #33 favours it, and it costs one env var pair plus `ALTER DATABASE ... OWNER TO qp_owner` and `GRANT audit_owner TO qp_owner WITH INHERIT FALSE`. Ownership by `qp_owner` is also what keeps `ALTER DEFAULT PRIVILEGES FOR ROLE qp_owner` pointed at the principal that actually creates the tables — otherwise future tables silently receive no grant. `audit_owner` takes `NOLOGIN` so the `SECURITY DEFINER` wrapper is the only path by construction. Dropping the unsuffixed `DATABASE_URL` makes a stray `process.env.DATABASE_URL` fail loudly rather than quietly picking a role. One roles script for compose and tests, because a reimplementation would let the grant tests pass against roles that differ from what ships | 2026-09-13 |
| 40 | Every list endpoint has an explicit `ORDER BY` served by an existing index — `id DESC` for the two top-level lists, `version DESC` for the two histories — and no endpoint paginates | Leaving order unspecified; ordering by `created_at DESC` with a new index; shipping offset pagination now; shipping keyset pagination now | Unspecified order is not neutral: Postgres returns heap order, which shifts after an `UPDATE`, so the question bank reshuffles after an edit and list assertions go intermittently flaky — ordering is correctness here, not presentation. `id DESC` costs nothing because primary keys are UUIDv7, making the PK index a creation-order index; `created_at` would add an index and, not being unique, would leave same-millisecond rows in arbitrary relative order, reinstating the instability. Pagination is genuinely deferrable at dozens-to-hundreds of rows, and because the client holds each list whole, presentation sort and filter belong there. The deferral is cheap specifically because of the ordering choice: a unique, indexed, monotonic key turns later pagination into `WHERE id < $cursor` on an existing index rather than a migration or an `OFFSET` scan | 2026-09-13 |
| 41 | Conditions, answers on the wire, the digest and the renderer's `onChange` are all keyed by **`itemId`**; publish rejects a question placed more than once in a version | Keying by `questionId` and forbidding duplicate placements to remove the ambiguity; keying by `questionId` and resolving duplicates by list position; allowing duplicate placements | `questionId` was ambiguous in the *validator* before it was ambiguous in evaluation: §5.2 rejects a predicate referencing a question at an equal or higher index, and with two placements that comparison has two answers. Naming the placement makes the question unaskable rather than answered — the same move as #6 making cycles unconstructible — and it aligns the condition with what `response`, the `items` error extension and the digest already key on. The duplicate-placement rule then survives for a different and better reason: two placements write two `response` rows carrying one `question_id` for one respondent, so `GROUP BY unnest(option_ids)` counts that person twice and the typed-column design exists precisely to make that query trustworthy. Nothing legitimate is lost, since reuse is reuse across questionnaires. The same keying reaches telemetry: a predicate has no id of its own, so `packages/telemetry`'s `session.item_skipped` event names the hidden *item*, not a predicate, to say which rule fired ([[6-observability#4. Domain events]]) | 2026-09-13 |
| 42 | Number answers cross the wire as a pattern-checked decimal **string**, and the client never sends `unit` — the server fills it from the pinned question version. The validator stores the string's canonical form (trailing fractional zeros and point stripped, `-0` → `0`), so an `integer` question accepts `72.0` as `72` | A JSON number with the precision concern handled at the column; letting the client send `{ value, unit }` as stored | Fastify parses a JSON number into an IEEE double, so the exactness `numeric` was chosen for is gone before the value reaches either the column or the digest — and #37 already requires exact decimal strings in the canonical form, so a double on the wire breaks idempotency as well as storage. Canonicalizing the string keeps it exact — only the spelling of trailing zeros is dropped, never a digit of value — and doing it before the row is built keeps the stored `numeric`, whose scale follows its input, in agreement with the digest. A client-supplied unit is worse than redundant: it is a hole straight through #12, which exists so that revising a question cannot retroactively change what an earlier answer meant. The wire pattern (`^-?(0|[1-9][0-9]*)(\.[0-9]+)?$`) admits no exponent notation, no leading zeros and no unary plus, so "a decimal string" has one spelling to check against, not a family of equivalent ones. This applies only to *answers*: a question's authored `min` and `max` stay ordinary JSON numbers, because they are never persisted as an evidentiary `numeric` value and never enter the digest | 2026-09-13 |
| 43 | The draft ETag is built from a `draft_revision` counter incremented in the same transaction as each draft mutation; `updated_at` exists alongside it for display only | An ETag over `updated_at`; a content hash of the draft; no ETag with last-write-wins | A concurrency check must not depend on a clock, and two draft writes inside one millisecond would compare equal on a timestamp — the same reasoning that put list ordering on a unique monotonic key (#40) and kept the submit digest off any clock (#37). Keeping `updated_at` as well costs one column and answers a real admin-list question ("last edited three minutes ago") that a counter cannot; the split is documented so nobody later mistakes the display field for the guarantee. A content hash would also work and costs a serialization on every read for no additional property | 2026-09-13 |
| 44 | The pinned definition endpoint is `private` and admin-only, and **no shared cache or CDN ever holds a definition**; the CDN lever covers the respondent bundle and static assets only | `public` immutable caching on the definition endpoint with a CDN absorbing repeat fetches, as [[3-scaling]] originally claimed; adding a public pinned endpoint for respondents to make that lever real | The two documents disagreed, and the boundary was right: #18 deleted the unpinned definition read, so a respondent never calls a definition endpoint at all — their copy rides inside the `no-store` session response — and the pinned endpoint is an authenticated admin surface. The CDN lever as written could not have worked, which matters because it sat in a graded scale section. Adding a public pinned endpoint to rescue it would reintroduce a second delivery path for the artifact whose single path is the boundary's whole claim. The equivalent win is taken in-process, where immutability makes it invalidation-free, and shared later via the L2 lever | 2026-09-13 |
| 45 | `qp_definition` holds `DELETE` on `definition.questionnaire_item` and on no other table; the item guard confines it to drafts | No `DELETE` anywhere, with draft items removed by a soft-delete column; resolving draft discard ([[#18. Open Questions]] §13) first; `ON DELETE CASCADE` from the version row | `PUT /draft` replaces the whole item list, so removing an item is a `DELETE`, and the no-`DELETE` rule made the endpoint unimplementable. The existing item trigger already rejects a `DELETE` whose parent is published, so the grant reaches only rows that were never part of a published definition, and #15 is about what has been published or collected. A soft-delete column would add state every reader has to filter for rows no one needs to keep. Discarding a whole draft stays open | 2026-09-13 |
| 46 | A `BEFORE INSERT` trigger rejects a `questionnaire_version` row inserted already `published`, reusing `reject_mutation()` | Relying on `version_state` plus the item guard; an application check | [[9-database-schema#11.6 The seed is an integration test wearing a disguise]] claimed the triggers refuse inserting a published row, but they covered only `UPDATE` and `DELETE`: a complete forged row inserted cleanly and only its items were refused, verified as `qp_definition`. One trigger on an existing function makes the claim true, and promotion in place (#8) the only way to a published row | 2026-09-13 |
| 47 | `TEST_DATABASE_URL` is an **admin** URL for an instance whose roles already exist: it creates the template and per-worker databases owned by `qp_owner`, and migrations still run as `qp_owner` | An owner-level URL, as first documented; giving `qp_owner` `CREATEDB`; a single shared database with no per-worker clones on that path | Creating databases needs `CREATEDB`, which `qp_owner` does not have, so the owner-URL form failed on its first statement. Granting `CREATEDB` would widen the shipped role everywhere to serve a test convenience. Migrating as `qp_owner` keeps default privileges attached to the principal that creates the tables (#39), so the escape hatch tests the same grants that ship | 2026-09-13 |
| 48 | `qp_execution` reads questionnaire versions only through `definition.published_questionnaire_version`, a `security_barrier` view of published rows, and has no `SELECT` on the base table; the view is revoked from `qp_definition` | Table-level `SELECT` as in the first grant list; row-level security for `qp_execution`; accepting it because a draft row carries no snapshot | A table grant let execution read draft rows — title, `created_by`, `draft_revision` — which [[7-application-boundary#2.1 What execution is structurally denied]] rules out. A view keeps the barrier a grant, the enforcement vocabulary the rest of the design uses, where RLS would be a new mechanism for one table (#33). The explicit revoke matters: default privileges would otherwise give `qp_definition` writes on an auto-updatable view that runs with the owner's rights | 2026-09-13 |
| 49 | `response (session_id, questionnaire_version_id)` references `session (id, questionnaire_version_id)`, made unique by `session_pinned_version_key`; the single-column keys stay | Deriving the version from the session and dropping the column; leaving it to submit code | Two independent keys let a response on a v1-pinned session carry a draft's or another questionnaire's version, verified as `qp_execution`, so a row could render against a definition its session never pinned (#14). The composite key closes it with one generated migration and one small unique index, inside `execution`, so it reaches into no authoring table (#17). Dropping the column would cost the self-describing export §6.1 of [[9-database-schema]] argues for | 2026-09-13 |
| 50 | Role passwords are restricted to URL-safe characters (`A-Z a-z 0-9 . _ ~ -`), enforced by `db/init/01-roles.sh` | Percent-encoding them in compose; passing separate `PG*` variables instead of URLs; documenting the limit without enforcing it | Compose interpolates passwords into the three connection strings (#39) and cannot percent-encode, so a password with `@` or `/` produced a malformed URI that surfaced only when migrate or the backend connected. Replacing the URLs would undo #39's one-variable-per-role shape. Refusing at init turns a late, confusing failure into an immediate one with a clear message, at the cost of a character set nobody generating a secret needs | 2026-09-13 |
| 51 | A version is published only through `definition.promote_draft`, a `SECURITY DEFINER` function owned by `qp_owner` and executable by `qp_definition` alone, which requires a draft and a snapshot matching the draft's item rows, writes `version_question_index`, promotes and moves the pointer; `qp_definition` has no `UPDATE` on those columns. The function does not audit | Leaving promotion to application code; a trigger allowing the promotion only under a transaction-local flag; a deferred trigger checking consistency at commit; `audit_owner` owning the function; the function auditing through a `USAGE`/`EXECUTE` grant to `qp_owner` (built, then rejected) | One `UPDATE` by `qp_definition` could publish any snapshot and a second could point respondents at it, with no index row or audit — verified live. A flag is settable by the same role, so it stops accidents only; a commit-time check cannot see the audit table. Column grants plus a definer function make the structural guarantee a database one, the #24 pattern, and the snapshot check stops a caller from promoting an item list the draft does not hold. The audit write stays out of the function because both ways to put it in cost more than they buy: a `qp_owner` path into `audit` reverses the arrangement [[9-database-schema#9.1 A dedicated role, inside the publish transaction]] establishes, where the migration identity has none, and `audit_owner` owning a definition-writing function would stop the `audit` schema being extractable on its own (#16). The cost is stated: a `qp_definition` holder calling the function directly publishes unaudited | 2026-09-13 |
| 52 | Publish-time validation stays in the application — `publishDraft` takes the locks, runs `validateDraft`, then calls `promote_draft` — and the `publish` audit row is written by `publishDraft` through the audit repository function in the same transaction | Reimplementing validation in SQL inside the function; a second, SQL-side satisfiability check; auditing inside the function | Validation is `@qp/shared`'s one implementation, shared with `POST /draft/validate` and the editor ([[5-questionnaire-format#5. Publish-time validation]]); a PL/pgSQL copy would be a second rule engine to keep in step, the drift #41 and the shared package exist to prevent. The function needs no opinion on predicates because it checks the snapshot's structure against the rows. Writing the audit row as `qp_definition` in the same transaction keeps a failed audit write rolling the publish back (#16, #24) without any new grant on `audit.record` | 2026-09-13 |
| 53 | Admin list default sort is client-side "most recently edited" (`updatedAt` desc) on both the questionnaire list and the question bank *(refined by #59: the questionnaire list sorts on the new `QuestionnaireSummary.updatedAt`, the bank on `latest.createdAt`)*; no other sort columns, no filters, and no search ship in Wave 3 | Building the fuller sort/filter surface (columns, status/type filters, search) now, before either screen exists | #40 already put presentation sort and filter on the client, since it holds each list whole; this picks the one axis that's actually useful for an author revisiting drafts and ships nothing else. The fuller surface is real work, just not blocking work — tracked as [gh#21](https://github.com/kenziesimpson/questionnaire-platform/issues/21), low priority | 2026-09-13 |
| 54 | The draft editor surfaces `422 questionnaire/draft-invalid` validation failures as a summary panel (jump-to-item links) for the first pass; inline per-item rendering is deferred | Inline per-item badges/icons only; both together from the start | Ship the simpler surface first. This is the same gap as the renderer showing only an item's first error code ([[#19. Future Work]], "a view listing every current error per question"), so both are tracked together as one follow-up: [gh#22](https://github.com/kenziesimpson/questionnaire-platform/issues/22), low priority | 2026-09-13 |
| 55 | `errorsByItemId` — the function grouping a problem+json body's `items`/`errors` extension into the renderer's `Record<itemId, SubmissionItemCode[]>` shape — lives in `packages/ui`, used by both apps; it drops `answer/not-visible` and `answer/unknown-item` for now rather than surfacing them anywhere *(amended by #62: built in the serial Wave 3 contract commit, before Tracks 6 and 7 branch)* | Building the grouping separately in each app; surfacing the two dropped codes as a toast or console warning now | One function keeps both apps reading one problem+json shape the same way, the same move as keying everything on `itemId` (#41) and canonicalizing the digest once (#37). The two codes never attach to a rendered item by design ([[10-frontend#3. `packages/ui` — primitives and the renderer]]), so there's genuinely no UI for them yet; revisiting is [gh#23](https://github.com/kenziesimpson/questionnaire-platform/issues/23), medium priority, with this reasoning captured there so it isn't re-litigated from scratch | 2026-09-13 |
| 56 | **A stored snapshot is loaded through one function in `@qp/shared`**, `readStoredDefinition(stored, formatVersion)`: it runs the format upgrade chain from the stored `format_version` — empty while only format 1 exists, so any other version is refused — then `Value.Check`s the result against `PublishedDefinition`, and throws on a snapshot failing either step. Execution loads every pinned snapshot through it, reading `format_version` from `published_questionnaire_version`. **The admin snapshot endpoint `GET /versions/:v` keeps serving the stored bytes verbatim**, including after a future format change, so the definition side does not call the loader; it checks each stored snapshot against the known formats before serving it as stored, never casting it, and a snapshot matching none is `500 internal`. **Changing the snapshot's shape requires all four of:** (a) bump `FORMAT_VERSION`; (b) keep the previous format's schema in `@qp/shared`; (c) add an upgrade step to `readStoredDefinition`'s chain; (d) widen `getVersion`'s `200` response and the endpoint's check to the union of all known formats, distinguished by `formatVersion` | Each reader keeping its own check (execution's local `Value.Check`, the definition side's cast); the admin endpoint upgrading a snapshot to the current format before serving it | Upgrades run in memory at read time and stored bytes are never rewritten (#7, [[5-questionnaire-format#6.5 Snapshot format version]]), so the upgrade chain is logic every reader must apply identically — the same drift risk that put the rule engine in `@qp/shared`, and a reader applying a different chain would interpret a document differently from what the respondent was shown. The loader is pure, a value and a number in and a `PublishedDefinition` out, so sharing it moves no query or grant across the boundary (#17, [[7-application-boundary#3.3 What stays shared]]). The admin endpoint stays verbatim because it promises the stored document, its tests assert byte identity with `snapshot::text`, and its `ETag` already carries `formatVersion` (#44), so a client can tell formats apart; upgrading there would serve bytes that were never stored under an immutable cache header. The cost: the admin client must read every live format, bounded by the support window ([[#18. Open Questions]] §4). A refused snapshot keeps its behaviour: `500 internal` with the trace id as `detail` (the request id when no span is active) | 2026-09-13 |
| 57 | **While authentication is stubbed, every authoring write records the author as the placeholder string `prototype-author`** — one constant, `AUTHOR_PLACEHOLDER`, exported from `modules/definition` and attached by the always-passing author hook ([[7-application-boundary#7. Access model and data barriers]]). **It is not an identity.** It lands in `question_version.created_by`, `questionnaire_version.created_by` and `audit.event.actor_id`, which are append-only by trigger and grant (#13, #24), so rows written before real authentication **can never be corrected** and will keep saying `prototype-author`. Anything reading authorship — the admin UI, audit review, a future migration that backfills real users — must treat that value as "unknown author", never as a user. The seed's `seed` actor is the same pattern and stays distinct from it | `null` until authentication exists; a per-request value such as a header or request id; a fake user id shaped like a real one | `null` is indistinguishable from "field not populated" and would be read as a bug, where a named placeholder is plainly deliberate and easy to search for. A request-derived value would be unauthenticated input in an audit column. A realistic-looking id is the worst option, because nothing could tell it from a real author afterwards. Keeping it a single constant means authentication replaces one import and one hook. The cost is permanent and stated: the prototype's audit trail cannot say who did anything | 2026-09-13 |
| 58 | **The question editor follows the Wave 3 prototypes for both items [[#20. Pending UI experimentation]] left open** ([[10-frontend#5.3 The question editor, and re-pinning]], [`docs/designs/`](designs/README.md)). *Constraint fields* follow `QuestionFields`: `text` — min and max length inputs and a `multiline` checkbox; `single_choice` / `multiple_choice` — the options list and an "Allow a freeform Other" checkbox, plus min and max selections inputs for `multiple_choice`; `number` — `numberKind` as a required Whole number / Decimal segmented control, min, max and unit inputs; `date` — earliest and latest as native date inputs, `relative` as an Any / Not in the future / Not in the past segmented control. **The ~~six~~ `QUESTION_RULE_CODES` cross-field rules are made unrepresentable in the controls, not reported after a save.** *(amended by #83: #61 and #83 each added a rule, so the count is dropped)* *Options authoring* follows `AdminQuestionEditor`: one row per option with a drag handle, the generated option id shown and locked, an editable label and a remove button; the freeform `other` option is marked on its own row. **The Yes / No template button (#36) is a required deliverable of Track 6's question editor** | Leaving both to the Track 6 agent; reporting cross-field failures inline after a save; author-typed option ids | Drawn so the screens are settled by something visible rather than invented per agent. Unrepresentable over reported is the predicate editor's move ([[10-frontend#5.4 The predicate editor]]): a max clamped at or above its min, selection bounds capped by the option count, ids generated so none can repeat, and only `other` able to be freeform, so a `400` reaching the author is an editor bug. Generated, locked ids take option-id stability (#12) out of the author's hands while relabelling stays safe. The Yes / No button is not polish: the brief requires yes/no to be visible in the admin UI, and #36 answers that with this template. It is also the only way a question gets the reserved `yes` / `no` ids once ids are generated, and those ids are what make yes/no answers aggregate across questionnaires ([[5-questionnaire-format#2. Question types]]) | 2026-09-14 |
| 59 | **`QuestionnaireSummary` gains `updatedAt`: the latest `questionnaire_version.updated_at` for that questionnaire**, which draft writes bump. That is what #53's "most recently edited" means on the questionnaire list. A `closesAt` change is not an edit and does not move it. The question bank sorts on `latest.createdAt`, with no contract change | Redefining #53 as creation order; counting a `closesAt` change as an edit; adding an `updatedAt` to `Question` as well | #53 named a sort the wire could not serve, since the summary carried only `createdAt`. The column already exists for this display (#43), so the change is one wire field and a join in the list query, not a migration. Retiring changes whether respondents can answer, not what the questionnaire says, and the list shows `closesAt` in its own column. Questions are append-only (#13), so a question's latest version *is* its last edit and `latest.createdAt` already means "most recently edited" | 2026-09-14 |
| 60 | **Compose re-applies `db/init/01-roles.sh` on every `up`** through a one-shot `roles` service (the `postgres` image, the bootstrap credentials, the committed script) that `migrate` waits on. The script sets each login role's password with an unconditional `ALTER ROLE` | Leaving it to `docker-entrypoint-initdb.d` alone and documenting `docker compose down -v`; re-running it by hand; creating roles in a migration; running it inside `migrate` | The entrypoint runs init scripts only on an empty data directory, so a role added after a volume exists never reached it and `migrate` failed with `28P01` — the fix was to wipe local data. Running the script each `up` makes adding a role or rotating a password a normal `up`. It does put the bootstrap credential in a container that runs routinely, which #39 avoided; the difference is that this container runs only `psql` and a committed script, not application code, and #39's point — that the *migration* identity is not a superuser — still holds. This is local compose only: a real deployment provisions roles from its secret store | 2026-09-16 |
| 60 | **Author-facing messages for the ten `DraftItemCode`s are one catalogue in `apps/admin`, typed `Record<DraftItemCode, …>`**, so a code added without a message fails to compile. Each message names the problem and the fix. It is separate from the respondent catalogue, `packages/ui`'s `questionnaire/messages.ts`. Wording and presentation get a design pass during Track 6 | Extending the respondent catalogue in `packages/ui`; showing the raw code; a partial map with a fallback string | #54's summary panel needs a sentence per code and none existed. Authors see these and respondents never do, so they belong to the one app that renders them rather than the package both apps import. A total `Record` makes completeness a compile-time property, where a fallback silently covers the next code added. Naming the fix is what an author needs: `predicate/forward-reference` is something they can act on, where a respondent message only has to say what is wrong | 2026-09-14 |
| 61 | **A question's response type is locked after its first save, enforced on the server.** `question/type-changed` joins `QUESTION_RULE_CODES`; appending a question version whose type differs from the latest version is `400 request/invalid` with that code, checked in `appendQuestionVersion` under the question row lock it already takes. The editor also disables the type selector | Locking in the editor only; allowing the change and re-validating drafts that pin the question; a `409` or `422` slug of its own | A condition is typed to the question it reads (#9), so a type change would invalidate rules in questionnaires the author cannot see from the dialog, surfacing as `predicate/type-mismatch` only when someone next re-pins the item (#30). The server check makes it an invariant rather than an editor convention. It is `400` for the reason every question rule is: the editor makes it unrepresentable, so reaching the server means a client bug (#20). A different type is a different question, created as one ([[5-questionnaire-format#6.2 Question identity and versioning]]) | 2026-09-14 |
| 62 | **`errorsByItemId` (#55) is built in a serial "Wave 3 contract" commit in `packages/ui/src/questionnaire/`, with its tests, before Tracks 6 and 7 branch.** Location and dropped codes are unchanged; this adds ownership and timing ([[4-implementation-plan#Wave 3 contract commit]]) | Whichever of Track 6 or 7 reaches it first; each track writing its own and reconciling at merge | Both apps consume it and neither owns `packages/ui`. Two parallel tracks writing one shared function either collide on the file or each write their own, which is the drift #55 exists to prevent. Wave 1a's contract commit is the precedent, and the same commit carries #59, #61 and #66 | 2026-09-14 |
| 63 | **The respondent app uses the shared primitives' sizes as they are for Wave 3.** The prototypes are first approaches, not pixel specs; tuning to their larger touch scale (52px option rows and 44px inputs against the primitives' 32px controls) is deferred | A respondent size variant on the primitives now; respondent-local style overrides | A size variant is a `packages/ui` change, which Wave 3 confines to the contract commit (#62); app-local overrides fork the primitives #32 shares. Deferring costs touch comfort, not a stated commitment: 32px clears WCAG 2.2's 24px minimum target size, and [[10-frontend#7. Accessibility]] claims no more | 2026-09-14 |
| 64 | **Authentication-related gaps do not block Wave 3.** `VersionSummary.publishedBy` stays `null` and the admin history screen renders it as absent. Recording a publisher is **deferred with authentication, not resolved**: `questionnaire_version` has no publisher column and `qp_definition` cannot read the publish audit row | Filling it from `created_by`; adding a `published_by` column and changing `promote_draft` now | `created_by` is whoever opened the draft, so it would label the wrong person. A new column today could only store `prototype-author` (#57), permanently, in rows that cannot be corrected, for no information; the migration and `promote_draft` change are worth making once, alongside real identities. Rendering "absent" rather than a placeholder keeps #57's rule that nothing presents the placeholder as a user | 2026-09-14 |
| 65 | **~~[gh#17](https://github.com/kenziesimpson/questionnaire-platform/issues/17) and~~ [gh#15](https://github.com/kenziesimpson/questionnaire-platform/issues/15) are punted and do not block Wave 3.** ~~gh#17: a question archived after it is placed in a draft blocks every `PUT /draft` for that questionnaire.~~ — **the gh#17 half is superseded by #75**, which brings its fix into Wave 3. gh#15: a taken `key` returns `500`, and `key` may be removed. Track 6 will hit both and neither fixes nor works around them, beyond not presenting a key input as required | Fixing both before Track 6; Track 6 working around gh#17 client-side | Both fixes are backend or contract changes outside Track 6's files, and gh#15's likely fix deletes the field, so client work around either would be thrown away. The authoring flow in [[4-implementation-plan#Manual checkpoints]] H6 archives nothing and needs no key | 2026-09-14 |
| 66 | **The shadcn primitives Track 6 needs — `Dialog`, `Select` / `Combobox`, `Table` and `Popover` — are added to `packages/ui` in the serial Wave 3 contract commit** (#62) *(amended: the unused `command`, `input-group` and Radix `select` primitives are removed; `native-select` and `tooltip` are added; `popover` is kept — [[11-structural-refactor#3. The PRs]], PR 9)* | Vendoring them into `apps/admin`; Track 6 adding them to `packages/ui` mid-track | Generated shadcn files carry no questionnaire logic, so they cost the contract commit little review. That commit is already the one serial step touching `packages/ui`, so adding them there keeps Tracks 6 and 7 inside their own apps. Vendoring into `apps/admin` would fork the shared versions #32 exists to keep consistent | 2026-09-14 |
| 67 | **When two tabs open the next draft and one gets `409 questionnaire/draft-exists`, the admin app refetches and navigates to the draft that now exists**, with no error shown | Showing the `409` as an error; offering a choice between the two | The losing tab wanted a draft and one exists, so the outcome the author asked for already happened. The server keeps it to one draft (#8), and the losing tab had written nothing, so nothing is lost ([[7-application-boundary#4.1 Endpoints]]). Contrast `409 questionnaire/draft-stale`, where the author's edit did not land and must be told | 2026-09-14 |
| 68 | ~~Preview's sample-answer inputs are plain inputs owned by the admin side panel, not a second renderer instance in `interactive` mode~~ — **amended by PR 12: the panel renders `<QuestionnaireItems mode="interactive">` over the visible items after all**, and can now enter the free-text Other answer. The previewed questionnaire itself stays `readonly` | ~~Rendering the questionnaire twice, once interactive for input~~ — **PR 12 adopts this; the rejection no longer stands**; the plain-input panel PR 12 deletes, which is what #68 originally chose; an interactive preview with submit disabled | ~~`packages/ui` owns no form state ([[10-frontend#3. `packages/ui` — primitives and the renderer]]), and a second interactive renderer would need someone to hold its answers inside the preview anyway. Panel inputs keep the renderer's contract to props in, markup out, while still letting an author walk both branches (`AdminPreview`)~~ — **that obstacle does not apply: `VersionPreview` already holds the answers**, the same state a plain-input panel needed anyway, so a second renderer instance keeps the renderer's contract (props in, markup out, no form state of its own) exactly as well as plain inputs did, while still letting an author walk both branches (`AdminPreview`) ([[11-structural-refactor#3. The PRs]], PR 12) | 2026-09-14 |
| 69 | **The list and bank screens keep TanStack Query's default refetch-on-window-focus** | Turning it off; polling | A second tab or a colleague's edit shows up when the author returns to the window, for no code. Lists are small and unpaginated (#40), so the extra reads are cheap. Off-by-default would be a choice to show stale data, and polling spends reads on a tab nobody is looking at | 2026-09-14 |
| 70 | **After a successful submit the respondent app clears the stored answers but keeps `{ sessionId, questionnaireId }`**, so reopening the link resumes to the `submitted` session and shows its receipt again | Clearing everything, as [[10-frontend#4.3 Local persistence]] first said; keeping the answers too | Clearing the session id makes a reopened link call `POST /sessions` and present a blank form to someone who has already answered, which invites a second submission and contradicts `RespondentDone` ("opening this link again shows this page"). The answers are what storage should not hold after submit: they are medical data that the server now holds authoritatively. The kept id fetches a session and a definition, never answers, and `GET /sessions/:id` already renders a submitted session as its receipt ([[10-frontend#4.2 Entry and resume]]) | 2026-09-14 |
| 71 | **Respondent partials are stored under one key per questionnaire, `qp:respondent:<questionnaireId>`, in an envelope carrying a `formatVersion`.** The envelope is shape-checked on load, and a mismatched or corrupt value is discarded rather than crashing | One key per session; storing the bare object with no version; migrating old envelopes | The lookup on landing is by questionnaire ([[10-frontend#4.2 Entry and resume]]), so the key is too. Storage outlives deploys, so the app will one day read a value an older build wrote, or one a user or extension mangled. A version plus a shape check makes that a fresh start instead of a white screen. Discarding loses only unsubmitted answers, since the server is authoritative for anything submitted. Migrating envelopes is machinery for a format that has one version (#33) | 2026-09-14 |
| 72 | **On `409 session/already-submitted` (a different digest), the respondent app fetches `GET /sessions/:id` and shows the receipt actually recorded**, noting that this form was already submitted | A dead-end error page; resubmitting | The `409` means a submission stands and this one was refused (#19), typically from a second tab. The respondent's useful next fact is what was recorded and when, which the resume read already returns. An error page would suggest nothing was saved | 2026-09-14 |
| 73 | **A network failure on any of the three execution calls gets a manual retry.** Submit is disabled while a request is in flight, and stored answers survive until a genuine `2xx` | Automatic retry with backoff; clearing answers optimistically on send | Submit is idempotent on the digest (#19, #37), so retrying it is safe, but a manual retry keeps the respondent aware that nothing has been confirmed yet. Disabling submit in flight avoids a second request racing the first. Answers held until a `2xx` mean a dropped response costs a click, not the form | 2026-09-14 |
| 74 | **"Start over" on a resumed session is punted; Wave 3 does not build it** ([gh#35](https://github.com/kenziesimpson/questionnaire-platform/issues/35)). `RespondentStart` draws the button. When built, it clears the answers and keeps the same session | Building it in Track 7; starting over with a new session | Resume works without it, and a restored answer can be changed by hand. A new session would leave two `in_progress` sessions for one respondent, the orphan reading as abandonment in the session record ([[6-observability#4. Domain events]]) | 2026-09-14 |
| 75 | **Archiving gates new placements only** — points 1–3 of [gh#17](https://github.com/kenziesimpson/questionnaire-platform/issues/17)'s resolution come into Wave 3, landing on `staging/track-6` before Track 6 merges to `main`. A placement is *new* when its `(questionId, questionVersion)` pair is not already in the stored draft. (1) `PUT /draft` refuses `draft/question-archived` for new placements only, so an archived question already placed no longer blocks saves. (2) Draft validation and publish report `draft/question-archived` for new placements only; an existing placement is not an error. (3) The draft editor shows no archived state for placed questions: Track 6 PR5 removes PR4's "Archived in bank" badge and frozen-draft copy. **Still deferred on gh#17:** an unarchive route (its audit action needs a migration), the picker showing archived questions already placed in the open draft, and whether archiving warns when a question sits in open drafts. Supersedes #65's punt of gh#17 (gh#15's punt stands) and narrows #31: adding an archived question is still rejected, an existing placement is not | Keeping #65's punt and shipping PR4's badge and copy; keeping `draft/question-archived` a publish blocker for existing placements; the full resolution including unarchive and the picker | Track 6 PR3 ships an Archive action that freezes any draft placing the question, with no warning. With two or more archived placements the draft cannot be recovered in the editor at all: each removal is its own `PUT` still carrying the other. Copy explaining the freeze would be thrown away by a fix already decided in gh#17. A publish blocker the editor does not show contradicts "the editor cannot show a green state that publish disagrees with" ([[10-frontend#5.2 The draft editor]]). The deferred points are conveniences once drafts stop freezing, and unarchive is a migration | 2026-09-15 |
| 76 | **Publish-checks presentation (Track 6 PR5, refining #60):** authors never see a problem code — the sentence is the finding, and the code stays in a `data-*` attribute for tests. An empty draft gets its own "no questions yet" all-clear line rather than "Ready". The draft editor's Remove confirmation for an archived question carries a small (i) note that re-creating it makes a new question whose answers are not tracked with the old one's | Showing the code beside each message, as `AdminDraftEditor` draws it; one "Ready" line for every passing draft; no note on Remove | A slug is for the code that reads it, not the author, and a `data-*` attribute keeps tests off the wording. "Ready" on an empty draft claims more than an empty pass means. Under #75 an archived question cannot be re-added once removed, and the obvious remedy — a new bank question — is a new `questionId`, which is the identity cross-version aggregation keys on ([[5-questionnaire-format#6.3 What a response stores]]) | 2026-09-15 |
| 77 | **A backend test worker's Postgres connection budget is bounded by its concurrency, never by its test count** ([[8-testing#4. Postgres for integration tests — Testcontainers]]). `testDatabase.connect(role)` is test-scoped and its clients close in `afterEach`; the per-test `TRUNCATE` and `readAuditEvents` share one lazily-opened `qp_owner` client per file; harness pools are capped well under `pg`'s default of ten; and a missing `VITEST_POOL_ID` is a hard error rather than a fallback to `qp_test_1`. ESLint keeps the rule in place: constructing a `pg` client or pool anywhere in the backend is warned on outside `db/client.ts` and the harness, and `_tests/` may not import `openDatabase` | Raising `max_connections` on the Testcontainers image; capping `maxWorkers` for the backend project; leaving `connect` file-scoped and closing clients by hand in each test | The suite's peak demand is `maxWorkers × per-file peak`, and per-file peak used to grow with the file's *test count* — 22 connections for a 23-test file — because `connect` closed its clients only in `afterAll`. `postgres:16-alpine` allows 100 with three held back for superusers, so on a machine with enough cores the ceiling arrived partway through a run and whichever file was executing failed with `53300`: a dozen or so of 328 tests, a different dozen each run, never reproducible from those files alone. Raising the ceiling or lowering the worker count hides that the demand is unbounded and does not travel to the `TEST_DATABASE_URL` path (§4), where the instance is not ours to configure. Closing by hand in each test puts a resource-lifetime obligation on every test author, which is what the harness exists to absorb. Bounding it instead took per-file peak to roughly five whatever the file does, and the suite passes repeatedly at `max_connections = 45` with 16 workers | 2026-09-16 |
| 78 | **Production bounds Postgres connection demand with an external pooler, not with arithmetic in application config** ([[3-scaling#3. Problem: write volume on submit]], [gh#73](https://github.com/kenziesimpson/questionnaire-platform/issues/73)). PgBouncer (or a managed pooler) in transaction mode is the scaling step, with per-role `CONNECTION LIMIT` as the guardrail beneath it. The definition/execution split stays one database | Dividing `max_connections` by an expected replica count in `config.ts`; a startup check that refuses to boot when the product exceeds the server; splitting definition and execution into two databases; raising `max_connections` on the instance | Demand is `replicas × processes × pools × max`, and each backend process holds 30 at `pg`'s default of ten per pool across three pools (20 across two when this was decided; `qp_reporting` added the third, #89), so four processes exhaust a stock 97-slot instance — a rolling update of three replicas at 25% surge, which rounds up to a fourth pod, reaches that on its own, and [gh#19](https://github.com/kenziesimpson/questionnaire-platform/issues/19)'s per-core cluster workers reach it on one 8-core box. No pod can know the replica count: an HPA changes it without a deploy, so static division either strands slots at low load or saturates during scale-out, and a pod that refuses to start mid-rollout breaks the deploy — which is why #77's approach is right for tests, where the worker count is known and fixed, and wrong here. A pooler inverts the problem, multiplexing many client connections onto a bounded set of server connections, so the ceiling stops being arithmetic anyone maintains. `CONNECTION LIMIT` costs one statement, is changeable at runtime without a restart (unlike `max_connections`), and stops execution starving definition or locking `qp_owner` out of migrations. Transaction-mode pooling discards session state, which this design survives because the roles connect as distinct users rather than `SET ROLE` on a shared connection ([[7-application-boundary#3.2]]). Asymmetric write traffic does not argue for two databases: PgBouncer pools are keyed by `(user, database)`, so the roles already take independently sized pools, and a split would turn `session_pinned_version_fk` — what makes “a session cannot pin a draft” structural — into application code | 2026-09-17 |
| 79 | **The track-based file-ownership table in [[4-implementation-plan#File ownership]] is deprecated**, in favour of [[11-structural-refactor#2. File ownership during the pass]] | Deleting the table; leaving it binding | Every track has merged, so the table no longer describes reality, yet it was still shaping the layout the structural refactor pass now changes. Deleting it would lose the record of how Wave 3 actually split work; marking it deprecated in place, with a pointer to its replacement, keeps that history readable without letting it keep governing new work | 2026-09-17 |
| 80 | **Execution persistence lives in `src/db/execution`, mirroring the definition side** ([[11-structural-refactor#3. The PRs]], PR 3). `src/modules/execution` keeps only `plugin.ts`: the routes, `problemFor` and the `cache-control` hook. The queries, the submit transaction and the `Session` / `Receipt` wire projections move under `src/db/execution` | Leaving execution's persistence inside its module; one flat `src/db` with no per-side directory | The definition half already splits this way — `src/db/definition` holds the repositories and `src/modules/definition` the HTTP layer — and execution was the only half where a route handler sat in the same directory as a `FOR UPDATE` transaction. One layout rule for both halves means the boundary can be linted rather than remembered (L1), and the two sides stay symmetric for the telemetry pass, which hooks the db layer in one place. A flat `src/db` would put session locking and authoring drafts in one namespace and lose the grant barrier's shape in code | 2026-09-17 |
| 81 | **A problem body is read off the wire whole or not at all.** `problemFromWire(wire)` in `@qp/shared` answers `undefined` unless the slug and every code in its extensions are ones the reading build knows; both the admin and the respondent then treat it as an unexpected response. It takes no policy argument. The parser validates a permissive envelope — the closed schema's members, open only in `type` and `items[].code` — so it can tell an unknown code apart from a malformed body, while the exported `ProblemDetails` stays closed and no route can serialize a code outside the union | Dropping unknown codes and rendering the rest, which is what [[11-structural-refactor#3. The PRs]] PR 1 specified as `unknownCodes: "drop"` for admin; keeping both policies behind one option | A partly-understood rejection under-reports: the form tells the author or respondent their submission has one problem when the server found two, they fix the one shown, resubmit, and fail again with nothing new to go on. A loud generic error is worse copy and better information. The plan's two-policy design rested on a false premise — it assumed admin already dropped unknown codes, but `apps/admin/src/api/problem-error.ts` and `apps/respondent/src/api/request.ts` both opened with the same `Value.Check(ProblemDetails, body)`, so both rejected, and a shared parser that keeps today's behaviour needs one behaviour, not two. Once partial rendering is ruled out, an option to do it is a trap for the next reader | 2026-09-18 |
| 82 | **The "other" option is the option whose id is `OTHER_OPTION_ID` (`other`) and that is freeform** — one definition, `freeformOptionOf` in `@qp/shared`, used by the submit validator, the renderer and the admin question editor ([[11-structural-refactor#3. The PRs]], PR 2a). The renderer shows the other text box only beside that option. The editor turns only that option into its Other toggle~~; an option under the `other` id that is not freeform is an ordinary option, and opening and saving the question keeps it one~~ — **that clause is superseded by #83**, which reserves the id `other` in both directions | Any freeform option is the other option, which is what the renderer did; any option with id `other` is, which is what the editor did; forbidding a non-freeform `other` option at save time | The three definitions agreed on everything the admin can author, because saving rejects a freeform option under another id (`question/freeform-not-other`, and the `freeform_is_other` check). They disagreed on a question saved through the API with a plain `other` option: the editor showed it as the freeform Other and saving the question made it freeform, so an edit the author did not make changed what respondents see and what `choice/other-text-*` accepts. The submit validator is the authority on what an answer may carry, so its definition is the one kept. Forbidding a plain `other` option would reject questions the API accepts today, which is a contract change this pass does not make | 2026-09-17 |
| 83 | **The id `other` is reserved for the freeform option in both directions: an option has the id `other` exactly when it is freeform.** Saving a question with a non-freeform `other` option is `400 request/invalid` with `question/other-not-freeform`, pointing at that option's `optionId`. The database holds the same rule as `freeform_exactly_when_other` (`freeform = (option_id = 'other')`), which replaces `freeform_is_other`, and the partial unique index `qvo_one_freeform` is dropped because the primary key now allows at most one freeform option per version ([[11-structural-refactor#3. The PRs]], PR 2c). Supersedes #82's plain-`other` clause | Keeping #82's one-way rule, where a plain `other` option is an ordinary option; a migration that makes stored plain `other` options freeform | #82 left one edge case. On a question with a plain `other` option, ticking **Allow a freeform "Other" option** in the editor failed with `question/duplicate-option-id`, an error about an id the author never typed and cannot change. With the id reserved both ways, that question cannot exist, and the editor has one case: the option with the id `other` is its Other toggle. This is a contract change: the two question-save routes accepted such a question before. A migration that rewrote stored rows would change a published question version, which #13 forbids, so the migration fails instead if any row breaks the rule | 2026-09-17 |
| 84 | **The respondent shows an item's errors once its field is left, not only after a submit attempt.** TanStack Form marks a field touched on blur; from then on its `aria-invalid` state and error text track that item's own per-answer validator live, updating as the answer changes. A submit attempt still touches and validates every item currently visible at once, so a first-ever submit continues to show every error together, exactly as it did before this change ([[11-structural-refactor#3. The PRs]], PR 8b) | Keeping the single whole-submission precheck that showed nothing until the first submit attempt; validating and showing every item's error as soon as the page loads, before anything has been answered | The old timing told a respondent about every problem at once, but only at the moment they were already reaching for Submit, and said nothing before that — including about an item they had already answered and left. Progressive disclosure on blur surfaces a mistake at the point the respondent's attention is already on that item, without demanding a value before they have had a chance to supply one. Showing every error on load would be worse than the old behaviour, not better: an empty required field is not yet a mistake for a respondent who has not reached it | 2026-09-18 |
| 85 | **Icons come only from lucide, through `@qp/ui/icons`.** `packages/ui/src/icons.ts` re-exports the lucide icon components the apps use; `lucide-react` is importable only there and inside `packages/ui/src/primitives`, and no file outside `packages/ui` draws a `<svg>` by hand ([[11-structural-refactor#3. The PRs]], PR 11) | Keeping each app's own hand-drawn icon set (`apps/admin/src/components/icons.tsx`'s 17 components, the inline `<svg>`s in four respondent screens); a per-app icon library instead of one shared set | Admin and the respondent had each hand-drawn the same handful of glyphs — a lock, an X, a plus, a spinner — as `<svg>` path data, one more thing to keep visually consistent by eye instead of by import. lucide already ships as `packages/ui`'s icon library for shadcn's generated primitives (`components.json`'s `iconLibrary: "lucide"`), so re-exporting from it through one module gives every consumer the same glyph set and lets L2 lint the boundary instead of relying on review to catch the next inline `<svg>` | 2026-09-18 |
| 86 | **The import extension convention depends on how a workspace is resolved.** `apps/backend`, `packages/shared` and `packages/telemetry` compile with `tsc` under `NodeNext`/`Node16` module resolution, so their relative imports keep the compiled `.js` extension. `apps/admin`, `packages/ui`, `apps/respondent` and e2e's `fixtures/` and `specs/` are resolved by Vite or Playwright, which resolve an extensionless specifier themselves, so those carry no extension. `e2e/stack/**` is a plain Node CLI, entered directly by `node stack/stack-cli.ts` outside Playwright's own resolver, and keeps its `.ts` extension for that reason even though the rest of e2e does not; `e2e/tsconfig.json` (bundler resolution, covering `fixtures/`, `specs/` and `playwright.config.ts`) and the new `e2e/tsconfig.stack.json` (`NodeNext`, covering `stack/` only) split the workspace's typecheck in two so `tsc` keeps enforcing the extension inside `stack/` at compile time, the same way it always did, rather than leaving that only checked at runtime. `NodeNext` resolution accepts a `.js` specifier for a relative import and quietly maps it to the sibling `.ts` file, on the assumption that a build step will later emit that `.js` — which `e2e/stack/**` never does, so a `.js` specifier there would type-check and lint clean and then fail at actual runtime with `ERR_MODULE_NOT_FOUND`. L13 therefore also forbids `.js` specifically inside `e2e/stack/**`, alongside its `.ts`-required exemption from the other two directions ([[11-structural-refactor#3. The PRs]], PR 14) | One extension convention for every workspace, `.js` everywhere or none everywhere; one `tsconfig.json` for all of e2e, accepting that `stack/**` would only fail at runtime if its extension were dropped; leaving a `.js` specifier in `e2e/stack/**` unchecked because `tsc` already accepts it | Node's ESM loader and `tsc` under `NodeNext` resolution refuse to resolve a relative specifier with no extension; Vite and Playwright resolve one themselves, and a written extension there is dead weight that drifts when a `.ts` file becomes a `.tsx` one. A single convention would either break the Node-run workspaces or add a permanently unneeded extension to the bundled ones. `e2e/stack/**` runs its CLI scripts (`node stack/stack-cli.ts …`) under plain Node the same way the backend does — confirmed by trying an extensionless relative import there, which Node's own resolver cannot find — so that directory follows the Node rule despite sitting inside an otherwise extensionless workspace. A single e2e tsconfig under `bundler` resolution would make that only a runtime failure (`stack:up`, or Playwright's `globalSetup`/`globalTeardown`, which import `stack/`'s other files); splitting the config keeps `tsc -p tsconfig.stack.json` catching a dropped extension there the same way it always did, and `e2e/package.json`'s `typecheck` script runs both configs. The `.js` case is the mirror gap: `NodeNext`'s `.js`-maps-to-`.ts` allowance exists for workspaces with a real build step, and `e2e/stack/**` has none, so `tsc` alone waves a `.js` specifier through — confirmed by writing one and watching both `tsc` and lint stay silent while `node` throws `ERR_MODULE_NOT_FOUND`. A lint rule is the only layer left that can catch it, since it is the one check that runs before either tsc's build-step assumption or an actual `node` invocation | 2026-09-18 |
| 87 | **A `_tests/` file never shares its basename with a sibling of a different extension.** `apps/respondent/_tests/screens/error-summary.test.ts` and `error-summary.test.tsx` merged into one `error-summary.test.tsx`, and [[8-testing#7. Test case enumeration]]'s citation for the pure-function cases now points at it too ([[11-structural-refactor#3. The PRs]], PR 21) | Leaving both files and special-casing the `.tsx` one for type-aware linting with `allowDefaultProject`; renaming one file to disambiguate instead of merging | Turning on `parserOptions.projectService` (PR 21, §4.2) surfaced that `tsc -p apps/respondent/tsconfig.test.json` silently dropped `error-summary.test.tsx` from its `include` resolution — confirmed by `--showConfig`'s resolved `files` list and reproduced in isolation: given a directory with both `foo.test.ts` and `foo.test.tsx`, TypeScript's config-file enumeration keeps only the `.ts` one. The `.tsx` suite had therefore never been type-checked by `npm run typecheck`, and `npm test` still ran it directly through Vitest since Vitest globs the filesystem itself rather than asking `tsc`. `allowDefaultProject` would silence the parser error without fixing the missing project coverage, and both files test the same source module (`src/screens/error-summary.tsx`), so `_tests/`'s one-file-per-source-file mirroring convention already implied they belonged together | 2026-09-18 |
| 88 | **The admin responses list ([gh#18](https://github.com/kenziesimpson/questionnaire-platform/issues/18)) is the first list to use #40's keyset-pagination exit, and renders each page as a plain table — no client-side list virtualization.** Fixed page size (20 sessions), `Previous` / `Next` over a keyset on the chosen timestamp and `id` — `(started_at DESC, id)` by default, and either timestamp in either direction since #90 — no total count fetched or shown | A virtualized/windowed list (e.g. `react-window`) rendering the full result set behind a fixed viewport; loading the whole result set client-side unpaginated, per #40's default for the other admin lists | A questionnaire's session count is unbounded and grows without limit, unlike the "dozens to hundreds" lists #40's default covers, so the whole-array convention doesn't apply and keyset paging — #40's stated exit — is the query shape. Once paging already bounds a page to a small, fixed row count, a virtualized list buys nothing: virtualization exists to keep the DOM cheap when hundreds or thousands of rows are mounted at once, and this design never mounts more than one page's worth. Revisit only if the real admin reporting surface (design doc §18.8, still undesigned) needs a materially larger page. Amended by #90: the order is no longer fixed, and `Newer` / `Older` became `Previous` / `Next` | 2026-09-18 |
| 89 | **`qp_reporting` — a new, dedicated read-only database role, not a widened `qp_definition` grant — backs the admin responses browser** ([gh#18](https://github.com/kenziesimpson/questionnaire-platform/issues/18)). Fourth login role (beside `qp_owner`, `qp_definition` and `qp_execution`), third backend pool, third encapsulated Fastify plugin (`src/modules/reporting`, mounted at `/api/reporting`). Its whole grant surface is `SELECT` and nothing else: on `execution.session` and `execution.response` (migration `0017`), and, for the two reads the plugin needs beyond them, on the `definition.published_questionnaire_version` view and on the single `id` column of `definition.questionnaire` (migration `0018`), plus `USAGE` on the `definition` and `execution` schemas. It holds no `INSERT`, `UPDATE`, `DELETE` or `TRUNCATE` on anything, no grant on any base authoring table or on `questionnaire_version`, and nothing in `audit`. The reporting module holds **only** this pool — it does not borrow `qp_execution`'s — so the module as a whole cannot write to the database, by grant and not by code discipline. Nothing `qp_execution` or `qp_definition` already hold is touched | Granting `qp_definition` `SELECT` on `execution.response`; having the reporting module borrow the `qp_execution` pool for the pinned-snapshot and questionnaire-exists reads (shipped first, reversed in review — that pool holds `INSERT`/`UPDATE` on `session` and `INSERT` on `response`, so the module would have been write-capable by convention rather than by grant, with a larger total capability than the read-only grants it avoided); granting `qp_reporting` the whole `definition.questionnaire` table (the existence check needs only `id`, and the row also carries the name, `closes_at` and the current-version pointer); one combined `qp_reporting`+`qp_execution` role | [[9-database-schema]] and [[7-application-boundary#3.2 Database grants]] call out, by name, that `qp_definition` holding no grant on `response` is the barrier a reviewer is least likely to expect — the authoring surface is not a back door into answer data, which in this domain is medical history. Widening that grant to ship a browsing screen would break the one guarantee the schema was explicitly designed to keep structural rather than remembered. Design doc §18 Open Question #8 and [[7-application-boundary#10. Open questions]] §2 already anticipated a third, later, read-only surface over session/response data for exactly this shape of need; this is the first slice of it, scoped narrowly on purpose — no domain-event access, no audited export. The measure that matters is what credentials the *module* holds, not only what its own role is granted: borrowing `qp_execution` to avoid two extra read grants would have made `modules/reporting` a surface that chooses not to write instead of one that cannot. `0010` is the precedent — it grants the published-only view to `qp_execution` after revoking it from everyone else — so granting the same view to a second, read-only role is the established shape. What it costs: `qp_reporting` can read published snapshots (question wording, options, rules; never answers), which `qp_execution` already can, and the backend gains a third pool, which moves the pooler trigger from the fifth process to the fourth (#78). A merged role would blur two separately-reasoned grants into one identity and make a future audited-export role harder to carve out cleanly | 2026-09-18 |
| 90 | **The admin responses list sorts by either timestamp, in either direction, and pages by keyset over the chosen column plus `id`** ([gh#18](https://github.com/kenziesimpson/questionnaire-platform/issues/18); amends #88, whose `(started_at DESC, id)` order and `Newer` / `Older` buttons it replaces). The list query takes `sort` (`started` \| `submitted`, default `started`) and `order` (`asc` \| `desc`, default `desc`) as strict enums; the admin URL carries them only when they differ from the default. **The total order** is the chosen column, then `id` in the same direction; `submitted_at` is `NULL` for an in-progress session, and **`NULL`s sort last under `submitted` in both directions**, ordered among themselves by `id`. Paging is direction-agnostic: `nextCursor` / `previousCursor` replace `olderCursor` / `newerCursor`, the buttons read `Previous` / `Next` (`Previous session` / `Next session` on the detail screen, which takes its neighbours from the same list query and so follows the same order), and a cursor is the base64url of `direction\|sort\|order\|value-or-null\|id`. Postgres row comparison does not work over a `NULL`, so a page is one or two statements with explicit predicates: the row comparison `(col, id) > (v, i)` for the non-null run, `col IS NULL` for the tail after it, `col IS NULL AND id > i` inside the tail, and `col IS NOT NULL` for the run behind it when paging backward from the tail. When two statements are needed they run in one `REPEATABLE READ` read-only transaction, so a page never straddles a session submitting mid-request. A `status` filter removes the segment it makes impossible (a `submitted` session has a `submitted_at`, an `in_progress` one has none), so a filtered page is one statement and no transaction; the second statement and the transaction are used only when the filters leave both null classes possible. Under `status=in_progress` the cursor-less first page carries `submitted_at IS NULL` as well: the `session_state` check makes it equivalent to the status, so it never changes a result, and it is the predicate that lets the planner seek the `NULL` tail of the sorted index instead of walking the submitted history; every later page already carries it in its own segment, so no statement carries it twice. **Sort and order come from a closed enum mapped to fixed columns and fixed SQL fragments — user input is never interpolated.** A cursor is validated field by field on decode (direction, sort and order from their enums, a canonical instant or the null marker — accepted only under `submitted` — and a uuid) and must match the request's `sort` and `order`; **a mismatched, stale or forged cursor reads as no cursor and the request is served as the first page** — `200`, never a Postgres error, and consistent with #88's existing treatment of a malformed one. Migration `0019` adds two indexes, `session_by_questionnaire_submitted_asc (questionnaire_id, submitted_at, id)` and `session_by_questionnaire_submitted_desc (questionnaire_id, submitted_at DESC NULLS LAST, id DESC NULLS FIRST)` (the `NULLS FIRST` on `id` is what the migration emits and is immaterial, `id` being `NOT NULL`); `started_at` needs no new one, being `NOT NULL` | Sorting the page the client already holds (a sort that reorders 20 of N rows is wrong, not partial); `NULLS FIRST` under descending, which is Postgres's own convention and lets one index serve every direction, but puts every unfinished session above the newest submission in "newest submitted first"; `COALESCE(submitted_at, 'infinity')` or a `status`-first sort key, which reach the same order through an expression index or a second scan and are no simpler than two plain indexes; a single `UNION ALL` statement, whose outer `ORDER BY` would re-sort at most 42 rows to restate what the branches already ordered; the `OR` form of the keyset predicate, which is a filter and re-walks every earlier page (#88); a partial index over `submitted_at IS NOT NULL` with the in-progress tail served by `session_in_progress`, which cannot order the tail by `id`; `OFFSET`; treating a mismatched cursor as a `400` | Sorting was asked for by name on the two timestamps, and the design would not bend to a client-side answer: a questionnaire's session count is unbounded (#88), so the sort has to be the query's own `ORDER BY`, on an index, or it degrades into a full sort behind every page. Total order is the hard part. A session that has not been submitted has no submit time, and the two natural choices are both defensible; `NULLS LAST` was chosen because the screen's question under "Submitted" is "what has come in", and an unfinished session above the newest submission answers a different one. It costs a second index: a btree scanned backward reverses its `NULL` placement, so `(submitted_at, id)` serves ascending-`NULLS LAST` forward and descending-`NULLS FIRST` backward, and descending-`NULLS LAST` needs its own definition — the four orders the query issues (both directions of both `order`s) are exactly two forward and their two reverses. The cost is two more index entries per session insert and per submit; the submit `UPDATE` was already not a heap-only update, since `status` and `last_activity_at` sit in `session_in_progress`. Both were checked with `EXPLAIN` against a Postgres holding 18,000 sessions of three questionnaires, and the plan test in `_tests/db/reporting/sessions.test.ts` keeps asserting it. **That holds for the unfiltered shape:** with no `status` or `version` filter, every sort, order and direction is an `Index Scan` on the expected index in the expected scan direction with the keyset in its `Index Cond` and no `Sort` node. **The filtered shapes are a weaker claim.** None of the three indexes carries `status` or `version`, so each is a post-filter on the scan, and post-filtering an ordered scan defeats its `LIMIT` whenever the wanted rows are not spread through it. Measured with `EXPLAIN (ANALYZE, BUFFERS)` on 36,000 sessions (30,000 in the questionnaire under test, 3,000 in each of two others; 10 in progress and 5 on a rare second version): `status=submitted` and the common version are the same sorted-index scan with the filter removing 0 or 1 rows, 24 to 26 buffers a page. `status=in_progress` under `sort=submitted` is a sorted-index scan with `submitted_at IS NULL` in the `Index Cond` on every page, first page included (13 buffers a page with 10 in progress, 24 with 300, 1,500 or 6,000 in progress). Under `sort=started` no index orders the filter's rows, so it is planned as `session_in_progress` under a top-N `Sort` when few are in progress (10 to 17 buffers) and otherwise as a walk of `session_by_questionnaire` that discards submitted rows in proportion to the in-progress share (about 21 divided by the share: 2,079 rows removed and 50 to 66 buffers at 1%, about 400 and 12 to 16 at 5%, about 84 and 5 to 7 at 20%); that is the planner's crossover between two plans, not a walk of the whole history, and the predicate below does not apply to it. **One filtered shape was pathological and is fixed; one is unbounded and is not.** (1) The first page of `sort=submitted&status=in_progress`, in either order, was planned from row-count statistics that assume the wanted rows are spread through the index, when they are exactly its `NULL` tail. The planner chose `session_in_progress` with 150 in progress (0.5%; 151 buffers) and flipped to the sorted index at 300 (1%), where it walked every submitted entry before reaching the tail: 30,013 buffers, 29,700 rows removed by the filter and 5 to 8 ms at 1%, 28,800 buffers and 28,500 rows removed at 5%, 24,257 buffers and 24,000 rows at 20%, against 24 buffers and 0.06 ms for the page it returns, on every load of that first page and growing linearly with the submitted history. **The fix is the predicate `submitted_at IS NULL` on that first page whenever `status=in_progress`** (`firstPage` in `db/reporting/keyset.ts`, so the keyset segment and the filter cannot emit it twice). It cannot change a result, and it puts the `NULL` tail in the `Index Cond`: 24 buffers and 0 rows removed by the filter at 1%, 5% and 20%, in either order (13 buffers at 10 in progress, where it replaces the `session_in_progress` plan). The plan test seeds all four populations and fails on the code without the predicate. The symmetric `submitted_at IS NOT NULL` under `status=submitted` was measured and left out: the submitted run is the front of both sorted indexes, so the scan meets no in-progress row before it has its page, the extra `Index Cond` changed no plan, buffer count or row count in any sort, order or direction, and a page backward from the tail already carries the predicate in its keyset segment. One residual of the same family is not touched by the predicate: the tail pages of that shape (`submitted_at IS NULL AND id > $`) are chosen from the same statistics, and under sampled statistics with random ids one run of the plan test at 5% in progress planned one of them as `session_in_progress` under a `Sort` and read all 1,500 in-progress sessions (bounded by the in-progress count, not the history); the test therefore seeds deterministic ids and analyses with full statistics (`default_statistics_target = 1000`) so its plans do not vary between runs. (2) A rare `version`: no index has `version`, and the planner reads all of `execution.session` (every questionnaire, not only this one) for every page under every sort, as a `Seq Scan` with `Sort` on top (35,995 rows removed, 554 buffers, 1.6 to 2.7 ms) or, from some cursors, a bitmap scan of the questionnaire's entries (226 buffers); the plan test pins only that no other node type appears. (2) is linear in table size, invisible at this size and a real cost at a large one; the alternatives, an index carrying `version` and a `status`-led partial index for the tail, are a different design and are left for the reporting surface (§18.8). Unresolved on purpose: `submitted_at` is set once, `NULL` to a value, so a session that submits between two page requests can appear twice or once fewer across them — the ordinary cost of keyset paging over a column that changes, bounded here to one transition per session and never a fault inside one request. The other place that shows is an empty page: a well-formed cursor past the end of the list, or a session that submits while an admin is on a later page, answers `200` with no items and no cursors, and the screen says the page is empty (not that nothing matches) and offers a Back to the first page button that drops the cursor and keeps the filters and sort, so the reader is never left to edit the URL. The list query keeps the previous page on screen while the next one loads (`placeholderData: keepPreviousData`), so a sort or page click does not unmount the button that was pressed and keyboard focus stays on it. **Known bug, deliberately not fixed in this change ([issue #120](https://github.com/kenziesimpson/questionnaire-platform/issues/120)):** the cursor carries a millisecond `Date`, and that is lossless only because the application writes both timestamps itself as millisecond `Date`s, at two call sites, `startSession` in `db/execution/sessions.ts` (`started_at` and `last_activity_at`) and `markSubmitted` in `db/execution/submit.ts` (`submitted_at` and `last_activity_at`). The schema does not enforce it: `started_at` and `last_activity_at` both carry `DEFAULT now()` (`0000_schema.sql`), which writes microseconds, and the `aSession` test fixture inserts through it. If a microsecond value ever reaches a cursor it is truncated to the millisecond, and the `(col, id)` tiebreak can then skip a row or return one twice across a page boundary | 2026-09-18 |
| 91 | **The admin responses list ([gh#18](https://github.com/kenziesimpson/questionnaire-platform/issues/18)) does nothing clever about sessions that arrive, or submit, while an admin is browsing it.** Each request returns the state of the database at the moment of that request, and nothing else is layered on: the list has no `refetchInterval`, no server push (SSE or websocket), no "N new sessions" indicator, and a cursor carries no snapshot or `as_of` bound. An admin sees a new session by reloading, returning to the first page, or changing the sort or a filter. The one automatic refetch is TanStack Query's stock behaviour, which the list's `useQuery` does not tune: a stale key refetches when the window regains focus, the connection returns, or the screen mounts again (opening a session and coming back), so the page on screen can change when the admin returns to it, never while they are looking at it. This is deliberate, not an oversight. **What the keyset paging of #88 and #90 does guarantee on its own:** under the default Started order a new session is the newest, so it sorts ahead of every cursor already issued and never shifts the pages behind them: `Next` after new sessions arrived repeats no row and skips none (the failure `OFFSET` paging would have), and `Previous` from the second page returns the same twenty rows, still with a page ahead of them, which is where the newcomer waits (a new session's `started_at` is the application's clock at insert, so this holds while that clock does not run backwards past a start already listed; a newcomer that shares the last listed row's millisecond sorts by its random v4 `id` and may land behind the cursor, on the next page, which still repeats and skips nothing already listed). Under Submitted order there is no such guarantee, and #90 already records why: a session that submits between two requests moves its `submitted_at` from `NULL` to a value, so it can appear twice or be missed once across them | Polling with a `refetchInterval`; server push; a "N new since you loaded" banner, which needs a cheap count or a high-water mark, and #88 rules out counts; a snapshot bound carried in the cursor (`started_at <= T` / `submitted_at <= T`), which would make one walk fully consistent at the price of a value the client holds and a filter on every page query; holding one long read transaction open across requests, which a stateless API and a pooler in transaction mode (#78) cannot do | The responses list is an admin browsing screen over a prototype, not a live monitor. The keyset choice already removed the worst failure, duplicated or skipped rows on the default order, and each alternative adds state, a moving part, or a query the design has deliberately avoided (#33): a timer and a request budget, a connection held per viewer, a count, or a bound to carry and validate in the cursor. Those refetches are left as the library default because it costs nothing and never changes a page mid-read; it is not a freshness feature and nothing depends on it. Revisit only if the real reporting surface (§18.8) needs a live view or a fully consistent walk, in which case the snapshot bound is the smallest of these. `e2e/specs/tier-3/e31-responses-list-new-arrivals.spec.ts` walks the whole behaviour: a session created while page one is open leaves that page unchanged, `Next` shows exactly the three remaining sessions, `Previous` returns the original page with one more behind it, and a reload puts the newcomer first | 2026-09-18 |
| 92 | **Telemetry leaves the application only through OpenTelemetry to a Collector, and we run no telemetry store of our own.** Nothing is written to the application's Postgres, which holds the answers telemetry must never contain. The Collector's exporter list is where a store is added; for local viewing the opt-in `observability` profile runs a Collector and `grafana/otel-lgtm` (planned, P1). Refines #16. Decisions O1, O9, O15 in [[6-observability#13. Decisions and open questions]] | A vendor agent in the application; telemetry tables in the application's Postgres; a store built for the project. An analytics store for domain events is [gh#118](https://github.com/kenziesimpson/questionnaire-platform/issues/118) | The Collector is the one component that knows the vendor, so the application never does (#16). The application's database is the one place a leaked answer would be most damaging, and a telemetry store there would put it beside the answers | 2026-09-18 |
| 93 | **The server owns `session.item_skipped` and `session.question_answered` for submitted sessions**, emitted from the path it already evaluates at submit. The browser emits only what the server cannot see, chiefly `session.abandoned`. Both carry the session id, so a session's client and server events join on it. Amends the account of which side emits which event in [[6-observability]] §4 and §6. Decision O11 | The browser emitting every navigation event, as first designed | The server already evaluates visibility at submit and is the trusted side, while client telemetry is spoofable. The cost: a skip is recorded only for a session that submits, so an abandoned session's skips are not seen | 2026-09-18 |
| 94 | **Session ids stay in traces and logs as named fields, and never in a URL that telemetry records.** Spans drop `url.path` and `url.full` and keep `http.route`, and the nginx access log masks session ids in paths and the list endpoint's `cursor` (which carries one) — planned, P1. Admin telemetry names a screen by its route template. Decisions O13, O19 | Dropping the session id from telemetry; hashing it | A session id is a bearer credential (#25), so a URL that carries it is a credential in a log. It is also the join key for "why did this respondent get stuck", so it stays as a field, where it can be found and is not part of a path | 2026-09-18 |
| 95 | **Reading a response's raw answers writes a `view_response` audit row in the same transaction as the read**, so a failed audit write fails the read. Only the session-detail read writes it; the list returns counts, status and timestamps and writes none. `qp_reporting` gains `EXECUTE` on `audit.record` for it. Extends #89, which is not edited here. B4 lands it; A1 (planned) narrows the grant so `qp_reporting` can record only this action. Decisions O14, O18 | A log event alone; auditing every list read | Reading raw answers is the access an audit trail exists to record, and a log line can be lost or rotated. The list returns only counts, status and timestamps, so it has no raw answer to record | 2026-09-18 |
| 96 | **Six alerts ship with the telemetry work**, pulled forward from the deferred SLOs: submit success rate, 5xx rate, p95 latency of the definition fetch, event-loop lag, pool queueing, and fewer than one month of future `response` partitions. SLOs and error budgets stay deferred. Amends the deferral of alerting in [[6-observability#8. SLOs and alerting]]. Decision O10 | Deferring all alerting with the SLOs | The six are the failures a respondent would feel and nothing else would report, the partition one especially: a missing partition fails every submit while every process is up | 2026-09-18 |
| 97 | **The `/api/telemetry` ingest sits behind a per-address rate limiter, with `trustProxy` set to one hop**, so the limiter sees the client's address behind nginx. Planned, B2. Decision O21, which B2 adds to [[6-observability#13. Decisions and open questions]], where its alternatives and reasoning are recorded. Partly answers §18 question 9 for this one endpoint | See O21 | The endpoint is unauthenticated and its input spoofable ([[6-observability#6. Client-side telemetry]]). Behind one nginx proxy, a limiter would otherwise key every request to the proxy's address, which is what `trustProxy` prevents | 2026-09-19 |

### 17.1 Alternatives considered in detail

Expanded notes on the options we weighed for decisions #1–#4, so the reasoning survives beyond the one-line table.

**Frontend / full-stack framing**

- *Next.js (unified frontend + backend).* Considered for fewer moving parts: one repo, one container, route handlers as a free API, NextAuth ecosystem. Rejected because its strengths (SSR, SEO, RSC streaming, auth integrations) don't apply to an internal admin tool and a respondent flow with auth out of scope, while its costs do — App Router / server components / caching semantics carry a learning curve, and the framework blurs the definition-vs-execution boundary that the brief explicitly grades. Domain logic would still need its own module structure, ORM, migrations and tests, so little is actually saved on the backend side.
- *Create React App / other React scaffolds.* Not viable; CRA is unmaintained. Vite is the current default and has the shallowest ramp.
- *Server-rendered admin (e.g. templates from Fastify).* Would reduce JS but make the respondent's dynamic next-question flow clunkier; a SPA is the natural fit for a stateful form.

**Backend framework**

- *NestJS.* Attractive because its module/DI structure makes the authoring / publishing / execution split very visible to a reviewer. Rejected for boilerplate volume and ramp time relative to the project size; we can achieve the same visibility with Fastify plugins/encapsulation and a deliberate folder layout.
- *FastAPI (Python).* Strong service ergonomics (Pydantic validation, auto OpenAPI). Rejected because a second language means API types are generated (openapi-typescript) instead of shared from a common package, and two toolchains complicate the one-command setup.
- *Express / Hono.* Express is the incumbent but lacks built-in schema validation and has a weaker plugin story; Hono is lightweight but its Node/OTel/pino ecosystem is thinner than Fastify's.

**Runtime**

- *Bun as server runtime.* Considered for speed and its built-in Postgres client. Rejected: the workload is I/O bound so runtime speed is not a lever; real scaling levers are pooling, indexing, caching published definitions, and read replicas. Bun runs Fastify through a Node-compat layer that can trip on plugins, pino transports and OpenTelemetry instrumentation — the wrong debugging to do in an interview timeline, and "straightforward to run" is graded. Bun remains an option as a package manager / test runner only, and Drizzle's `bun-sql` adapter keeps a later runtime swap cheap.

**Data access**

- *Prisma.* More batteries-included (schema DSL, generated client, Prisma Migrate) and arguably the most reviewer-friendly client. Rejected as heavier than needed and more abstracted from SQL; partitioning, `FOR UPDATE` locks and JSONB queries are more natural in Drizzle.
- *Raw driver only (`pg`, `postgres.js`, or `Bun.sql`).* Rejected because repeatable migrations and a reviewable, typed schema are explicit deliverables; a bare driver would mean hand-rolling both.
- *Other databases (SQLite, MongoDB).* SQLite would simplify local setup but weakens the scaling and concurrency story the brief asks us to defend. A document store fits the questionnaire definition JSON well but makes response history, transactions around publish, and relational integrity between versions and responses harder to model and explain. Postgres covers both with JSONB.

Decisions #6, #7 and #9 concern the questionnaire format; their alternatives are written up in
[[5-questionnaire-format#7. Alternatives considered]] rather than repeated here. Decisions #17–#21 and #25 concern
the application boundary; theirs are in [[7-application-boundary#9. Alternatives considered]]. Decision #22 concerns testing; its alternatives are in [[8-testing#8. Alternatives considered]].

## 18. Open Questions

Ordered roughly by what blocks what.

1. **The v2 demo change — resolved** (Decisions Log #26). Version 2 relabels `opt_hyperten` on `qst_which_condition` from "Hypertension" to "High blood pressure (hypertension)" and changes nothing else; the option id is unchanged, so v1 and v2 responses aggregate together while each renders through its own pinned `questionVersion`. Worked delta and the rejected alternatives in [[5-questionnaire-format#3.1 Version 2 — the demo change]].
2. **Formatting subtypes for `text`** — email, phone, regex patterns. Deliberately pinned rather than rejected; we want these, just not before the vertical slice is complete. Also listed in [[#19. Future Work]].
3. **In-flight sessions at retirement.** The hard cutoff ships now ([[#8.1 Questionnaire lifecycle and retirement]]). Whether `cutoffMode: 'hard' | 'soft'` becomes a per-questionnaire setting is undecided. Also in [[3-scaling#8. Open questions]].
4. **Snapshot format support window.** How many past `formatVersion`s the loader commits to upgrading from, and what triggers dropping support for one.
5. **Pages / sections.** Deferred, not rejected — [[5-questionnaire-format#1. The model]] states the constraint any future design must respect.
6. **Checkpoint endpoint — resolved: deferred** (Decisions Log #25). `PUT /sessions/:id/progress` does not ship. The session id is a bearer capability living in the same browser storage as the partial answers, so the server-side copy is unreachable in the cases that would need it, and the operational half is already covered by domain events. Now carried as [[#19. Future Work]] rather than an open question; the table shape is in [[9-database-schema#12. Open questions]] so it stays additive. Reasoning: [[7-application-boundary#9.7 A debounced checkpoint endpoint]].
7. **Withholding unreachable questions** from the definition payload in sensitive deployments. Already tracked in [[3-scaling#8. Open questions]]; ties to HIPAA future work.
8. **Admin reporting surface.** `qp_definition` is denied any read on `response` ([[#12. Database]]), which is correct and leaves "how does an admin see aggregate results" unanswered. Expected shape is a third read-only surface with its own role over the session record and domain events, with raw answers behind an explicit, audited export. Not designed yet — [[7-application-boundary#10. Open questions]] §2. **The database-role slice of this is now resolved and built:** a narrower prototype — an admin screen that browses individual raw sessions/responses with no aggregation — has shipped as [gh#18](https://github.com/kenziesimpson/questionnaire-platform/issues/18), reading through a new, dedicated `qp_reporting` role that holds `SELECT` and nothing else — on `execution.session`, `execution.response`, the published-versions view and `definition.questionnaire.id` (Decisions Log #89), rather than widening `qp_definition`. It does not resolve the wider question above: still not built are domain-event access, the audited export for raw answers, and any aggregation, server-side filtering beyond version/status (sorting by the two timestamps is built, Decisions Log #90), and the list is deliberately naive about sessions arriving while an admin pages through it (Decisions Log #91), or export on this screen itself. Also still open, carried over from the prototype canvas linked from gh#18: whether opening a session should be audited (today it is not — gh#18's acceptance criteria required no new write path, and an audited read would be one), and who may read this screen at all. The second is the sharper one, and it is about session ids rather than URL length: **`/api/reporting` is unauthenticated (no auth exists anywhere in the prototype, admin included), and its list route returns the ids of `in_progress` sessions, each a live bearer capability for `GET /api/run/sessions/:id` and `POST /api/run/sessions/:id/submit` — so anyone who can reach the admin screen can resume and submit a stranger's in-flight session.** That is a property of the prototype's no-auth posture, not a defect specific to this screen, and it is not mitigated by the UI showing only the first 8 characters of an id (the full id is in the route, the API payload and a list-row tooltip). Detail and the candidate mitigations: [[7-application-boundary#10. Open questions]] §2; the bearer-capability model it strains is [[7-application-boundary#7. Access model and data barriers]].
9. **Rate limiting the execution surface.** `POST /sessions` is unauthenticated and trivially abusable. `@fastify/rate-limit` is a small addition; whether it ships in the prototype or is stated as an edge concern is open, and it interacts with where a service split would put the public ingress.
10. **Version diffing** — `GET /versions/:a/diff/:b`, which would make "what changed in v2" a first-class answer and is directly useful for the mandatory demo. Deferred as additive: both snapshots are already retrievable and the admin app can diff client-side.
11. **Testing scope beyond the graded set.** Property-based testing of the rule engine, accessibility assertions in the end-to-end specs, and mutation testing as the check on whether the rule-engine suite is real, all deferred — [[8-testing#9. Open questions]]. One has weight outside testing: the concurrency claims in [[3-scaling]] are arguments rather than measurements until something load-tests the delivery endpoint.

12. **Frontend open questions** live in [[10-frontend#10. Open questions]]: single-page rendering past demo size, the client side of admin authentication, error and empty states, and how far optimistic updates go in the draft editor.

13. **Discarding never-published drafts.** The one plausible exception to "nothing is deleted" ([[#3. Constraints]]): a draft that was never published is work in progress rather than a record, and an author who abandons one has no way to clear it. **Partly resolved:** removing items *from* a draft is settled by Decisions Log #45 — `qp_definition` holds `DELETE` on `questionnaire_item` only, and the item guard confines it to drafts. Discarding a *whole* draft is still punted — drafts are harmless at prototype scale and the partial unique index already limits them to one per questionnaire — and would need `DELETE` on draft `questionnaire_version` rows plus a decision on `ON DELETE CASCADE` ([[#12. Database]]). Question versions are out of scope here — those are append-only by [[2-design-doc#17. Decisions Log]] #13 regardless.

14. **Timezone semantics for relative date constraints — provisional.** `not_future` / `not_past` are evaluated server-side against UTC today with **one day of tolerance**, while the respondent app evaluates them against the browser's local date (Decisions Log #38, [[5-questionnaire-format#2.4 Relative date constraints resolve against two different clocks]]). A strict UTC comparison would block a correct answer for any respondent far enough east or west, which is an outage; the tolerance accepts a date at most one day beyond true, which is a data-quality rounding error. The exact alternative — the client submits its UTC offset with the session or the submission and the server validates against the respondent's own calendar — costs a field on the wire, a decision about trusting it, and a decision about storing it so the check stays reproducible later. Worth revisiting if there is time; not worth the machinery before the vertical slice is complete.

15. **Who published a version — deferred with authentication** (Decisions Log #64). `VersionSummary.publishedBy` is `null` and the admin history screen renders it as absent. `questionnaire_version` has no publisher column and `qp_definition` cannot read the publish audit row; adding `published_by` needs a migration and a change to `promote_draft`, worth making once real identities exist rather than to store `prototype-author` (#57).

16. **Two authoring bugs, one partly brought into Wave 3** (Decisions Log #65, #75). [gh#17](https://github.com/kenziesimpson/questionnaire-platform/issues/17): a question archived after it is placed in a draft blocked every `PUT /draft` for that questionnaire, with no unarchive to recover. **Fixed in Wave 3 by #75:** archiving gates new placements only, so an existing placement no longer blocks saves or publish, and the draft editor shows no archived state. Still deferred: an unarchive route, the picker showing archived questions already placed in the open draft, and whether archiving warns about open drafts. Until the picker change, an archived question removed from a draft cannot be put back. [gh#15](https://github.com/kenziesimpson/questionnaire-platform/issues/15): a taken `key` returns `500`, and `key` has no reader, so it may be removed rather than mapped to a `409`.

17. **"Start over" on a resumed respondent session — punted** (Decisions Log #74, [gh#35](https://github.com/kenziesimpson/questionnaire-platform/issues/35)). `RespondentStart` draws the button; Wave 3 does not build it. When it is built it clears the answers and keeps the same session, since a new session would leave two in progress for one respondent.

## 19. Future Work

- **Close the duplicate `option_ids` shape in the database.** An `IMMUTABLE` helper wrapping `SELECT count(DISTINCT e) FROM unnest(a) e`, called from the `multiple_choice` branch of `response_shape`, is the one remaining thing needed to make every invalid answer shape unrepresentable rather than merely rejected. Deferred under Decisions Log #33 and #34 because a correct client cannot produce the shape and the validator already covers it; it returns as one custom migration with no application change — [[9-database-schema#13.8 An `IMMUTABLE` helper closing duplicate `option_ids`]].
- **Split the frontends.** Deploy the respondent questionnaire app and the admin portal separately; the questionnaire app is potentially hit much harder and benefits from a CDN, aggressive caching and edge rate limiting, while the admin portal stays behind auth. Keep them as separate entry points/packages from the start so this is a deploy change. See [[3-scaling#6. Future improvement: split the frontends]].
- **Split definition and execution into separate services.** Chiefly a security improvement: the authoring API then need not be routable from the public internet at all, and a vulnerability reached through the unauthenticated respondent surface would land in a process whose database role cannot touch authoring tables and whose network cannot reach the admin service. Also buys independent scaling (respondent traffic is orders of magnitude higher) and decoupled deploy cadence, so authoring releases stop restarting the surface that collects medical answers. The prototype deliberately keeps this a deployment change rather than a rewrite — see [[7-application-boundary#8. Deployment topology]].
- Redis as cache and (separately) durable ingest queue, triggered by measured load. See [[3-scaling#3. Problem: response ingest vs. reads]] §3–5.
- **Checkpoint endpoint and true cross-device resume.** `PUT /sessions/:id/progress`, upserting partial answers on a debounce. It becomes worth its cost once respondents have an identity to look a session up by, rather than only a bearer session id sitting in the same browser storage as the answers it would recover (Decisions Log #25). Additive: one table, one grant and one route, with the shape already recorded in [[9-database-schema#12. Open questions]] and nothing existing to migrate.
- An AI-assist system for initializing a questionnaire
- **Format subtypes for `text`** — email, phone and regex-pattern validation. Pinned deliberately rather than rejected; see [[#18. Open Questions]] §2.
- **Unit conversion for `number`.** Answers already carry the unit they were collected under ([[#5. Questionnaire Format]]), so converting between compatible units (cm/in, kg/lb) for display and analytics is additive rather than a migration.
- **Question translation and locale-specific published snapshots.** Translate question text, option labels and help text, then serve the respondent a `snapshot` JSONB for their locale instead of the one canonical blob assumed today ([[#12. Database]] §2.1, [[9-database-schema#3. `definition`]]). Keys, ids, `constraints` and `visible_when` predicates stay locale-independent — only the human-readable strings vary — so this is additive: one publish producing N locale snapshots (or a base snapshot plus per-locale string overlays) rather than a schema change to the definition model.
- **Erasure on request (GDPR / HIPAA).** The stated exception to [[#3. Constraints]]: removing one respondent's data as an explicitly invoked, audited operation, including from snapshots' derived indexes and cold-storage partitions. Deliberately not a cascade delete.
- **Independently versioned question bank.** Drafts and a publish step on questions themselves, for authors who need to park half-finished edits. Additive over today's append-only model — see [[5-questionnaire-format#7. Alternatives considered]] §7.5.
- **Upgrade a draft to the latest question versions.** A per-question review action, so moving a questionnaire forward does not mean removing and re-adding items.
- **A question version picker in the draft editor.** Choose which version of a question an item pins, rather than taking whatever was current when it was added. This is also the change that makes concurrent-edit divergence (Decisions Log #31) visible at the moment it matters, instead of leaving it to be noticed in version history.
- **React Aria in place of Radix primitives.** Considered and passed over for now ([[10-frontend#9.5 React Aria instead of Radix]]); the migration is deliberately contained, because `packages/ui/primitives/` is the only place Radix is imported and everything else consumes primitives, so a swap happens one primitive at a time behind unchanged props. Two things would trigger it: **localisation**, which brings date, number and unit input that must be correct per locale — React Aria's strongest ground and the point at which native inputs stop sufficing — and a real **screen-reader conformance requirement**, since React Aria carries substantially more platform-specific behaviour.
- **A dropdown presentation for `single_choice`.** Today it renders as a radio group. The control already keeps the answer mapping apart from the view that draws the options, so a select is a second view behind the same props rather than a new control ([[10-frontend#3. `packages/ui` — primitives and the renderer]]).
- **A view listing every current error per question.** The renderer shows only the first `SubmissionItemCode` for an item; a question failing two rules reveals the second only once the first is fixed. Listing all of them is a change inside the renderer's error catalogue and field components, since the `errors` prop already carries the full list ([[10-frontend#3. `packages/ui` — primitives and the renderer]]). Tracked alongside the draft editor's inline error rendering as [gh#22](https://github.com/kenziesimpson/questionnaire-platform/issues/22), Decisions Log #54.
- **A/B testing whole questionnaires.** Noted during ideation; deferred as orthogonal to the versioning model — a published version is already the unit an experiment would assign against.
- **Standalone audit logging service.** Extract the audit trail behind a transactional outbox so it can be operated, backed up and access-controlled independently of the application database. The schema-and-role design shipping now (Decisions Log #16) is deliberately outbox-ready: audit writes go through a single repository function, and audit rows carry their own id and timestamp rather than borrowing the domain row's. See [[6-observability#5.1 Isolation — separate schema with a restricted role]].
- **Invariant monitoring and a synthetic leak test.** Periodic gauges for orphaned answers, never-shown items and live snapshot format versions — drift that returns HTTP 200 and moves no existing signal — plus a leak test completing the medical-condition demo questionnaire end to end, which is the only signal that proves the *workflow* works rather than that the processes are up. Cheap to build here because the demo questionnaire already exists. See [[6-observability#9. Correctness and invariant monitoring]].
- **Deploy and migration markers on dashboards.** Most incidents are change-caused, and "what changed at 14:02" is the first question in all of them.

## 20. Pending UI experimentation

**Nothing is pending.** The two Wave 3 items that needed a design pass before Track 6 could build them — the question editor's constraint fields per response type and the options-authoring widget — are resolved by the Wave 3 prototypes, adopted as Decisions Log #58. Detail in [[10-frontend#5.3 The question editor, and re-pinning]].
